# Project DevLog: MSDS_EXtaction_V3(v24+GUI통합)
* **📅 Date**: 2026-06-14
* **🏷️ Tags**: `#Project` `#DevLog` `#Parser` `#Feature` `#Wako` `#Ammonia` `#Tooltip` `#Reset`
* **📌 Version**: `V24.4.3.19` (이전 버전 `V24.4.3.15` 계승 및 업데이트)

---

> 🎯 **Progress Summary**
> 와코(Wako) 및 암모니아수 등 변칙 부등호 수치 및 세로 나열형 구조에서의 파싱 오류 정합성 전면 정상화 및 GUI 테이블 리셋, 대문자 Rem.% 일원화, 신호등 풍선도움말(Tooltip) 스코어링 시스템 구축 완료.

### 🛠️ Execution Details & Changes
* **Git Commits**: 완료 (최종 수술 완료 후 작업 확정 커밋 완료)
* **Core File Modifications**:
  * 📄 `msds_engine_v5.py`:
    * 와코사 변칙 부등호 교정 추가 및 세로형 지형 성분 분리 차단막, 대제목 유령 숫자 배제 가드레일을 적용하였습니다.
    * 영문 변종 잔량 키워드(`residual`)를 추가하고, AI 분석 품질 점수 및 정형 태그를 연산하는 공간 제로형 툴팁 스코어링 장치를 탑재했습니다.
  * 📄 `msds_core.py`:
    * 잔량 키워드 가공 공정 내에 `"residual"` 키워드를 추가하고, `rem.%` 등의 잔량 출력이 일관된 대문자 `"Rem.%"` 포맷으로 교환 정합되도록 처리했습니다.
  * 📄 `smu_gui.py`:
    * GUI 엔진 새로 고침("엔진 🔄") 버튼 입력 시 기존 테이블 데이터를 깨끗이 비우도록 리셋 로직(`self.table.setRowCount(0)`, `self.results = []`)을 보완했습니다.
    * `ExtractionWorker`가 엔진의 스코어링 정보를 수집하여 전달하고, 테이블 렌더링 시 신호등 셀(0번 열)에 실시간 풍선도움말(`setToolTip`)이 팝업되도록 연동했습니다.

### 🚨 Troubleshooting (Mistake Log)
* 🐛 **Problem 1 (와코 문서)**: `=<100` 수식 기호 누락으로 인해 와코사의 100% 수치가 누락되거나 미기재로 분석되는 현상.
  * 💡 **Cause**: `_normalize_single_content` 내에서 전각 기호 등은 변환하고 있었으나 와코 특유의 `=<` 등 변칙 기호가 치환 대상에서 빠져 있어 부등호 인식 파이프라인에서 수치가 거부됨.
  * 💡 **Solution**: 변칙 부등호 치환 로직을 명시적으로 추가하여 이를 해결함.
* 🐛 **Problem 2 (암모니아수 문서)**: 세로 나열식 지형의 성분 텍스트가 이전 행으로 무단 흡수되어 함량 배달 사고가 발생하고 미기재%가 되는 현상.
  * 💡 **Cause**: 표 구조가 아닌 세로 리스트형 문서에서 아래쪽 성분의 지시어가 Y축 40픽셀 울타리 내에 들어와 이전 성분 행에 잘못 병합됨.
  * 💡 **Solution**: 새로운 성분명이 발견되면 무조건 병합을 중단하도록 단절 조건을 주입하여 성분 구역을 독립 격리함.
* 🐛 **Problem 3 (영문 잔량 잔존)**: 영문 MSDS에서 `residual` 등 변종 표기로 유입된 잔량이 미기재% 환각에 빠지거나 소문자 `rem.%`로 부조화스럽게 노출되는 현상.
  * 💡 **Cause**: `residual`이 키워드 필터에서 제외되어 있어 `else` 공정으로 분기되었으며, `msds_core.py`에서 강제 소문자 세탁을 거쳐 `rem.%` 형태로 최종 유지됨.
  * 💡 **Solution**: 수집 엔진 및 코어 공정 내 `residual` 토큰 감지를 동기화하고, 최종 빌드 시 `Rem.%`로 대소문자를 정교하게 교정되도록 처리함.

### ⏭️ Next Steps
- [x] v24.4.3.19 수술 도면 적용 및 회귀 테스트 100% 통과 확인.
- [x] 신호등 툴팁 스코어 및 새로 고침 시 테이블 초기화 동작 확인.
- [ ] 실제 GUI 및 업무 기동 환경에서 대규모 PDF 묶음 정밀 검증 진행 대기.
