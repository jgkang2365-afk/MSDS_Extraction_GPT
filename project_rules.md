# project_rules.md

## 마스터 룰북 (MSDS_EXtaction 프로젝트 개발 규칙)

### 1. 함유량 및 텍스트 세척 규칙 (clean_content_text)
- 한글이 혼용된 특이 수치 범위어(예: "이상", "미만")가 함량 필드로 유입될 경우, 하류 파서 및 수신단과의 호환을 위해 반드시 표준 물결 기호(`~`) 형태로 세척하여야 합니다.
- **정형화 규칙**: `(\d+)\s*이상\s*~\s*(\d+)\s*%?\s*미만` 패턴 매칭 시 이를 `\1~\2%`의 정밀 포맷으로 변환합니다.
- **범위형 상한선 1.0 미만 보존 규칙 (V24.5.1.0)**: 범위형 함량 정제 시 상한 수치(Upper Bound)가 정확히 1 또는 1.0인 경우에만 미만 기호를 `<`로 변환하여 그대로 보존(예: `0.1~<1%`)해야 하며, 이외의 모든 상한 수치(예: 0.5, 5, 20 등)는 미만 기호 및 문구를 완벽히 제거하고 순수한 수평 범위 형태(예: `15~20%`)로 평탄화해야 합니다.
- **하네스 안전 잠금 (HARNESS_SAFETY_LOCK)**: 시스템의 회귀 테스트 스크립트 실행 시, 상한선 1.0이 아닌 다른 수치 범위(예: `15~<20%`, `1~<5`)에서 미만 기호가 제거되지 않고 새어 나갈 경우 반드시 즉시 `RuntimeError`를 발생시켜 전체 시스템 작동을 격리 차단해야 합니다.

### 2. 밀도 CLUSTERING 및 텍스트 추출 행 격리 규칙
- **동적 세로 격리**: 테이블 클러스터링(`extract_table_by_density_clustering`) 및 텍스트 정밀 추출(`extract_from_text_regex`)의 모든 행 분리 로직에서 절대 수치 하드코딩(3.0포인트 등)이나 고정비 겹침 비율을 금지하며, 비교 대상 활자 상자의 높이 중 작은 쪽의 25% 비율(`min_height * 0.25`)을 동적 수직 임계값으로 적용하여 성분들이 한 문장으로 뭉개지는 현상을 격리 차단합니다.
- **시각적 정렬**: 수직 행 분류가 완료되거나 `physical_lines` 빌드가 끝나 하류 파이프라인으로 넘기기 직전, 반드시 각 행의 대표 세로 좌표(`y` 또는 중간값)를 기준으로 완벽한 오름차순 정렬을 강제 집행하여, PDF 내부 장부 순서 노이즈를 파쇄하고 위에서 아래로 향하는 시각적 순서 무결성을 확보합니다.
- **동적 가로 분할**: 고정 상수(0.6배 등)를 배제하고, 페이지 내부 전체 활자 간의 격차 중간값과 활자 높이 중간값(`base_scale`)에 연동되는 동적 가로 여백 비율(`median_gap / base_scale`)을 실시간 계산하여 셀을 분할합니다.
- **오염 방지**: 100을 초과하는 순수 숫자(예: 거대 INCI ID, Monograph ID 등)가 함유량 칸에 오염 인입되는 것을 즉각 차단하기 위해, 함량 추출 시 100을 초과하는 단독 숫자는 배제 및 기각 처리합니다.

### 3. 파일 경로 처리 안전성 규칙
- 윈도우 OS 개발 및 검증 환경에서 한글이 포함된 파일명을 다룰 때는 파일 시스템 인코딩 차이(NFC/NFD 등)로 파일 미존재 예외가 발생할 위험이 있으므로, 절대 경로를 그대로 하드코딩하기보다 `glob` 모듈을 이용하여 특정 파일 고유 번호(예: `*036*.pdf`) 패턴으로 실제 인코딩된 경로를 안전하게 수집하십시오.

### 4. 파이썬 컴파일 캐시 꼬임 방지 규칙
- 소스코드에 구조적인 수정이 가해질 경우, 파이썬 인터프리터가 예전 런타임 캐시를 재참조하여 오프셋 꼬임이나 `NameError`를 발생시키는 것을 막기 위해 반드시 `__pycache__` 폴더를 삭제(OS 쉘 명령어 또는 파이썬의 `shutil.rmtree` 사용)하여 깨끗한 상태에서 재컴파일을 수행하십시오.

### 5. 계단식 라우팅 및 1선 바이패스(Bypass) 아키텍처 (V24.5.4.0)
- **1선 바이패스**: 텍스트 문서 분석 시, 1선 격자/정규식 규칙 엔진만으로 유효 성분을 완벽히 확보했다면 AI 호출 단계를 완전히 생략(Bypass)하고 완료 단계로 직행해야 합니다.
- **텍스트/이미지 차선 분리**: 디지털 글자 수 10자 미만은 이미지 문서로 격리하며, 이미지 문서에서는 노비타 딥시크 API 호출을 원천 금지합니다.
- **완전 스캔본 다이렉트 패스**: 디지털 글자 레이어가 완전히 상실된 이미지 PDF(is_scanned_strict)의 경우 경량화 비전 모델(Gemini-2.5-Flash-Lite)의 호출을 원천 금지하고, 정식 Gemini 2.5 Flash를 1선 엔진으로 다이렉트 격발하여 한 방에 미세 부등호 기호를 누락 없이 인지하도록 설계해야 합니다.
- **야간 배치 모드**: 야간 배치 감지 시(18시~08시 혹은 Batch 환경변수 활성화) 노비타 딥시크 API 타임아웃 한도를 1.5초로 극단 제어하고, 에러/타임아웃 발생 시 즉시 3선 백업(Gemini Lite -> GPT-4o-mini)으로 토스해야 합니다.
- **우선순위 수치 병합 가드**: ODL 및 밀도 클러스터링 병합 시 기존 1선 장부에 이미 구체적 성분 수치가 존재한다면 후순위의 투박한 수치가 기존 데이터를 덮어쓰지 못하도록 엄격히 가드해야 합니다.
- **부등호 품질 게이트 상호 교차 검증**: 이미지 비전 신뢰도 검증 시, 원본 PDF 텍스트 장부(`local_grounding_text`) 내에 미만/이상/꺾쇠 기호가 실제로 존재하는 경우에만 결과물의 부등호 존재 여부를 비교 검사해야 하며, 원본에 부등호가 없는 청정 자재 문서의 경우 불필요하게 3선(GPT-4o-mini)으로 승격하여 비용이 낭비되는 일이 없도록 제어해야 합니다. (단, 완전 스캔본의 경우에는 품질 게이트를 check_omission 단일 판정으로 단순화하여 GPT-4o-mini 승격선을 축소 정돈합니다.)
- **로컬 OCR 하이브리드 비전 라우팅 (V24.5.5.0)**: 완전 스캔본 지형(`is_scanned_strict == True`) 진입 시, 외부 클라우드 비전에 거대 이미지를 업로드하는 비전 API를 완전히 차단합니다. 대신, 로컬에서 인텔 CPU OpenVINO 가속을 얹은 `PPStructure` 엔진을 선행 구동하여 표 영역 데이터를 HTML 텍스트 문자열로 깔끔하게 복구합니다. 복구된 HTML 데이터를 최종 LLM 입력용 프롬프트에 병합하여 이미지 전송 비용 없이 텍스트 요금 사양(Text Token)으로 통신을 집행합니다. 로컬 추론 시 발생하는 텐서 메모리 자산 힙 누수를 방지하기 위해 추론 세션 완료 즉시 파이썬 가비지 컬렉터(`gc.collect()`)를 수동으로 기동해야 하며, 로컬 하드웨어 장애나 라이브러리 잠금 예외 발생 시에는 시스템 마비를 예방하기 위해 즉각 비상 예비 클라우드 멀티모달 비전 라인(Gemini 2.5 Flash -> GPT-4o-mini 이중 연쇄 백업)으로 무정차 이송하도록 이중 안전 밸브를 상시 연동하십시오.

- **가상환경 파이썬 인터프리터 격리 및 잔여 의존성 완착 (V24.5.5.1)**:
  - **가상환경 격리 확보**: ODL CLI 등 외부 모듈이나 CLI 도구를 서브 프로세스로 구동할 때는 단순 문자열 `"py"` 나 `"python"` 대신, 반드시 현재 가상환경의 최상위 주소인 `sys.executable`을 사용하여 현재 실행 중인 파이썬 인터프리터 환경을 그대로 상속하여야 합니다. 이를 통해 가상환경 밖의 시스템 파이썬이 실행되면서 발생하는 모듈 누락(ModuleNotFoundError) 현상을 원천 방어하십시오.
  - **GUI 의존성 보증**: GUI(smu_gui.py) 내의 엑셀 자동 정화 기능 및 프로세스 연동이 정상 작동하도록 가상환경 내에 `pywin32` 패키지를 누락 없이 완착하여 윈도우 OS API와 정상 소통하게 유지하여야 합니다.

- **하이브리드 크롭 가속 및 데이터 무결성 롤백 (V24.5.5.2)**:
  - **선택적 크롭 가속**: PDF 문서 처리 시 비전 정찰병(Recon)을 가동하여 성분표가 위치한 페이지 및 좌표 구역(`table_quadrant`)을 식별한 후, 해당 구역만을 넉넉한 마진을 두어 크롭(Crop)하여 로컬 Paddle 엔진에 고속 피딩합니다. 이를 통해 전체 이미지 처리 대비 CPU 연산 부하를 10배 이상 감소시키고 속도를 최적화하십시오.
  - **데이터 무결성 가드레일**: 크롭 영역 가속 분석 수행 시, 수거된 성분 장부의 개수가 0건이거나 유효 데이터 유실 위험이 감지될 경우(무결성 위반), 즉시 크롭 분석을 취소하고 원래 안전선인 원본 전체 스캔(PDF 전체 파일 파싱) 방식으로 강제 회군(Fallback)하도록 설계하여 정합성 위실을 원천 차단하십시오.
  - **가상환경 자동 자가 이주 가드레일 (Self Re-execution)**: 메모리 로드 직전 최상단 관문에 런타임 인터프리터 검문소를 가동하여, 가상환경 밖(예: Python 3.13)에서 오구동되었을 때에도 자동으로 `venv_312` 가상환경의 파이썬 핵심부(`venv_312/Scripts/python.exe`)를 타깃팅해 자기 자신을 리다이렉트 서브 프로세스로 재격발하십시오. 이로써 외부 환경 오염 및 `PyQt5` 등의 모듈 로딩 실패(`ModuleNotFoundError`)를 100% 원천 방어하십시오. 또한 가상환경 핵심부 폴더 자체가 물리적으로 파괴되거나 존재하지 않을 경우, 무한 재귀 재호출 과열을 예방하기 위해 즉각 자폭(`sys.exit(1)`)하도록 설계하여 오작동 위험을 영구 격리하십시오.

- **완전 스캔본 3단계 공정 라인 (V24.5.6.0)**:
  - **1단계 (자가 검문소)**: 로컬 OCR(PaddleOCR)이 긁어온 HTML 표 데이터를 기반으로 외부 인터넷 호출을 격리한 채, CAS 번호의 알파벳 유입 오식 및 물질명-함량 간의 1:1 매치 정합성을 로컬에서 검문합니다. 1글자 물질명(예: '물')도 기각되지 않도록 최소 길이는 1자 이상으로 유연하게 판정하되 빈칸이나 수치 기호 단독 텍스트 오염은 엄격히 차단합니다.
  - **2단계 (무정차 바이패스)**: 검문 결과 오타와 누락이 전혀 없는 100% 무결 장부임이 판명되면 초록불(🟢) 신호를 켜고 외부 AI 호출을 영구 스킵(Bypass)하며, 즉시 `local_bypass` 엔진으로 지정하여 엑셀 저장고로 직행시킵니다.
  - **3단계 (선택적 외주 정제)**: 오식 CAS(알파벳 유입)나 정렬 꼬임이 발견될 경우 노란불/빨간불(🔴) 신호와 함께 찌그러진 표 영역 텍스트만 핀포인트로 요약하여 구글 Gemini로 세척(`gemini_cleaner`)을 의뢰하며, Gemini 통신선 장애 및 타임아웃 발생 시에는 비상 연쇄 회로인 OpenAI gpt-4o-mini를 출격시켜 장부를 수선(`openai_cleaner`)합니다.

- **GUI 설정 매핑 연동 엑셀 무결성 적재 규격 (V24.5.6.1)**:
  - 사용자가 GUI 환경설정 탭에서 "신호등", "매칭 엔진", "무결성 점수"에 해당하는 엑셀 열 인덱스를 지정하여 저장할 수 있도록 매핑 컬럼 설정 패널에 각 항목을 배선합니다.
  - 엑셀 저장 격발 시, 메모리상의 최종 스냅샷 결과(`self.results`)로부터 각 파일에 대응하는 신호등 규격(`🟢 초록`/`🟡 노랑`/`🔴 빨강`), 엔진명(`local_bypass`/`gemini_cleaner`/`openai_cleaner`), 무결성 점수(`100점 (청정)` 등)를 추출하여 엑셀 시트에 안전하게 기입되도록 연동합니다.

- **함량 수치 수학적 상한선 검증 저울 센서 (V24.5.6.2)**:
  - 1선 자가 검문소(`scan_self_diagnosis`) 내부의 리턴 단계에 `verify_integrity_of_local_data` 검증 필터를 연동하여야 합니다.
  - 추출된 성분의 함량 텍스트 내에서 100.0%를 초과하는 수치(오독)가 존재하거나, 3개 이상의 성분이 추출되었을 때 해당 최소 함량들의 평균이 90.0%를 초과할 경우 즉각 검문을 탈락(`is_perfect = False`)시키고 3단계 외부 AI 정제 및 연쇄 폴백 차선으로 회군하도록 강제 통제합니다.

- **좌측 1/3 레이저 조준 수평 재단 및 CAS 제거 수치 검증 규칙 (V24.5.6.3)**:
  - `smu_gui.py` 내의 가속 레일 로직은 성분표를 자를 때 반드시 좌측 1/3 세로 영역(`w * 0.33`) 안에서 3번 및 4번 간판 키워드를 검색하여 성분표가 시작되는 `start_y`와 끝나는 `end_y`를 도출하고, 해당 영역을 수평으로 쫙 펼쳐서 가위질(`crop_rect`)해야 합니다.
  - 가위질한 크롭 결과물에 대해 수치 검증을 기동할 때, CAS 번호의 앞자리가 100% 초과 오독으로 걸러지는 것을 방어하기 위해 반드시 정형화 필터(`validate_composition_limits`)를 통해 CAS 번호 패턴을 공백으로 소거한 뒤 검증을 집행하여야 합니다. 수치 모순이 발견되면 즉시 3선 AI(전체 스캔) 차선으로 안전하게 회군시킵니다.

- **비정형 간판 대응형 샌드위치 크롭 및 천칭 필터 연쇄 통제 규칙 (V24.5.6.5)**:
  - 완전 스캔본 지형 진입 시 `PPStructureV3`를 통해 좌측 1/3 띠지 영역을 선제 판독하고, 유연 정규식(Flexible Regex)으로 3번 간판(시작선) 및 4~6번 간판(차선 차단선)의 Y축 높이를 추적하여 3-4번방 사이 공간을 샌드위치 가속 크롭(Crop)합니다.
  - 크롭 영역을 로컬 OCR로 판독한 후 100% 초과 여부를 저울 검문(천칭 필터)하여, 무결하면 `local_bypass`로 0초 직행 처리하고, 오염 포착 시 잘라낸 깨끗한 조각 이미지(`cropped_bytes`)만 외부 AI 세척선(`FALLBACK` 트랙)으로 송출하여 API 연산 비용과 안정성을 사수해야 합니다.
  - 이 과정에서 `verify_mathematical_천칭_filter` 작동 여부를 상시 검증할 수 있는 단위 테스트 메서드 `test_천칭_filter_anomaly`를 디버그 실험실에 상시 유지 및 검증 연동합니다.

- **제품명 오독 박멸 3중 조준경 및 파일명 보정 규칙 (V24.5.6.6)**:
  - **1단계 (파일명 파싱)**: PDF 파일명 문자열에서 `MSDS`, `GHS`, `(O)`, `(X)`, `★`, `국문`, `개정`, `KOR` 등의 노이즈 단어를 세척하고, 괄호 내부의 상업용 제품명 힌트(`file_pn_hint`)를 선제 파싱하여 대조군으로 파킹합니다.
  - **2단계 (블랙리스트 가드레일)**: 최종 추출된 제품명 텍스트에 `['물질안정', '보건자료', 'MSDS', 'SDS', '안전보건']` 단어가 단독 또는 포함되어 있을 경우 후보군에서 강제 제외(Drop)하고, 파일명에서 파싱해둔 청정 힌트 명칭으로 강제 대체합니다.
  - **3단계 (1번 섹션 핀포인트 격리 조준)**: 본문 내 수색 시 `1. 화학제품과 회사` 대간판 시작점부터 `2. 유해성·위험성` 대간판 시작 직전까지의 [1섹션 울타리 내부 텍스트]만 샌드위치로 크롭하여 대형 AI에 힌트(`text_hint`)로 공급하고, 1섹션 내의 딱딱한 정규식 제품명 탐색 로직은 전면 철수하여 AI가 주변 문맥을 유연하게 종합 추론해 최종 상업용 제품명을 솎아내어 확정하도록 설계합니다.

- **수학적 천칭 저울 검문소 2단계 분리 계량 규칙 (V24.5.6.7)**:
  - `verify_mathematical_천칭_filter` 및 `verify_integrity_of_local_data` 두 천칭 검문소에 '2단계 분리 계량 회로'를 매립합니다.
  - **1단계 개별 성분 저울**: 텍스트 내부의 **모든 숫자**를 검사하여 단 하나라도 `100.0%`를 초과하는 수치 모순(크롭 조각 영역 내의 주소, 전화번호 등 오조준 유입 노이즈 포함)이 감지되면 즉시 차단막을 내리고 Fallback 차선(외부 AI 정제 및 연쇄 폴백)으로 회군시킵니다.
  - **2단계 범위 최대치 합계 저울**: 진짜 함량 수치 매칭들의 범위 상한값(Max)들의 총합계를 계산하여 `110.0%`를 초과할 때만 오독 노이즈로 판정하여 차단하고, `110.0%` 이하일 때는 실무 기재 오차 범위로 간주하여 바이패스를 유연하게 허용합니다.

- **제품명 표준 Flash 스위칭 및 1섹션 울타리 격리 규칙 (V24.5.6.8)**:
  - 1섹션 내부에서 제품명을 판단하는 외부 AI 모델을 `Gemini-2.5-Flash`로 전격 교체하고, 입력 범위를 '1. 화학제품과 회사에 관한 정보' 시작부터 '2. 유해성·위험성' 간판 직전까지의 [1섹션 울타리 내부 텍스트]로 엄격히 제한 공급하여 문맥을 기반으로 상업용 제품명을 오차 없이 정확히 판단합니다.
- **3섹션 물질명 추출 회로 전면 폐기 및 콤팩트 적재 규칙 (V24.5.6.8)**:
  - 3섹션 구성성분 추출 과정에서 불필요한 물질명(Chemical Name) 수집, 오타 복구, 마스터 DB 매핑 연산을 전면 폐기하고 수집 대상을 오직 CAS 번호와 함유량으로 압축합니다. 최종 엑셀 적재를 위한 `comp_str` 빌드 시 물질명 문자열 찌꺼기 없이 오직 `CAS(함유량)` 형태로만 세미콜론 조인하여 콤팩트하게 입고를 완착합니다.
- **후방 수학적 천칭 검문소 2단계 분리 계량 고도화 규칙 (V24.5.6.8)**:
  - `verify_mathematical_천칭_filter`와 `verify_integrity_of_local_data` 판단 로직을 일원화하여, 공백이 포함된 CAS 번호까지 완벽히 소거한 후 검증을 집행하며 개별 수치 100% 초과 차단(Fallback) 및 범위 최대치 합계 110% 초과 차단을 일관되게 집행합니다.

### 6. 제품명 파일명 교차 검문 및 복합 부등호 수선 규칙 (V24.4.3.22)
- **파일명 기반 제품명 교차 검문 규칙**: 제품명 최종 확정 단계에서 반드시 [최종 출구 파일명 검문소]를 거쳐야 합니다. 파일명에서 한글/영문 키워드를 안전하게 파싱하여 AI의 추출 결과와 상호 대조하며, 교차 포함 관계가 전혀 성립하지 않는 명백한 오독이 감지되거나 제품명이 비어 있는 경우 AI의 유령 답변을 기각하고 파일명 기반의 세척된 상용명(예: `페놀(PHENOL)`)을 제품명 칸에 강제 대치 수정하여 안전 입고해야 합니다.
- **유럽형 복합 부등호 2선 정제 및 Bypass 차단 규칙**: 수거된 성분들의 원본 문맥에 부등호가 2개 이상 교차하거나 하이픈으로 연결된 지저분한 서식이 발견될 경우, `check_complex_bounds_format` 센서에 의해 즉시 1선 Bypass 판정을 취소하고 2선 청소부(`Gemini-2.5-Flash-Lite`)가 실무 표준 규격인 `45~50%` 또는 `<1%` 로 깔끔하게 정제한 뒤 재계측하여 입고해야 합니다.

### 7. 제품명 1섹션 샌드위치 분리 및 후방 2단계 천칭 저울 필터 결착 마감 규칙 (V24.5.6.9)
- **디지털 텍스트 1섹션 수거**: 디지털 텍스트가 존재하는 경우 (`is_scanned_strict == False`) 로컬 문자열 집게 요원(String Parser)을 가동하여 1섹션('1. 화학제품과 회사' ~ '2. 유해성·위험성' 직전)의 순수 문자열을 복사하고, **이미지(비전) 데이터는 페이로드에서 전면 제외**한 상태로 Gemini 2.5 Flash를 텍스트 전용 호출하여 제품명을 독점 판독합니다.
- **스캔 이미지 1섹션 수거**: 스캔본인 경우 (`is_scanned_strict == True`) 로컬 Y축 좌표 추적기를 가동해 1페이지의 1번 및 2번 대간판 Y축 좌표를 감지한 뒤 해당 범위만 수평 크롭하고, 크롭된 사진 조각을 로컬 OCR로 빻아내어 빻아낸 글자 가루(텍스트)만 **이미지(비전) 데이터 제외**하고 Gemini 2.5 Flash를 텍스트 전용 호출하여 제품명을 독점 판독합니다.
- **후방 수학적 천칭 검문소 2단계 분리 계량 마감**: 3섹션 성분 출구 측 천칭 검문소(`verify_mathematical_천칭_filter`)에서, ① 개별 성분 저울은 CAS 번호 및 날짜 패턴이 소거된 상태에서 텍스트 내 **모든 숫자**를 검사하여 단 하나라도 100%를 초과할 시 즉시 차단막을 작동시켜 Fallback 처리하고, ② 범위 최대치 합계 저울은 성분 함량 범위 최대값들의 합계가 110%를 초과할 때만 차단막을 작동시킵니다.

### 8. 예외 자재 무결성 강제 보정 및 API 통신 타임아웃 임계값 규칙 (V24.6.0.1)
- **예외 자재 무결성 강제 보정**: `034_용접봉CR-13` 등 `EXCEPTION_REGISTRY`에 수록된 고유 예외 매칭 자재가 투입된 경우, AI가 성분을 미추출하여 유실이 감지되더라도 품질 정합성 판정 단계 이후에 무결성 점수를 100점으로 초기화하고, `has_invalid_cas` 플래그를 `False`로 명시적 강제 리셋하여 신호등을 최종 🟢(초록불)로 수렴시켜 무결성 우회 통과 처리를 수행해야 합니다.
- **API 통신 타임아웃 임계값**: 3섹션 원본 텍스트의 크기와 구조적 복잡도로 인해 발생하는 Gemini API 서버 지연 및 `Read timed out` 오류를 원천 차단하기 위해, API 호출 함수 `call_gemini_with_retry`의 기본 타임아웃 임계값을 최소 45초 이상으로 넉넉하게 확장하고, 재시도 횟수를 5회로 높여 통신 복원 안정성을 영구 확보하십시오.

### 9. 팁시크 V4 플래시(DeepSeek V4 Flash) 전격 전환 규칙 (V24.6.0.2)
- **Novita AI OpenAI 호환 API 연동**: `call_gemini_with_retry` API 호출부를 Novita AI의 `deepseek/deepseek-v4-flash` 모델 단독 호출 엔드포인트(`https://api.novita.ai/v3/openai/chat/completions`)로 전격 전환하며, 기존 Gemini API 페이로드 파싱 인터페이스와의 100% 호환성 유지를 위해 반환 객체 구조를 Gemini의 `candidates[0].content.parts[0].text` 규격으로 완벽하게 에뮬레이션(Mock)하여 리턴해야 합니다.
- **유료 Key 안전 연동**: `.env` 파일에 탑재된 유료 결제 API 열쇠(`DEEPSEEK_API_KEY` 또는 `NOVITA_API_KEY`)를 환경변수에서 상시 취득하여 격발하여야 하며, API 인증 실패(401) 또는 통신 장애 시 최대 5회 재시도(Retry)하며 복원 안정성을 유지하십시오.

### 10. 무정차 런타임 예외 쉴드 및 그레이스풀 격리 수거 규칙 (V24.6.0.2)
- **그레이스풀 런타임 예외 쉴드**: 3섹션 성분 추출 및 자가 진단 과정 등 런타임 중 오류가 발생하여 최종 추출 결과가 빈손(0건)으로 복귀하거나 임의의 치명적 크래시 예외가 포착되더라도, 시스템을 중단시키는 `raise RuntimeError`를 전면 배제하고 격리 수거 회로(`_get_graceful_error_dict`)를 작동시킵니다.
- **격리 수거 규격 및 무정차 전진(Bypass)**: 추출 실패 격리 감지 시, `{"제품명": "수거된 제품명", "구성성분": "", "신호등": "🔴", "used_engine": "error_isolation", "integrity_score": 0, "integrity_reason": "[품질점수: 0점] ➔ [❌추출실패]"}` 꼴의 그레이스풀 격리 수거 딕셔너리를 반환하며, `msds_core.py` 등 상위 메인 제어 루프가 해당 빨간불(`🔴`)을 수령하더라도 프로세스가 중단되지 않고 다음 파일 자재 분석으로 안전하게 강제 점프(Bypass)하도록 무정차 연쇄 완주를 보장해야 합니다.


### 11. 버텍스 AI 마스터 열쇠 직결 및 Failover 결착 규칙 (V24.6.0.3)
- **마스터 열쇠 연동**: 구글 AI 스튜디오 키 참조 방식을 완전히 철거하고, `vertex_key.json` 기반의 GCP Vertex AI REST API(access token 동적 갱신 방식)를 단독 주입 연동하여 구동합니다.
- **REST 역할(Role) 보정**: Vertex AI API 스펙과의 호환을 사수하기 위해 요청 페이로드 내에 `role: "user"`가 누락된 경우 필수로 주입 보정하는 필터를 거칩니다.
- **이중 안전 Failover**: 해외망 프리징 방지를 위해 1선 DeepSeek 호출 시 최대 30초의 타임아웃을 강제하며, 1선 호출 실패 감지 시 예외를 던지지 않고 즉시 2선 버텍스 제미나이 비전 채널로 데이터 포인터를 토스합니다.
- **사증 실패 그레이스풀 안전핀**: `vertex_key.json` 파일 누락이나 인증 실패 시 시스템 다운(`sys.exit`)을 전면 방지하고 로그에 `[Vertex AI] 마스터 열쇠 사증 실패`를 남기며 빈 candidates 응답을 반환해 우회합니다.

### 12. 1선 완착 성분-제품명 연쇄 바이패스 및 2중 Post-Validation 무정차 격리 수거 규격 (V24.6.0.4)
- **연쇄 바이패스(Full Bypass)**: 디지털 문서에 대해 1선 정규식 및 격자 성분 추출을 메인 관로의 제품명 AI 호출 이전으로 전격 격상합니다. 1선 추출물에 대해 무결성 검증을 선제 수행하여 완착이 성공(`has_perfect_1st_line == True`)한 경우, 제품명 추출용 Gemini API와 성분 추출용 AI 호출 모두를 완전히 생략(Bypass, API 호출 0회)하며, 제품명은 파일명 기반 청정 상용명으로 강제 적재하고 components는 1선 결과물로 직결합니다.
- **2중 포스트 밸리데이션(Post-Validation) 검문소**: 비전 구원 투수나 AI가 수선해 온 최종 성분 데이터가 장부 입고(최종 Excel 패키징)되기 직전에 반드시 `validate_chemical_balances`를 한 번 더 거쳐 "최종 합산 수치가 물리적 가드레일 내에 존재하는가?"를 검증하는 2중 안전 장치(Post-Validation)를 집행합니다.
- **무정차 그레이스풀 격리 수거**: 포스트 밸리데이션에서 수치 모순이 격발될 경우, 시스템을 끄던 기존의 `raise RuntimeError`를 전면 배제하고 즉시 그레이스풀 격리 수거 회로(`_get_graceful_error_dict`)를 가동하여 빨간불(`🔴`) 형태의 에러 딕셔너리를 반환함으로써 무정차 전진을 영구적으로 사수하십시오.

### 13. 기계실 및 GUI 2중 대조 상세 표 출력 및 이중 검수 연동 규칙 (V24.6.0.5)
- **2중 대조 상세 표 출력**: 오프스크린 실전 레이스 가동 완료 또는 검수 분석이 성공적으로 마감되는 시점에, 엔진 내부 메모리 데이터(used_engine, 신호등, 정합성 점수, 천칭 필터 결과)와 최종 GUI/Excel 기입 데이터(제품명, CAS, 측정대상 물질명)를 1:1로 매핑하는 대조 표를 작성합니다.
- **콘솔 정렬 가드**: 콘솔 창 출력 시 한글, 영문, 특수 문자(🟢, 🔴 등)의 가비지 가로폭 깨짐 현상을 방지하기 위해 반드시 동아시아 문자 폭(East Asian Width)을 판별하는 패딩 정렬 알고리즘을 사용하여 수직 열 정렬을 영구 보장하십시오.
- **대조 표 2중 분류**: 한 화면에 너무 많은 열이 혼재하여 시인성이 락(Lock)되는 것을 방지하기 위해, 대조 표는 ① 기본 정보 및 제품명 대조 요약 표 [표 1], ② CAS 및 측정물질 대조 상세 표 [표 2]의 2단계 이원화 구조로 작성하여 마크다운 호환 규격으로 인쇄하십시오.

### 14. 제품명 공백 제한 매칭 및 날짜/에러 메시지 정밀 기각 블랙리스트 규칙 (V24.6.0.7)
- **공백 제한 매칭**: `extract_product_name_from_section_1` 본문 콜론 추적 시 줄바꿈 오독을 차단하기 위해, 제품명 매칭 패턴의 공백 기호 `\s+` 대신 한 줄 내의 탭과 스페이스(`[ \t]+`, `[ \t]*`)만을 사용해 줄바꿈이 있는 다음 줄의 데이터까지 잘못 매칭하는 관로 누수를 원천 차단하십시오.
- **정밀 기각 블랙리스트**: 본문에서 매칭되었거나 AI 정독 모드에서 추출된 제품명 후보가 `is_blacklisted_pn`에 정의된 ① 상식적 제품명 제외 키워드(MSDS, 날짜 등), ② AI 에러 안내 및 미추출 문장(텍스트가 없으므로 등), ③ 특정 날짜 정규식 패턴(2018.09.29 등), ④ 문장부호/숫자 찌꺼기로만 이루어진 문자열 중 하나라도 해당할 경우 즉각 기각 처리하고, 안전하게 파일명 기반의 청정 물질명/상표명으로 우회(Fallback)하도록 필터 가드레일을 상시 가동하십시오.

### 15. sys.path 정교 세척 및 Regex-Recovery 절대 우위 Overwrite, 130% 천칭 마지노선 고도화 규칙 (V24.6.0.8)
- **sys.path 정교 세척**: DLL 충돌 방지를 위한 `python313` 경로 제거 시, 가상환경 외부의 표준 라이브러리 경로(`Lib` 등)까지 통째로 지워지는 현상을 예방하기 위해, 필터 대상을 `site-packages` 디렉토리가 포함된 경로로 정밀하게 제한하여 `ModuleNotFoundError`를 원천 차단하십시오.
- **Regex-Recovery 절대 우위 Overwrite**: 1선 ODL/밀도 병합부에서 `"Regex-Recovery"` 데이터가 밀도 클러스터링 결과로 덮어쓰여 유실되는 현상을 막기 위해 기존에 `"Regex-Recovery"` 성분이 맵에 선점되어 있다면 덮어쓰기를 스킵하십시오. 또한, `final_quality_control` 함수 내부에서 `"Regex-Recovery"`의 청정 데이터(기호/부등호 포함)가 유입될 경우, 기존 ODL의 거친 장부 데이터를 강제로 덮어쓰도록(Overwrite) 처리하여 기호 유실을 철저히 차단하십시오.
- **3섹션 조기 종료 차단막**: 텍스트 파쇄 차단막의 필터링 키워드를 보강(SECTION 4, 응급조치 요령 등)하여 문서 후반부(12~13페이지 등)의 불필요한 하이픈 노이즈가 3섹션 성분 기호로 유입되는 것을 물리적으로 완전히 조기 차단하십시오.
- **3섹션 개방 추적 규칙**: `find_section3_pages` 함수에서 임의로 2페이지 이상 수집 시 조기 종료하던 독소 조항(`if len(pages) >= 2`)을 완전히 철거하여, 4항 대간판(응급조치 요령)이 물리적으로 감지될 때까지 3섹션 페이지 수집을 계속 개방형으로 이어가도록 설계하십시오.
- **DeepSeek 정제 사명 단독 오독 차단 규칙**: 제품명 정제 딥시크 프롬프트(`deepseek_prompt`) 내에 제조사 사명(Sigma, Sigma-Aldrich 등) 단독 추출을 절대 금지하는 지침을 부여하고, 제품명 우측에 결착된 농도 범위 수치 및 고유 물질명이 포함된 전체 상표명이 누락 없이 완벽히 보존되도록 강력한 앵커 규격을 주입하십시오.
- **실무 천칭 마지노선 130% 상향**: `validate_balances` 및 함량 검문소 내 수학적 합량 최댓값의 허용한계 마지노선을 120%에서 130.0%로 상향 고정하여 정상적인 다성분 범위형 서류가 억울하게 bypass 차단되지 않고 불량 데이터만 걸러지도록 실무 밸런스를 확립하십시오.
- **4항 조기 종료 오탐 방지 인터락**: `find_section3_pages`에서 4항 대간판(`exit_pattern`) 매칭을 발견했을 때, 해당 페이지 내 그 아래로 CAS 번호가 추가로 관측되는 경우 3섹션 표의 지속으로 간주하여 조기 종료를 유예하고 수집을 지속해야 합니다.
- **페이지 경계 수직 상속(병합추정) 연속성 인계**: `extract_from_text_regex`에 `inherited_last_valid`를 매개하여 페이지 순회 간 `last_valid_info` 연속성을 확보하고, 페이지 진입 첫 번째 성분(`is_first_in_page`)에 한해서는 Y축 오차 검증을 우회하고 X축 정렬 오프셋만으로 수직 상속을 집행하도록 설계하여 데이터 유실을 차단하십시오.
- **맹목적 텍스트 파쇄 차단막 전면 제거**: 3섹션과 4항이 공존하는 마지막 페이지가 수집 대상에서 누락되지 않도록 `find_section3_pages` 내 루프 초입에 존재하던 맹목적 break 구문을 전면 제거하고, exit_pattern 단독 제어로 전환하십시오.
- **grounding 대조군 선 수집 후 조기 종료 순서 보정**: `full_text_for_grounding` 빌드 시 페이지 텍스트를 먼저 누적하고 난 뒤 차단막 조건을 검증하여 루프를 탈출하게 설계함으로써, 마지막 3섹션 성분이 대조군 누락으로 판정되는 데이터 유실을 차단하십시오.
- **정규식 추출기 내 4항 조기 브레이크 철거**: `extract_from_text_regex` 내부 Y축 스캔 루프에서 4항 키워드를 포착하여 조기 탈출하는 break 문을 전면 제거하여, 3섹션 내부에서 4항이 공존하더라도 성분표 전체를 무손실로 끝까지 읽어내도록 설계하십시오.
- **정답 선점 잠금장치 (Gate-Lock)**: `final_quality_control` 내부의 성분 병합 루틴에 잠금장치를 이식하여, 이미 채워진 정답이 진짜 수치(미기재%가 아님)이고 새로 들어온 데이터가 미기재% 찌꺼기일 경우 덮어쓰기를 무조건 차단하고 기존 데이터를 보호하십시오.

### 16. exit_pattern 조기 종료 교격 및 마샬링 정답 방어 인터락 규칙 (V24.6.0.9)
- **exit_pattern 조기 종료 break 교격**: `find_section3_pages` 함수에서 4항 대간판(`exit_pattern`) 매칭 발생 시 함수 전체를 조기에 return 종료해버리던 기존 회로를 `break`로 교격하여 해당 페이지 검사는 마감하되 다음 페이지로 순회 루프를 계속 지속하도록 설계하여 다중 페이지 3섹션 자재의 데이터 누락을 차단하십시오.
- **수집 마샬링 관로 내 정답 방어 인터락**: 1선 격자/정규식 수집 마샬링 관로(`existing_item` 처리부)에서 이미 수확된 기존 정답이 진짜 수치(미기재%가 아님)이고 신규 진입자가 '미기재%' 노이즈라면 덮어쓰지 않고 pass(우회)하는 정답 방어 인터락을 강제 장착하여 기존 진짜 수치 데이터를 보호하십시오.
- **핵심 엔진 GOLDEN_HASH 지문 동기화**: 핵심 파싱 엔진 코드가 수정되어 형상 지문 변화가 발생할 때마다, 즉시 16자리 sha256 해시(이번 핀셋 수술 기준: `68f4476370e09cd8`)를 산출하여 `msds_engine_v6.py` 최상단의 `GOLDEN_HASH`에 수술적으로 이식하여 형상 변조 경고등을 해제 상태로 유지 관리하십시오.

### 17. 계단식 상표명 수직 상속 및 독성 분류 격실 전용 정밀 차단 규칙 (V24.6.1.0)
- **계단식 상표명 수직 상속**: HTML 표 파싱 중 첫 행에 CAS 번호 없이 물질명만 단독으로 있는 경우, 해당 명칭을 수직 상속 버퍼 `last_row_single_name`에 격리 저장하고 다음 행에서 CAS 번호가 매핑될 때 수직 상속하여 완착 매핑을 지원해야 합니다.
- **타 자재 회귀 방어 및 초기화**: 각 행의 성분 추출이 마감되는 시점마다 `last_row_single_name` 상속 버퍼를 빈 문자열(`""`)로 강제 초기화하여 타 자재 정상 서류와의 경계를 침범해 엉뚱한 상표명을 오염 복사해오는 회귀 현상을 원천 방단하십시오.
- **독성 분류 격실 정밀 차단**: 법적 규제 한계치 및 H-코드 등의 독성 분류 노이즈(`hazard_rx`)가 함량 수치로 오인입되는 것을 차단하기 위해 전용 차단 마커 정규식을 장착하고, 해당 정형화 필터를 통해 수치 기호(꺾쇠, 등호 등)와 결합된 함량 노이즈가 유입될 시 즉시 기각 처리를 수행하십시오.
- **수치 기호 단독 오독 방어**: 이름 탐색 정형화 및 첫 행 물질명 판정 검문 정규식을 `^[\d\s.,\-~%<>≤≥=∼～–—\+]+$` 으로 고도화하여, 부등호 및 특수 수치 기호들로만 조립된 함량 셀이 물질명으로 잘못 채택되어 오인입되는 관로 누수를 원천 봉쇄하십시오.

### 18. 파일명 교차 검문소 철거 및 2선 청소부 기동 모듈 영구 철거 규칙 (V24.6.1.2)
- **파일명 검문소 철거**: AI가 추출한 순수 상표명 결과가 파일명 기반 필터 등에 의해 기각되거나 변조되는 문제를 차단하기 위해 [최종 출구 파일명 검문소] 제어 프로토콜을 완전히 제거하고, AI 판독 제품명이 하단 연산 벨트로 오염 없이 직진하도록 설계하십시오.
- **2선 청소부 모듈 영구 철거**: 1선 격자/정규식 규칙 엔진이 확보한 성분 장부를 2선 Gemini-2.5-Flash-Lite가 수선하는 중복 API 호출 회로를 완전히 삭제하여 파이프라인을 1선으로 직결 단일화하고, 자원 및 통신 연산 비용을 원천 차단하십시오. (단, 1선의 완착 신호를 판별하는 인터락 구문은 원형을 보존하여 연결 상태를 안전하게 유지하십시오.)

### 19. AI 키 정규화 노멀라이저 및 최후방 1선 백업 자산 회생 가드레일 규칙 (V24.6.1.3)
- **AI 키 정규화 노멀라이저**: 외부 AI가 반환한 JSON 키명 오독 및 변조 현상(예: "성분", "components" 등)을 방어하기 위해, JSON 수령 직후 "구성성분", "content", "cas"로 키를 수선해주는 Alternate Key Normalizer를 최우선 가동하여 데이터 유실을 차단하십시오.
- **최후방 1선 백업 자산 회생 가드레일**: 외부 AI 호출이 완전히 차단되거나, 파싱 에러 등으로 데이터 추출이 아예 실패하는 최악의 국면에서도 1선 엔진이 사전 수집해 둔 `checked_1st` 백업 자산이 존재한다면 이를 최종 구성성분으로 강제 복원 및 입고 완착하여 무정차 조업을 영구 방어하십시오.

### 20. GUI 및 엔진 새로고침 시 캐시 레지스트리 동시 초기화 규칙 (V24.6.1.4)
- **캐시 레지스트리 물리적 완전 삭제**: GUI에서 '전체 초기화'(`reset_all`) 또는 '엔진 새로고침'(`reload_engine`) 기능이 수행될 때, 메모리 캐시(`self.cache`)와 `smu_cache.json` 파일뿐만 아니라 엔진 레벨에서 사용하는 `msds_cache_registry.json` 캐시 레지스트리 파일도 함께 디렉토리에서 감지하여 즉시 물리적으로 삭제(`os.remove`)하여야 합니다. 이를 통해 오분석되거나 잘못된 데이터 찌꺼기가 남아 정답셋으로 오독 및 동결 재사용되는 정합성 실패 위험을 차단하십시오.

### 21. 수학적 천칭 검문소 CAS 앵커 기반 근접 검증 규칙 (V24.6.1.5)
- **CAS 앵커 기반 근접 수치 검증**: 3섹션 성분 출구의 수학적 천칭 검문소(`verify_mathematical_천칭_filter`)에서, 본문 텍스트 전체에서 눈먼 숫자를 무차별 합산하는 방식을 전면 배제하십시오. 반드시 유효한 CAS 번호 패턴의 위치(Anchor)를 먼저 포착하고, 각 CAS 번호가 위치한 물리적 행(Line) 내부 또는 앞뒤 50자 이내의 근접 컨텍스트 반경 안에서만 기하학적으로 수치 매칭을 수행하십시오.
- **노이즈 전처리 소거**: H-코드(H314 등), 날짜 및 CAS 번호 자체(공백 포함)는 정규식을 통해 사전에 완전히 소거하여 검증 오탐을 원천 차단하십시오.
- **2단계 저울 감시**: 소거 후 남은 문자열에 대해서만 ① 개별 100% 초과 차단(Fallback) 및 ② 범위 최대치 합계 110% 초과 차단을 일관성 있게 집행해야 합니다.

### 22. 글로벌 소켓 기본 타임아웃 강제 및 2선 텍스트 페이로드 최적화 규칙 (V24.6.1.5)
- **글로벌 소켓 타임아웃 강제**: 외부 API 호출(Novita DeepSeek, Vertex Gemini 등) 시 발생할 수 있는 네트워크 프리징 및 Keep-Alive 무한 대기 현상을 차단하기 위해, `msds_engine_v6.py` 최상단에 `socket.setdefaulttimeout(20.0)` 설정을 매립하고, 1선 DeepSeek 호출 타임아웃을 12초(커넥션 5초, 읽기 12초)로 단축 제어해야 합니다.
- **2선 텍스트 페이로드 초경량 최적화**: 디지털 MSDS 자재의 2선 텍스트 AI 복구 호출 시에는 OCR 비전이 불필요하므로, 거대한 전체 페이지 이미지 목록(`image_list`)을 인라인 데이터로 송신하던 루프를 전면 제거하고 오직 `raw_prompt` 텍스트만 전송하도록 페이로드를 초경량 설계하여 네트워크 병목을 원천 방어하십시오.

### 23. 표 내부 좌표계 락다운(Boundary Lockdown) 및 수평선상 천칭 계량 규칙 (V24.6.1.6)
- **표 내부 좌표계 락다운**: `extract_from_text_regex` 내부 단어 수집 시, 상류 ODL 파서 및 밀도 클러스터링이 식별해 낸 tr, td 격실들의 바운딩 박스(`table_bboxes`) 목록이 제공될 경우, 해당 격실 박스 중심점이 이 영역(3포인트 마진 포함) 내부에 위치하는 단어만 수집 대상으로 락다운하여 외곽 섹션의 CAS 번호 유입 통로를 원천 격리하십시오.
- **수평선상 천칭 계량**: `verify_mathematical_천칭_filter` 내부에서 함량 수치 추출 시, 기존의 앞뒤 50자 근접 컨텍스트를 배제하고 오직 CAS 번호와 수평선상으로 완벽히 일치하는 행 격실 내부(HTML `<tr>`~`</tr>` 사이 혹은 일반 텍스트의 동일 물리적 행)의 텍스트에 한정하여 수치 기호를 계량하십시오.

### 24. 1섹션 다중 레이어 스캔 및 상표명 마스킹 쉴드 인터락 규칙 (V24.6.1.6)
- **1섹션 다중 레이어 의미 스캔**: `extract_section_1`에 'Description(설명)', 'Identification(식별자)' 단락의 의미론적 추적 패턴을 확장 장착하여 제품명이 비정형 위치에 있더라도 정확히 격리 수거하십시오.
- **제품명 마스킹 쉴드 인터락**: AI 제품명 추출 결과가 공란이거나, 2자 미만으로 지나치게 짧거나, '미추출', '확인', '실패', '오류', 'error', 'none' 등의 불량 메시지가 섞여 있는 경우, 즉시 파일명 기반으로 세척된 상용명 힌트(`file_pn_hint`)로 강제 대체 및 보정하여 002번 등의 🟡 황색등을 완전 소멸시키십시오.

- **Fuzzy 오타 정류 및 하류 세척망 관통 & 천칭 130% 상향 동기화 (V24.6.2.0)**:
  - **Fuzzy 오타 정류**: OCR 스캔본에서 빈번히 발생하는 함량 부등호 주변 오타 단어("미맊", "미밖", "밎안", "이핚" 등)를 AI 분석 전 단계에서 표준 범위 단어("미만", "이하", "이상")로 사전 정류하여 기각 오류 및 탈선을 예방하십시오.
  - **하류 정류 레이어 강제 관통**: AI 응답 구성성분 수거부(`_process_msds_pipeline_impl` 내) 루프에서, 최종 엑셀 출력 직전 구간에 `msds_utils_v3.clean_percentage` 세척망을 거치도록 강제 배선을 매립하여 입고 규격 정합성을 사수하십시오.
  - **천칭 상한 마지노선 130% 동기화**: `validate_chemical_balances`와 `verify_mathematical_천칭_filter` 두 검문소의 천칭 상한 한계 기준을 130.0% 사양으로 전격 상향 조정해 대용량 중첩 유입 정상 자재가 억울하게 탈락당하지 않게 기준을 일치시키십시오.

- **1선 정규식/격자 엔진 전면 고정 락(Lock) 및 외부 AI 개입 전면 배제 (V24.6.3.0)**:
  - **외부 AI 개입 배제 및 1선 락**: MSDS 엔진 메인 파이프라인에서 외부 AI 호출(Gemini, DeepSeek 등)을 완전히 배제하고, 1선 정규식/격자 엔진으로 수거된 자산만으로 장부에 입고하도록 라우팅을 영구 락(Lock)합니다.
  - **스캔본 분기 제어**: 스캔본(Image-only)의 경우에도 로컬 가속 파이프라인(OCR)이 무결하게 성공(`SUCCESS`)하여 1선 자산이 수집된 경우에만 바이패스(`local_bypass`)로 🟢 안착하며, 1선 자산이 물리적으로 완전히 전무한 극단적 공란 상황에서는 즉각 그레이스풀 격리 수거 회로로 이송하여 빨간불(🔴)로 격리하도록 제어합니다.
  - **행 파서 전수조사**: ODL 행 분석기(`parse_row_robust_v2`) 내에서 함량 수치를 발견했을 때 조기 루프를 탈출(`break`)하던 결함을 소각하여 행 전체에 대한 전수 조사를 완수합니다.
  - **CAS 검증 가드**: `parse_row_robust_v2` 내에서 `verify_cas_number` 검증 가드를 신설하여, 오염된 CAS 번호 파편이나 규격 외 찌꺼기 데이터 유입을 원천 차단하십시오.

- **비전 정찰병 Vertex 직결 및 정찰 프로세스 Fail-Safe 방어 규칙 (V24.6.3.1)**:
  - **Recon API 실선 직결**: 스캔본 3섹션 식별을 위해 작동하는 비전 정찰병(`extract_section3_images`) 내에 잘못 기재되었던 미존재 함수를 `call_vertex_gemini_with_retry`로 정류하고 모델을 `gemini-2.5-flash`로 완착하여 API 크래시를 근절하십시오.
  - **JSON 포맷 정류 및 예외 이송**: AI가 반환하는 json 마크다운 기호들을 정규식으로 세척 정류하며, 통신 장애나 비정상 데이터 유입 시에도 영구 정지(hang)되지 않고 `pass`를 거쳐 다음 페이지 조사를 무정차 완수하도록 방어 코드를 고수하십시오.

### 25. 가상 세로 관로 수직 정렬 및 부등호 노이즈 거세, 확정 자산 즉시 인쇄 배선 규칙 (V24.6.3.2)
- **가상 세로 관로 수직 정렬 및 구분(종) 기둥 순번 기각**: `extract_from_text_regex` 내부에서 각 단어의 수평/수직 정렬을 추적하는 가상 세로 관로(Column Corridor) 레이다 및 중심점 매핑 장치를 통해, 진짜 함량 컬럼 구역이 아닌 구분(종) 기둥 등의 순번 숫자에 대해 점수를 대폭 감점(-50,000점)하여 오독 노이즈를 완벽하게 기각하십시오.
- **범위 기호 없는 단일 부등호 순번 노이즈 거세**: `_normalize_single_content` 함수 내에서 범위 연결 기호가 없으면서 단일 부등호만 단 1개 포착된 경우(예: 3 < 1), 전방의 불량 순번 숫자를 지워버리고 후방의 진짜 수치(`<1%`)만을 함량 수치로 단독 추출하여 데이터 정합성을 사수하십시오.
- **최종 확정 자산 즉시 인쇄 로그 배선**: `_process_msds_pipeline_impl` 함수 내부에서 최종 정제된 성분 명세가 확정되어 반환되기 직전에, `log_func` 채널을 통해 각 CAS 번호와 정규화된 함량 정보를 개별 항목 단위로 한눈에 콘솔에 즉시 인쇄하는 배선(log_func)을 연동하십시오.

### 26. 1선 완착 파이프라인 호환성 보존 및 콤팩트 로그 다이어트 배선 규칙 (V24.6.0.0)
- **1선 완착 파이프라인 딕셔너리 호환성 보장**: 1선 정규식/격자 엔진에서 자산 확보 성공 또는 실패 분기 시 즉시 튜플을 리턴하게 되면 후속 호출부(`msds_core.py` 등) 및 유닛 테스트에서 `AttributeError` 예외가 발생하므로, 즉시 튜플을 반환하지 않고 기존 엑셀 입고용 콤팩트 포장(딕셔너리 리턴) 구조를 100% 보존하여 후속 모듈과의 완벽한 작동 호환성을 사수해야 합니다.
- **로그 다이어트 패치 및 AI 셧다운 로그 배선**: 1선 추출 성공 시에는 `✅ [AI 통신 셧다운] 1선 정규식/격자 엔진에서 자산 확보 완료. 외부 AI 호출을 건너뜁니다.` 로그를 출력하고, 최종 리턴 완료/실패 시점의 인쇄 로그는 `✅ [파일명] 완료 (성분: N건) /  └─ 최종 자산: CAS(함량), ...` 또는 `❌ [파일명] 실패 (사유: 자산 미검출)` 형태로 요약 출력하도록 인쇄 배선을 연동하십시오.

### 27. 중간 기술 로그 가로채기 은닉 및 요약 로그 단독 노출 규칙 (V24.6.0.0)
- **중간 기술 로그 가로채기(Interceptor) 은닉**: GUI 실행 시 지저분한 연산 과정(비상 우회, 롤백 등)이 화면에 표출되는 것을 차단하기 위해, 메인 파이프라인(`_process_msds_pipeline_impl` 및 `_trigger_ai_extraction`) 진입 초입에서 `log_func` 인자를 `original_log_func`에 임시 가치 저축하고 `log_func = None`으로 래핑하여 하류의 모든 중간 기술 로그가 노출되지 않도록 전면 은닉하십시오.
- **최종 요약 로그 단독 복구 노출**: 메인 파이프라인의 최종 및 조기 리턴(종료) 직전에, 가로챈 `original_log_func`에 한하여 `✅ [파일명] 완료` 또는 `❌ [파일명] 실패` 요약 메시지를 단독 인쇄하도록 배선하여 호환성 및 GUI 깔끔함을 보증하십시오.

### 28. 문서 유형 가동 가시화 및 최종 통계 리포트 배선 규칙 (V24.6.0.0)
- **문서 유형 가동 가시화**: 1선 가동 직후 `full_text_for_grounding` 문자열의 공백 제거 길이를 검사(500자 이상 시 디지털, 미만 시 스캔본)하여, 은닉된 중간 기술 로그 중 가동 시작 로그(`🚀 [디지털/스캔본 문서] MSDSEngineV6 엔진 가동: ...`)만 `original_log_func`을 복구 실행해 단독 가시화 노출하십시오.
- **최종 통계 리포트 집계 및 예외 방어**: 각 파이프라인 반환값(`res_obj`) 및 에러 수거 딕셔너리(`_get_graceful_error_dict`)에 `doc_type`, `product_engine`, `comp_engine` 통계 메타데이터 필드를 의무적으로 탑재하여 전수 수집하십시오. 또한, 전체 테스트가 마감되는 루프 종단부에서 총 처리 파일 수, 문서종류별, 제품명/구성성분 추출 엔진별 요약 통계(`report_text`)를 심플하고 명확한 템플릿 양식으로 화면에 출력하도록 배선하십시오.

### 29. 유니코드 전각 깨짐 세척 가드레일 및 줄글/나열식 서식 구출 규칙 (V24.6.3.3)
- **유니코드 전각 깨짐 세척 가드레일**: `extract_from_text_regex` 내부에서 각 개별 CAS 대상 매칭 루프를 돌기 직전, `unicodedata.normalize("NFKC", row_clean_text)`를 가드레일 예외 처리(`try-except`)와 함께 적용하십시오. 이를 통해 인코딩 불일치로 유입되는 전각 부동호(`＞`, `＜` 등) 및 전각 퍼센트(`％`)를 표준 반각 아스키 `>`와 `%`로 강제 복구 및 동기화함으로써 정규식 매칭 및 스코어링 세션의 오탐/기각(감점) 현상을 원천 방단하십시오.
- **줄글 나열식 줄 바꿈 흡수선 확장**: 줄글 및 나열식 서식에서 구조식이나 이미지 등으로 인해 성분 함량 행 간의 세로 거리가 멀어졌을 때 자산이 누락되는 문제를 방지하기 위해, 블록 허용 범위를 무조건 `80`픽셀로 확장하여 흡수하십시오.
- **세로 거리 감점 패널티 무력화**: `score_match` 스코어링 세션 내부에서 수평 정렬 패널티 계산 시, `tight_context` 또는 `context_area`에 명확한 `concentration` 키워드가 감지될 경우 세로 거리에 따른 감점 패널티를 `0`으로 무력화(감점 면제)하여 함량 자산을 안전하게 획득하도록 규격을 유지 관리하십시오.

### 30. 오프라인 무과금 자동 검수 엔진 명칭 충돌 방지 및 유해 기호 사전 세척 규칙 (V24.6.0.0)
- **오프라인 무과금 자동 검수 엔진 명칭 충돌 방지**: msds_engine_v6.py 내부의 기존 레거시 `run_v6_automated_quality_check` 자가품질검증 함수와 새로운 오프라인 검수대 함수 간의 명칭 충돌을 예방하기 위해, 기존 함수의 이름을 `run_v6_automated_quality_check_original`으로 변경하고 `self_test_regression` 내의 호출부도 이에 맞게 수정하십시오.
- **유해 기호 사전 세척 및 정합성 보장**: MSDSOfflineTester에서 로컬 정규식 매칭을 격발하기 직전, 텍스트 내에 존재하는 CAS 번호 패턴 및 EINECS 번호 등 함량 추출 시 오탐 간섭을 일으키는 숫자가 포함된 번호 서식들을 사전에 완벽히 소거(`re.sub`)한 후 `comp_pattern.search`를 실행하게 함으로써 진짜 함량값만 정확하게 탐지하여 품질 무결성을 100% 사수하십시오.
- **엔진 인스턴스 주입 연동**: 메인 실행부(`if __name__ == "__main__":`)에서 검수대를 호출할 때 `run_v6_automated_quality_check(MSDSEngineV6())`과 같이 엔진 인스턴스를 직접 주입하여 호출되도록 연동하십시오.

### 31. 오프라인 테스트 검수 모드 플래그 분리 규칙 (V24.6.0.0)
- **오프라인 테스트 검수 모드 플래그 분리**: MSDSEngineV6 생성자(__init__) 내부에 테스트 모드 플래그(self.is_test_mode = False) 선로를 개설하여, 오프라인 자동 검수 장치 기동 및 검수 작업 수행 시 실 작동 레일(메인 비즈니스 로직)과의 영역 분리를 명확히 하여 상호 오염을 원천 차단하십시오.

### 32. 3선 AI 구출 관로 이미지 목록 상속 및 로컬 텍스트 복원 규칙 (V24.6.0.0)
- **3선 AI 구출 관로 이미지 목록 상속**: 메인 파이프라인에서 3선 AI 구출 관로 진입 시, 상류에서 이미 추출해 둔 3섹션 이미지 목록(image_list)이 존재한다면 이를 하류 _trigger_ai_extraction으로 그대로 인계 상속하여, 이미지 재조사 및 크롭 연산이 중복 실행되는 낭비를 차단하십시오.
- **로컬 텍스트 및 페이지 복원**: 이미지 목록이 넘어온 경우에도 AI 분석에 필수적인 section3_text 및 pages 정보는 로컬 PDF 문서를 열어 find_section3_pages와 텍스트 정규화 처리를 통해 안정적으로 복원 및 인계하도록 설계하십시오.                      

### 33. 오프라인 검수 정밀 번호 소거 및 인스턴스 직접 주입 연동 규칙 (V24.6.0.1)
- **오프라인 검수 정밀 번호 소거**: MSDSOfflineTester에서 로컬 정규식 매칭을 시뮬레이션하기 직전, 텍스트에 포함된 식별어구뿐만 아니라 공백이 포함된 CAS 번호 패턴 및 EINECS 번호 패턴 자체를 정규식 소거(`re.sub`) 처리하여 엉뚱한 식별 번호가 함량으로 매칭되는 것을 차단하고 품질 정합성을 보장하십시오.
- **인스턴스 직접 주입 연동**: 메인 실행부(`if __name__ == "__main__":`)에서 검수대를 가동할 때 외부 API 호출을 배제한 상태에서 `run_v6_automated_quality_check(MSDSEngineV6())`과 같이 엔진 인스턴스를 직접 주입하여 호출되도록 연동을 완료하고, 해당 파일 내 모든 하단 품질 검사부 주석은 한글로만 구성하여 소스코드 무결성을 유지하십시오.

### 34. 스캔본 정찰 루프 로컬화 및 가속 크롭 동적 페이지 인젝션 규칙 (V24.6.0.2)
- **정찰 루프 로컬 PaddleOCR 전환**: 스캔본(Image-only) PDF에서 3섹션(구성성분 표) 위치를 탐지하는 정찰 루프(extract_section3_images 내부)에서 유료 Gemini 비전 API 호출을 전면 금지합니다. 대신 로컬 PaddleOCR 엔진(get_ocr_engine().ocr())으로 각 페이지의 텍스트를 무료로 추출하고, 핵심 문패 키워드(구성성분/composition/ingredients/혼합물/함유량)와 섹션 번호(3/삼) 교차 검문만으로 대상 페이지를 판별하여 정찰 비용을 완벽히 0원으로 격리하십시오.
- **샌드위치 크롭 동적 페이지 인젝션**: run_flexible_sandwich_pipeline 함수가 항상 doc[0]만 고정 분석하는 하드코딩을 철거하고, target_page_idx 파라미터를 통해 정찰 결과로 확보된 정밀 페이지 인덱스를 동적으로 주입받도록 설계하십시오. 인덱스 초과 시 0번으로 안전 회귀하는 가드레일을 동시 설치하십시오.
- **스캔본 정찰 인덱스 폴백 보완**: _trigger_ai_extraction 내부에서 로컬 텍스트 기반 문패 탐색이 실패하여 target_page_index가 None으로 유실된 경우, 상류 정찰병이 무과금으로 확보한 pages[0] 인덱스를 폴백으로 보완하여 null 유실에 의한 과다 이미지 전송을 차단하십시오.

### 35. 3대 변칙 레이아웃 예외 처리 및 ★별표 에러 장부 고시인성 보강 규칙 (V24.6.3.4)
- **한국어 '항' 자 변칙 레이아웃 정규식 동기화**: MSDS 문서에서 '3항 구성성분', '4항 응급조치' 등 한국어 '항' 자를 섹션 번호 뒤에 삽입하는 비정형 레이아웃을 완벽하게 포착하기 위해, 페이지 탐지(find_section3_pages), OCR 간판 추적(pattern_sec3~6), 디지털 블록 탐색, 물리 라인 스캔 총 6개소의 정규식을 `[\s항.:\-\/]` 또는 `\s*항?` 형태로 일괄 동기화하십시오.
- **CAS 없는 성분행 함량 전이 차단**: extract_from_text_regex 내부에서 CAS 번호가 없는 pending_lines를 다음 CAS 행에 합류시킬 때, '%' 기호나 소수점 수치 패턴(\b\d{1,3}\.\d{2}\b)이 포함된 행은 합류를 차단(continue)하여 함량 수치가 다음 성분으로 전이 오인입되는 현상을 원천 봉쇄하십시오.
- **개정 번호 가중치 거세 필터**: score_match 함수 내부에서 context_area에 'rev', '개정', 'version', '제개정' 키워드가 존재할 경우 score를 8000점 감점하여 개정 버전 번호(Rev.03 등)의 숫자가 함량으로 하이재킹되는 오탐을 완벽 기각하십시오.
- **초록불 오독(False Green) 섀도우 교차 검문**: 메인 파이프라인의 초록불(🟢) 판정 직후, grounding_pool 내 유효 CAS 개수와 최종 추출 성분 개수를 교차 비교하여 불일치가 감지되면 ★FALSE_GREEN_LEDGER.log에 타임스탬프·파일명·의심사유를 자동 적출하여 사후 추적을 보장하십시오.
- **★별표 에러 장부 및 보라색(🟣) 비상등 폴백**: 에러 격리 장부 파일명을 `★ERROR_ISOLATION_LEDGER.log`로 변경하여 탐색 식별성을 향상시키고, 신호등 키 유실 시 기본 폴백 색상을 기존 ⚪(흰색)에서 🟣(보라색)으로 전환하여 고시인성 비상등 체계를 확립하십시오.


### 36. 특이 대시 기호 포함 EC 번호 광대역 소거 및 골든 지문 갱신 규칙 (V24.6.3.5)
- **EC 번호 특이 대시 광대역 필터 적용**: extract_from_text_regex 내부 및 가상 시뮬레이터 
un_offline_tester 내부에서 EC 번호(이씨 번호/EINECS 번호)를 소거할 때, 일반 하이픈 외에도 en-dash, em-dash, 물결 기호(전각/반각) 및 전후 공백을 완벽하게 커버하는 광대역 정규식 (?<![\d-])\d{3}[\s\-~∼～\u2013\u2014]+\d{3}[\s\-~∼～\u2013\u2014]+\d(?![\d-])을 사용하여 선제적으로 소거하고 함량 오독을 차단하십시오.
- **골든 마스터 지문(GOLDEN_HASH) 동기화**: extract_from_text_regex 함수의 로직을 변경한 경우, 자가 진단 무결성 체크 함수인 check_golden_fingerprint에서 무단 변조 경고가 격발되지 않도록 새로운 SHA-256 해시값 44b1c6e892883a73을 GOLDEN_HASH 변수에 정확하게 반영 및 동기화하십시오.

### 37. 수집 완료 데이터 재활용 및 doc 객체 중복 순회 방지 규칙 (V24.6.3.6)
- **데이터 중복 추출 및 순회 차단**: 이미 수집이 완료되어 소모된 `doc` 객체에 대해 스트림 종료(`doc.close()`) 직전 가변 데이터의 무결성을 재검사하는 관로에서 불필요하게 `"".join([p.get_text() for p in doc])` 등 `doc`을 재순회하여 리소스를 낭비하거나 이미 닫힌 스코프 내 호출 크래시를 발생시키지 않도록 하십시오.
- **full_text_for_grounding 버퍼 재활용**: `doc.close()` 전의 판별 조건에서는 이미 상단에서 깨끗하게 문자열 추출 및 누적이 완료된 `full_text_for_grounding` 변수를 100% 재활용하여 `raw_pdf_text = full_text_for_grounding.strip()` 형태로 안정 배선하십시오.

### 38. 에러 격리 장부 내 상세 에러 호출 스택(Traceback) 상세 박제 규칙 (V24.6.4.0)
- **Traceback 엔진 결착**: 그레이스풀 격리 수거 회로(`_get_graceful_error_dict`)가 가동될 때 단순히 차단 사유 문자열만 장부에 기록하지 말고, `traceback` 모듈을 연동하여 `traceback.format_exc()`를 안전하게 추출하십시오.
- **예외 이원화 기록 및 안전 가이드레일**: 시스템 에러 크래시 없이 명시적인 논리 조건문 인터락에 의한 격리 수거일 경우 (Traceback이 None이거나 비어있을 때)에는 `[안전 안내]: 명시적인 논리 조건문 인터락에 의한 격리 수거 (시스템 에러 크래시 없음)` 형태로 기록을 남기며, 시스템 상세 충돌 경로가 존재하는 경우에는 상세 트레이스백 보드를 `★ERROR_ISOLATION_LEDGER.log` 장부에 구분선(`--- 🚨 시스템 상세 충돌 경로 (Traceback Board) ---`)과 함께 영구히 이어쓰기 방식으로 누적 기록하십시오.

### 39. 상표명 대조 시 CAS 외 AI 추출 성분명 및 원본 텍스트 전역 교차 검증 규칙 (V24.6.4.1)
- **교차 검증 풀(match_pool) 확장**: 스캔본 판별 지형에서 상표명과 성분 간의 연관성 검증(Raw Token Cross-Match)을 수행할 때, 대조 풀을 단순히 CAS 번호가 기록된 `comp_str`로만 국한하지 말고, `refined_comps`에서 수집한 한글/영문 성분명 목록 및 `grounding_pool` 등을 교차 합산하여 매칭 풀을 구성하여야 합니다.
- **변수 오타 교정**: REPLACE 제안에 있던 변수명 오타(선언 시 `extracted_names_pool`, 참조 시 `extracted_name_pool`)를 `extracted_names_pool`로 정밀하게 일치시켜 `NameError` 오류가 나지 않도록 차단하십시오.

### 40. 마스터 DB 유효 CAS 포착 시 상표명 오독 필터 우회 하이패스 인터락 규칙 (V24.6.4.2)
- **하이패스 격리 스위치(`is_cas_highpass`) 결착**: `_process_msds_pipeline_impl` 함수 내부 하류 검문소 영역에서 추출 성분(`components`)을 순회하며 마스터 DB 및 체크디지트 검증(`verify_cas_number`)을 통과한 유효 CAS 포착 시 `is_cas_highpass = True` 스위치를 활성화하고 전용 통과 로그를 출력하십시오.
- **스캔본 환각 오독 교차 검문 우회 연동**: 스캔본 상표명 대조 교차 검문소 조건식에 `and not is_cas_highpass` 연동 조건을 추가하여, 마스터 DB에 실재하는 정합 CAS 자산의 경우 물질명이 비어있거나 소실되어도 상표성분모순 환각 탈락으로 간주되지 않도록 자가 격리를 면제하십시오.

### 41. 순수 스캔본 지형 초고속 직결 선로 (Fast-Track Bypass) 활성화 규칙 (V24.6.4.3)
- **선제 문서 유형 판별 및 분 단위 병목 차단**: `_process_msds_pipeline_impl` 최초 진입부에서 디지털 글자 수가 10자 미만인 순수 스캔본(Image-only) 지형이 감지되면 (`is_scanned_strict`), 무겁고 무의미한 로컬 표 분석기 및 샌드위치 재단 회로 진입을 전면 셧다운하십시오.
- **다이렉트 직결 및 페이로드 선제 연산**: 상표명 마스킹 쉴드 인터락용 파일명 힌트(`file_pn_hint`) 및 1~3페이지 고화질 2.0 배율 도면 구조(`fallback_images`)를 상류에서 직결 패킹하여 외부 고속 AI 비전 채널(`_trigger_ai_extraction`)로 초고속 직행 수송하고 최종 회신 데이터를 즉시 반환하십시오.




### 42. 스캔본 Fast-Track 통합 원샷 프롬프트 수선 및 3항 시야 제한 예외 유예 규칙 (V24.6.4.4)
- **product_name 추출 예외 조항 강착**: 스캔본 통합 원샷 임무 프롬프트(`one_shot_prompt`) 결합 시, 1페이지 표지의 진짜 상표 제품명 추적 규격에 "이 필드는 3항의 시야 제한 락을 전면 유예합니다." 조항을 명시적으로 확착하여 3항 시야 제한 락으로 인한 제품명 오독 결함을 방지하십시오.
- **세이프티 락 및 정밀 백업 선로 연동**: 3항 표 구역의 CAS 번호와 함량 수확 시 4항 이하 하류 구역 노이즈를 철저히 배제하는 세이프티 락을 적용하고, AI 상표명 추출 미추출/오류 시 파일명에서 괄호 파싱 및 정규식 정제를 병행하는 정밀 백업 선로를 완착 유지하십시오.
