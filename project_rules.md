# project_rules.md

## 마스터 룰북 (MSDS_EXtaction 프로젝트 개발 규칙)

### 1. 함유량 및 텍스트 세척 규칙 (clean_content_text)
- 한글이 혼용된 특이 수치 범위어(예: "이상", "미만")가 함량 필드로 유입될 경우, 하류 파서 및 수신단과의 호환을 위해 반드시 표준 물결 기호(`~`) 형태로 세척하여야 합니다.
- **정형화 규칙**: `(\d+)\s*이상\s*~\s*(\d+)\s*%?\s*미만` 패턴 매칭 시 이를 `\1~\2%`의 정밀 포맷으로 변환합니다.
- **범위형 상한선 1.0 미만 보존 규칙 (V24.5.1.0)**: 범위형 함량 정제 시 상한 수치(Upper Bound)가 정확히 1 또는 1.0인 경우에만 미만 기호를 `<`로 변환하여 그대로 보존(예: `0.1~<1%`)해야 하며, 이외의 모든 상한 수치(예: 0.5, 5, 20 등)는 미만 기호 및 문구를 완벽히 제거하고 순수한 수평 범위 형태(예: `15~20%`)로 평탄화해야 합니다.
- **하네스 안전 잠금 (HARNESS_SAFETY_LOCK)**: 시스템의 회귀 테스트 스크립트 실행 시, 상한선 1.0이 아닌 다른 수치 범위(예: `15~<20%`, `1~<5`)에서 미만 기호가 제거되지 않고 새어 나갈 경우 반드시 즉시 `RuntimeError`를 발생시켜 전체 시스템 작동을 격리 차단해야 합니다.

### 2. 밀도 클러스터링 및 텍스트 추출 행 격리 규칙
- **동적 세로 격리**: 테이블 클러스터링(`extract_table_by_density_clustering`) 및 텍스트 정밀 추출(`extract_from_text_regex`)의 모든 행 분리 로직에서 절대 수치 하드코딩(3.0포인트 등)이나 고정비 겹침 비율을 금지하며, 비교 대상 활자 상자의 높이 중 작은 쪽의 25% 비율(`min_height * 0.25`)을 동적 수직 임계값으로 적용하여 성분들이 한 문장으로 뭉개지는 현상을 격리 차단합니다.
- **시각적 정렬**: 수직 행 분류가 완료되거나 `physical_lines` 빌드가 끝나 하류 파이프라인으로 넘기기 직전, 반드시 각 행의 대표 세로 좌표(`y` 또는 중간값)를 기준으로 완벽한 오름차순 정렬을 강제 집행하여, PDF 내부 장부 순서 노이즈를 파쇄하고 위에서 아래로 향하는 시각적 순서 무결성을 확보합니다.
- **동적 가로 분할**: 고정 상수(0.6배 등)를 배제하고, 페이지 내부 전체 활자 간의 격차 중간값과 활자 높이 중간값(`base_scale`)에 연동되는 동적 가로 여백 비율(`median_gap / base_scale`)을 실시간 계산하여 셀을 분할합니다.
- **오염 방지**: 100을 초과하는 순수 숫자(예: 거대 INCI ID, Monograph ID 등)가 함유량 칸에 오염 인입되는 것을 즉각 차단하기 위해, 함량 추출 시 100을 초과하는 단독 숫자는 배제 및 기각 처리합니다.

### 3. 파일 경로 처리 안전성 규칙
- 윈도우 OS 개발 및 검증 환경에서 한글이 포함된 파일명을 다룰 때는 파일 시스템 인코딩 차이(NFC/NFD 등)로 파일 미존재 예외가 발생할 위험이 있으므로, 절대 경로를 그대로 하드코딩하기보다 `glob` 모듈을 이용하여 특정 파일 고유 번호(예: `*036*.pdf`) 패턴으로 실제 인코딩된 경로를 안전하게 수집하십시오.

### 4. 파이썬 컴파일 캐시 꼬임 방지 규칙
- 소스코드에 구조적인 수정이 가해질 경우, 파이썬 인터프리터가 예전 런타임 캐시를 재참조하여 오프셋 꼬임이나 `NameError`를 발생시키는 것을 막기 위해 반드시 `__pycache__` 폴더를 삭제(OS 쉘 명령어 또는 파이썬의 `shutil.rmtree` 사용)하여 깨끗한 상태에서 재컴파일을 수행하십시오.

### 5. 계단식 라우팅 및 1선 바이패스(Bypass) 아키텍처 (V24.5.4.0)
- **1선 바이패스**: 텍스트 문서 분석 시, 1선 격자/정규식 규칙 엔진만으로 유효 성분을 완벽히 확보했다면 AI 호출 단계를 완전히 생략(Bypass)하고 완료 단계로 직행해야 합니다.
- **텍스트/이미지 차선 분리**: 디지털 글자 수 10자 미만은 이미지 문서로 격리하며, 이미지 문서에서는 노비타 딥시크 API 호출을 원천 금지합니다.
- **완전 스캔본 다이렉트 패스**: 디지털 글자 레이어가 완전히 상실된 이미지 PDF(is_scanned_strict)의 경우 경량화 비전 모델(Gemini-2.5-Flash-Lite)의 호출을 원천 금지하고, 정식 Gemini 2.5 Flash를 1선 엔진으로 다이렉트 격발하여 한 방에 미세 부등호 기호를 누락 없이 인지하도록 설계해야 합니다.
- **야간 배치 모드**: 야간 배치 감지 시(18시~08시 혹은 Batch 환경변수 활성화) 노비타 딥시크 API 타임아웃 한도를 1.5초로 극단 제어하고, 에러/타임아웃 발생 시 즉시 3선 백업(Gemini Lite -> GPT-4o-mini)으로 토스해야 합니다.
- **우선순위 수치 병합 가드**: ODL 및 밀도 클러스터링 병합 시 기존 1선 장부에 이미 구체적 성분 수치가 존재한다면 후순위의 투박한 수치가 기존 데이터를 덮어쓰지 못하도록 엄격히 가드해야 합니다.
- **부등호 품질 게이트 상호 교차 검증**: 이미지 비전 신뢰도 검증 시, 원본 PDF 텍스트 장부(`local_grounding_text`) 내에 미만/이상/꺾쇠 기호가 실제로 존재하는 경우에만 결과물의 부등호 존재 여부를 비교 검사해야 하며, 원본에 부등호가 없는 청정 자재 문서의 경우 불필요하게 3선(GPT-4o-mini)으로 승격하여 비용이 낭비되는 일이 없도록 제어해야 합니다. (단, 완전 스캔본의 경우에는 품질 게이트를 check_omission 단일 판정으로 단순화하여 GPT-4o-mini 승격선을 축소 정돈합니다.)
- **로컬 OCR 하이브리드 비전 라우팅 (V24.5.5.0)**: 완전 스캔본 지형(`is_scanned_strict == True`) 진입 시, 외부 클라우드 비전에 거대 이미지를 업로드하는 비전 API를 완전히 차단합니다. 대신, 로컬에서 인텔 CPU OpenVINO 가속을 얹은 `PPStructure` 엔진을 선행 구동하여 표 영역 데이터를 HTML 텍스트 문자열로 깔끔하게 복구합니다. 복구된 HTML 데이터를 최종 LLM 입력용 프롬프트에 병합하여 이미지 전송 비용 없이 텍스트 요금 사양(Text Token)으로 통신을 집행합니다. 로컬 추론 시 발생하는 텐서 메모리 자산 힙 누수를 방지하기 위해 추론 세션 완료 즉시 파이썬 가비지 컬렉터(`gc.collect()`)를 수동으로 기동해야 하며, 로컬 하드웨어 장애나 라이브러리 잠금 예외 발생 시에는 시스템 마비를 예방하기 위해 즉각 비상 예비 클라우드 멀티모달 비전 라인(Gemini 2.5 Flash -> GPT-4o-mini 이중 연쇄 백업)으로 무정차 이송하도록 이중 안전 밸브를 상시 연동하십시오.

- **가상환경 파이썬 인터프리터 격리 및 잔여 의존성 완착 (V24.5.5.1)**:
  - **가상환경 격리 확보**: ODL CLI 등 외부 모듈이나 CLI 도구를 서브 프로세스로 구동할 때는 단순 문자열 `"py"` 나 `"python"` 대신, 반드시 현재 가상환경의 최상위 주소인 `sys.executable`을 사용하여 현재 실행 중인 파이썬 인터프리터 환경을 그대로 상속하여야 합니다. 이를 통해 가상환경 밖의 시스템 파이썬이 실행되면서 발생하는 모듈 누락(ModuleNotFoundError) 현상을 원천 방어하십시오.
  - **GUI 의존성 보증**: GUI(smu_gui.py) 내의 엑셀 자동 정화 기능 및 프로세스 연동이 정상 작동하도록 가상환경 내에 `pywin32` 패키지를 누락 없이 완착하여 윈도우 OS API와 정상 소통하게 유지하여야 합니다.

- **하이브리드 크롭 가속 및 데이터 무결성 롤백 (V24.5.5.2)**:
  - **선택적 크롭 가속**: PDF 문서 처리 시 비전 정찰병(Recon)을 가동하여 성분표가 위치한 페이지 및 좌표 구역(`table_quadrant`)을 식별한 후, 해당 구역만을 넉넉한 마진을 두어 크롭(Crop)하여 로컬 Paddle 엔진에 고속 피딩합니다. 이를 통해 전체 이미지 처리 대비 CPU 연산 부하를 10배 이상 감소시키고 속도를 최적화하십시오.
  - **데이터 무결성 가드레일**: 크롭 영역 가속 분석 수행 시, 수거된 성분 장부의 개수가 0건이거나 유효 데이터 유실 위험이 감지될 경우(무결성 위반), 즉시 크롭 분석을 취소하고 원래 안전선인 원본 전체 스캔(PDF 전체 파일 파싱) 방식으로 강제 회군(Fallback)하도록 설계하여 정합성 위실을 원천 차단하십시오.
  - **가상환경 자동 자가 이주 가드레일 (Self Re-execution)**: 메모리 로드 직전 최상단 관문에 런타임 인터프리터 검문소를 가동하여, 가상환경 밖(예: Python 3.13)에서 오구동되었을 때에도 자동으로 `venv_312` 가상환경의 파이썬 핵심부(`venv_312/Scripts/python.exe`)를 타깃팅해 자기 자신을 리다이렉트 서브 프로세스로 재격발하십시오. 이로써 외부 환경 오염 및 `PyQt5` 등의 모듈 로딩 실패(`ModuleNotFoundError`)를 100% 원천 방어하십시오. 또한 가상환경 핵심부 폴더 자체가 물리적으로 파괴되거나 존재하지 않을 경우, 무한 재귀 재호출 과열을 예방하기 위해 즉각 자폭(`sys.exit(1)`)하도록 설계하여 오작동 위험을 영구 격리하십시오.

- **완전 스캔본 3단계 공정 라인 (V24.5.6.0)**:
  - **1단계 (자가 검문소)**: 로컬 OCR(PaddleOCR)이 긁어온 HTML 표 데이터를 기반으로 외부 인터넷 호출을 격리한 채, CAS 번호의 알파벳 유입 오식 및 물질명-함량 간의 1:1 매치 정합성을 로컬에서 검문합니다. 1글자 물질명(예: '물')도 기각되지 않도록 최소 길이는 1자 이상으로 유연하게 판정하되 빈칸이나 수치 기호 단독 텍스트 오염은 엄격히 차단합니다.
  - **2단계 (무정차 바이패스)**: 검문 결과 오타와 누락이 전혀 없는 100% 무결 장부임이 판명되면 초록불(🟢) 신호를 켜고 외부 AI 호출을 영구 스킵(Bypass)하며, 즉시 `local_bypass` 엔진으로 지정하여 엑셀 저장고로 직행시킵니다.
  - **3단계 (선택적 외주 정제)**: 오식 CAS(알파벳 유입)나 정렬 꼬임이 발견될 경우 노란불/빨간불(🔴) 신호와 함께 찌그러진 표 영역 텍스트만 핀포인트로 요약하여 구글 Gemini로 세척(`gemini_cleaner`)을 의뢰하며, Gemini 통신선 장애 및 타임아웃 발생 시에는 비상 연쇄 회로인 OpenAI gpt-4o-mini를 출격시켜 장부를 수선(`openai_cleaner`)합니다.

- **GUI 설정 매핑 연동 엑셀 무결성 적재 규격 (V24.5.6.1)**:
  - 사용자가 GUI 환경설정 탭에서 "신호등", "매칭 엔진", "무결성 점수"에 해당하는 엑셀 열 인덱스를 지정하여 저장할 수 있도록 매핑 컬럼 설정 패널에 각 항목을 배선합니다.
  - 엑셀 저장 격발 시, 메모리상의 최종 스냅샷 결과(`self.results`)로부터 각 파일에 대응하는 신호등 규격(`🟢 초록`/`🟡 노랑`/`🔴 빨강`), 엔진명(`local_bypass`/`gemini_cleaner`/`openai_cleaner`), 무결성 점수(`100점 (청정)` 등)를 추출하여 엑셀 시트에 안전하게 기입되도록 연동합니다.

- **함량 수치 수학적 상한선 검증 저울 센서 (V24.5.6.2)**:
  - 1선 자가 검문소(`scan_self_diagnosis`) 내부의 리턴 단계에 `verify_integrity_of_local_data` 검증 필터를 연동하여야 합니다.
  - 추출된 성분의 함량 텍스트 내에서 100.0%를 초과하는 수치(오독)가 존재하거나, 3개 이상의 성분이 추출되었을 때 해당 최소 함량들의 평균이 90.0%를 초과할 경우 즉각 검문을 탈락(`is_perfect = False`)시키고 3단계 외부 AI 정제 및 연쇄 폴백 차선으로 회군하도록 강제 통제합니다.

- **좌측 1/3 레이저 조준 수평 재단 및 CAS 제거 수치 검증 규칙 (V24.5.6.3)**:
  - `smu_gui.py` 내의 가속 레일 로직은 성분표를 자를 때 반드시 좌측 1/3 세로 영역(`w * 0.33`) 안에서 3번 및 4번 간판 키워드를 검색하여 성분표가 시작되는 `start_y`와 끝나는 `end_y`를 도출하고, 해당 영역을 수평으로 쫙 펼쳐서 가위질(`crop_rect`)해야 합니다.
  - 가위질한 크롭 결과물에 대해 수치 검증을 기동할 때, CAS 번호의 앞자리가 100% 초과 오독으로 걸러지는 것을 방어하기 위해 반드시 정형화 필터(`validate_composition_limits`)를 통해 CAS 번호 패턴을 공백으로 소거한 뒤 검증을 집행하여야 합니다. 수치 모순이 발견되면 즉시 3선 AI(전체 스캔) 차선으로 안전하게 회군시킵니다.







