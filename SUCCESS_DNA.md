# SUCCESS_DNA.md

## MISTAKE LOG (실수 및 실패 기록 목록)
- **오류 명칭**: Windows PowerShell 경로 상의 특수 문자 파싱 에러
  - **증상**: powershell을 사용하여 경로에 `(v24+GUI통합)`이 포함된 파이썬 스크립트를 구동할 때 괄호 기호로 인해 CommandNotFoundException 에러가 발생함.
  - **해결책**: 스크립트 실행 시 상대 경로를 인자로 넘기거나, 경로 전체를 큰따옴표(`"`)로 감싸서 실행함.
- **오류 명칭**: 윈도우 파일 시스템 한글 인코딩 불일치로 인한 파일 없음 에러
  - **증상**: 파이썬 `os.path.exists()`를 사용하여 한글이 포함된 `036_아이생각수성내부프로 (M-BASE)_GHS국문.pdf`를 불러올 때, 윈도우 OS의 기본 인코딩 차이로 인해 경로 탐색에 실패하는 에러 발생.
  - **해결책**: `glob` 모듈을 이용하여 파일 고유 번호(`*036*.pdf`) 형태의 glob 패턴으로 실제 파일명을 동적으로 가져오도록 수정하여 파일 시스템 경로 매칭 정밀도를 높임.
- **오류 명칭**: 파이썬 인터프리터 런타임 캐시 꼬임에 의한 라인 번호 불일치 및 NameError
  - **증상**: 소스코드 파일을 수정한 후, `__pycache__` 폴더 내에 예전 컴파일 결과가 상주해 있으면 파이썬이 수정 전 라인 번호를 참조하여 `NameError` 등의 비정상 런타임 오류가 발생함.
  - **해결책**: `shutil.rmtree('__pycache__')` 로 캐시 디렉토리를 물리적으로 지워 깨끗한 상태로 재컴파일을 강제함.
- **오류 명칭**: NoneType 개체의 문자열 조작 시도(strip) 에러
  - **증상**: `refine_msds_components_strict` 함수 내부에서 AI가 검출한 성분의 `name`, `cas`, `percentage` 등의 필드가 `None`일 때, 단순 `.strip()`을 시도하여 `AttributeError: 'NoneType' object has no attribute 'strip'` 오류가 발생해 전체 테스트 흐름이 중단됨.
  - **해결책**: `comp.get('name', '').strip()` 형식을 `(comp.get('name') or '').strip()` 형태로 수정하여, `None`이 반환되는 특수한 경우에도 빈 문자열(`''`)로 자동 대체된 뒤 `.strip()`이 안전하게 실행되도록 방어벽을 구축함.
- **오류 명칭**: 병합 단계에서의 1선 우선순위 ODL 수치 덮어쓰기 에러
  - **증상**: ODL에서 `40~50%` 와 같은 정밀한 범위형 수치를 획득했음에도, 그보다 덜 구체적인 `≥40%` 와 같은 밀도 기반 클러스터링의 단일 부등호 수치가 최종 장부 병합 단계에서 기존 값을 덮어써버려 오염 및 정합성 실패가 발생함.
  - **해결책**: 병합 시 기존 1선 장부에 이미 구체적 성분 수치(미기재%가 아닌 유효값)가 존재한다면 밀도 및 AI 수거물이 기존 데이터를 덮어쓰지 못하도록 제한하는 가드를 장착하여 해결함.
- **오류 명칭**: 이미지 품질 게이트에서의 단순 부등호 판정으로 인한 비용 누출 에러
  - **증상**: 원본 문서에 부등호가 없는 청정 자재(예: "물 50%")임에도 불구하고, 결과물에 부등호가 없다는 이유만으로 품질 미달 판정을 내려 GPT-4o-mini(3선) 등 상위 엔진으로 강제 승격시키는 심각한 비용 누출 결함 발생.
  - **해결책**: 원본 PDF 텍스트 내부에 부등호/조건어가 실제로 존재하는 경우에만 결과물의 부등호 여부를 대조하는 상호 교차 검증 연동 공식을 이식하여 해결함.
- **오류 명칭**: 완전 스캔본 지형에서의 부등호 품질 게이트 무력화 및 경량 모델 누출 에러
  - **증상**: 디지털 글자 레이어가 없는 완전 스캔본(is_scanned_strict == True)의 경우, 로컬 텍스트가 비어 있어 부등호 교차 검증 게이트가 무력화됨. 이로 인해 gemini-2.5-flash-lite 모델의 부등호 유실 및 환각이 품질 게이트를 그대로 통과하여 하류로 누출되는 현상 발생.
  - **해결책**: 스캔본 지형에서는 품질 한계가 명백한 Lite 모델을 배제하고 인지력이 높은 정식 Gemini 2.5 Flash를 즉시 격발하도록 라우팅을 재배선하였으며, 품질 게이트를 check_omission 단일 판정으로 단순화하여 GPT-4o-mini 승격선을 축소 정돈함.
- **오류 명칭**: msds_engine_v5.py 내 datetime 모듈 누락 에러
  - **증상**: 야간 배치 모드 검사 시 `datetime.now()`를 호출하려 했으나 `datetime` 모듈이 임포트되어 있지 않아 `NameError: name 'datetime' is not defined` 크래시가 유발됨.
  - **해결책**: `msds_engine_v5.py` 임포트 최상단에 `from datetime import datetime`을 수술적으로 장착하여 해결함.
- **오류 명칭**: scan_self_diagnosis 내 1글자 한글 물질명 기각 에러
  - **증상**: 자가 검문소에서 물질명 판정 기준을 2글자 이상(`>= 2`)으로 설정한 탓에, 한글 1글자 성분명인 `물`이 기각되어 정합성 오류로 오탐 판정되어 완벽 무결 자재임에도 Bypass를 통과하지 못함.
  - **해결책**: 판정 조건을 1글자 이상(`>= 1`)으로 완화하고 숫자/기호 단독 검문은 유지함으로써 `물`과 같은 성분도 안정적으로 수거하도록 수정함.
- **오류 명칭**: test_천칭_filter_anomaly 단위 테스트 및 검증 호출 누락 에러
  - **증상**: 비정형 간판 추적 및 천칭 필터 결착 시, 천칭 가드레일이 오작동하거나 예외가 발생하는 것을 상시 독립 검증하는 유닛 테스트 함수가 누락되어 검증성 담보가 훼손됨.
  - **해결책**: msds_engine_v5.py 하단 디버그 구역에 test_천칭_filter_anomaly를 구현하고 self_test_regression 시작 관문에 호출 연동하여 상시 자가 검증이 이루어지도록 조치함.
- **오류 명칭**: msds_engine_v5.py 내 numpy 임포트 누락으로 인한 NameError
  - **증상**: 샌드위치 크롭 파이프라인(run_flexible_sandwich_pipeline) 구동 시, numpy 모듈이 임포트되어 있지 않아 NameError: name 'np' is not defined 크래시가 유발됨.
  - **해결책**: msds_engine_v5.py 최상단 임포트 영역에 import numpy as np를 장착하여 해결함.
- **오류 명칭**: 1번 및 7번 자재 등에서의 제품명 대제목(물질안전보건자료) 오독 에러
  - **증상**: PDF 본문 추출 시 총제목("물질안전보건자료" 등)이나 관용명 텍스트 뭉치를 제품명으로 오독하여 잘못된 정보가 장부에 기록됨.
  - **해결책**: 파일명에서 노이즈를 세척하여 상업용 명칭 힌트(file_pn_hint)를 추출하고, 추출된 제품명이 블랙리스트(['물질안정', '보건자료', 'MSDS', 'SDS', '안전보건'])에 걸리는 경우 파일명 힌트로 자동 보정하는 2중 안전 가드레일을 장착하고, 1번 섹션 핀포인트 전처리 조준경을 심어 해결함.
- **오류 명칭**: 1번 섹션 외의 딱딱한 제품명 탐색 정규식에 의한 비정형 오독 에러
  - **증상**: `extract_product_name_hybrid` 함수 내부에서 "가. 제품명", "Product Name" 등 특정 간판 키워드를 정규식으로 잘라내는 경직된 로직을 사용한 탓에, 비정형 변형 간판이 나타나거나 문맥이 뒤엉킬 때 제품명을 올바르게 솎아내지 못하고 오독이 발생하는 문제 발생.
  - **해결책**: 제품명 탐색부의 딱딱한 정규식 슬라이싱 로직을 전면 철수시키고, 1섹션("1. 화학제품과 회사에 관한 정보") 시작부터 2섹션("2. 유해성·위험성") 시작 직전까지의 울타리 내부 텍스트 전체를 크롭하여 대형 AI에 공급하고 주변 문맥을 스스로 유연하게 추론하여 확정하도록 재배선함.
- **오류 명칭**: 천칭 검문소 가속 크롭 내 주소/전화번호 노이즈의 함량 수치 미필터링 에러
  - **증상**: `verify_mathematical_천칭_filter` 함수를 함량 전용 정규식으로만 정제하도록 수정했을 때, 크롭 영역 오조준으로 인해 주소 및 전화번호 노이즈(예: `9900` 등 100% 초과 숫자)가 유입되었으나 함량 기호가 없어 검출되지 않고 Bypass 통과됨으로써 005번 자재 등의 FALLBACK 유도가 실패하는 현상 발생.
  - **해결책**: 개별 성분 저울의 검사 범위를 텍스트 내 **모든 숫자**로 확장하여 크롭 오조준 노이즈를 100% 초과로 즉각 차단하도록 복구하고, 범위 최대치 합계 저울에만 진짜 함량 수치 매칭들의 최대값 합산을 적용하는 2단계 분리 계량 회로로 정밀 설계하여 무결성을 방어함.
- **오류 명칭**: 1섹션 제품명 AI 판단 시 경량 모델의 한계 및 전체 텍스트 공급 에러
  - **증상**: 경량 모델(`Gemini-2.5-Flash-Lite`)을 사용하고 본문 전체 텍스트나 비정형 범위 힌트를 공급하는 경직된 로직 탓에, 가. 품명/제품명 등 비정형 간판이 나올 때 상업용 명칭 힌트가 누락되거나 이종 텍스트를 제품명으로 환각 추출하는 오독 현상 발생.
  - **해결책**: 판단 모델을 체급이 검증된 표준형 `Gemini-2.5-Flash` 정직원으로 스위칭하고, 공급되는 텍스트 범위를 '1. 화학제품과 회사에 관한 정보' 시작부터 '2. 유해성·위험성' 간판 직전까지의 [1섹션 울타리 내부 텍스트]로 칼같이 샌드위치 오려내어 공급함으로써 오독을 근원적으로 방어함.
- **오류 명칭**: 3섹션 물질명 추출 연산의 과도한 토큰 및 자원 낭비 에러
  - **증상**: KOSHA API가 CAS 번호를 기반으로 정식 물질명을 완벽하게 자동 매핑해 줌에도 불구하고, 앞선 3섹션 가위질 구역에서 한글/영문 물질명을 수집하고 오타를 세척하기 위해 프롬프트와 정규식 파싱 연산을 무리하게 구동하여 토큰 비용과 연산 자원이 극심하게 낭비되는 에러 발생.
  - **해결책**: 3섹션 가속 추출 및 자가 진단, 정밀 텍스트 파싱 등에서 물질명 추출 회로를 전면 폐기하고 수집 타깃을 오직 CAS 번호와 함유량 두 가지로 압축하였으며, 최종 엑셀 적재를 위한 `comp_str` 빌드 시 물질명 문자열 찌꺼기 없이 오직 `CAS(함유량)` 형태로만 세미콜론 조인하여 콤팩트하게 입고되도록 포장 규격을 개편함.
- **오류 명칭**: 공백 포함 CAS 번호에 의한 수학적 천칭 검문소 개별 저울 오탐 에러
  - **증상**: `verify_mathematical_천칭_filter` 내부에서 수치 검증 전 CAS 번호를 소거할 때 하이픈 주변에 공백이 섞여 있는 CAS 번호(예: `64742 - 54 - 7`)가 제대로 소거되지 않고 누출되어, 앞자리의 거대한 숫자가 100% 초과 모순으로 오인되어 개별 저울 차단막이 오작동(Bypass 기각 및 Fallback 유도)을 일으키는 오탐 에러 발생.
  - **해결책**: 공백이 포함된 CAS 번호 패턴(`\d{2,7}\s*-\s*\d{2}\s*-\s*\d`)까지 전처리 공백으로 말끔하게 소거하도록 정규식 소거 패턴을 보정하여 수치 검증 오탐을 차단함.
- **오류 명칭**: HTML 인코딩 기호(HTML Entities) 유입으로 인한 msds_engine_v5.py 구문 에러 (SyntaxError)
  - **증상**: `replace_file_content` 파일 쓰기 도중, 수치 비교 연산자(`<=`, `>=`)가 `&lt;=`, `&gt;=` 형태의 HTML entities로 인코딩되어 적용되면서 파이썬 컴파일 시 `SyntaxError`가 발생해 실행이 즉시 차단됨.
  - **해결책**: HTML 인코딩되어 적용된 기호들을 본래의 비교 기호(`<`, `>`, `<=`, `>=`)로 원복 교정하여 SyntaxError를 즉각 방어함.
- **오류 명칭**: PaddleOCR ocr() 호출 시 cls=False 인자 전달에 의한 unexpected keyword argument 크래시 (V24.6.0.1)
  - **증상**: 스캔본 1섹션 제품명 추출 시 `ocr_engine.ocr(img_crop_np, cls=False)` 형태로 호출할 때, PaddleOCR 패키지 내부 API 규격 불일치로 인해 `unexpected keyword argument 'cls'` 크래시가 유발되어 프로세스가 다운됨.
  - **해결책**: `cls=False` 인자를 제거하고 `ocr_engine.ocr(img_crop_np)` 로 호출하도록 간결화하여 호환성을 확보함.
- **오류 명칭**: 문서 전체 텍스트 CAS 스캔으로 인한 3섹션 외 유출 CAS 오인입 및 스나이퍼 누락 오탐 에러 (V24.6.0.1)
  - **증상**: 003번 및 007번 자재 등에서 독성 정보(11번)나 규제 정보(15번) 등의 하류 본문 내에 참고 기재된 이종 CAS 번호들이 문서 전체 텍스트 기반 `check_omission` 스나이퍼 누락 센서에 걸려 대조군으로 오인입됨. 이로 인해 성분표 테이블에는 진짜 성분이 2개/9개만 존재함에도 대조군이 3개/13개로 부풀려져 무결성 점수가 40점으로 하락(🟡 경고등)하는 불일치 오탐 결함 발생.
  - **해결책**: `check_omission` 함수 내부에 3섹션("3. 구성성분의 명칭 및 함유량" ~ "4. 응급조치 요령" 직전) 바운더리를 샌드위치로 정밀 가위질하는 구획 센서를 장착하고, 가위질된 3섹션 띠 영역 내부에서 추출된 CAS 번호들만을 진짜 원본 성분 대조군으로 공급하여 타 섹션의 누출 CAS 번호 간섭을 원천 분리 차단함.
- **오류 명칭**: 완전 스캔본 지형 내 grounding_pool 누락으로 인한 CAS 번호 유실 및 하드 크래시 마비 에러 (V24.6.0.2)
  - **증상**: 디지털 텍스트 레이어가 완전히 없는 이미지 PDF(005번 등) 처리 시 `grounding_pool`이 빈 문자열 `""`이 되어 유효 성분이 전부 환각으로 기각 및 삭제됨. 이로 인해 최종 성분 리스트가 0건이 되어 `raise RuntimeError` 하드 크래시가 유발되고 전체 후속 배치 공정이 정지되는 미숙한 에러 발생.
  - **해결책**: `is_scanned_strict` 검문에서 참일 경우 로컬 OCR 복구 데이터(`local_ocr_html` 또는 `res_acc['raw_data']`에서 HTML 태그 제거) 혹은 `section3_text`를 `grounding_pool`에 강제 공급하도록 수선하였으며, `process_msds_pipeline` 최외곽에 `try-except` 예외 쉴드를 씌워 에러 발생 시 `🔴` 격리수거 딕셔너리를 반환하도록 조치함.
- **오류 명칭**: 실전 팁시크 V4 플래시(DeepSeek-V4-Flash) 환경변수 직결 연동 에러 (V24.6.0.2)
  - **증상**: 기존 무료/Gemini 대장 키 한계를 극복하고 유료 팁시크 V4 플래시를 실전 검증하기 위해, .env 파일에 매핑된 API 키와 모델을 안정적으로 가동해야 함.
  - **해결책**: `call_gemini_with_retry` API 호출부 내부를 Novita AI OpenAI 호환 엔드포인트(`https://api.novita.ai/v3/openai/chat/completions`)의 `deepseek/deepseek-v4-flash` 호출로 전면 개편하고, Gemini의 JSON 파싱 응답 규격(candidates)을 완벽히 모방하여 하류 공정과의 정합성을 100% 사수함.
- **오류 명칭**: 1선 완착 통과 자재의 제품명 AI API 호출 낭비 에러 (V24.6.0.4)
  - **증상**: 디지털 텍스트 문서에서 1선 정규식 및 격자만으로 유효 성분이 완벽하게 확보(`has_perfect_1st_line == True`)되어 성분 API 호출은 Bypass 하도록 되어 있었으나, 1섹션 제품명 추출용 Gemini API 호출은 정규식 매핑 성공 여부와 관계없이 무조건 앞단에서 선행 실행되어 불필요하게 API 호출 비용이 낭비되는 오류가 발생함.
  - **해결책**: 메인 파이프라인의 성분 정밀 1선 추출 단계를 제품명 AI 호출 이전으로 전격 격상시켜, 1선에서 완벽한 무결성이 조기 확보된 경우 제품명 LLM 호출과 성분 LLM 호출 모두를 단 0회로 원천 차단하고 파일명 기반 명칭으로 즉시 바이패스시키는 연쇄 바이패스 라우팅으로 개편함.
- **오류 명칭**: 3선 비전 구원 투수 및 AI 수선본 함량에 대한 무결성 검증 누락 에러 (V24.6.0.4)
  - **증상**: 1선 바이패스가 취소되어 2선 청소부나 3선 Vertex Gemini Vision(비전 구원 투수)이 투입되어 사진을 읽고 최종 수선해 온 수치 데이터에 대해서는, 장부 입고 직전 "최종 합산 수치가 물리적 가드레일 내에 존재하는가?"를 검증하는 Post-Validation 검문소가 누락되어 오독된 데이터가 장부에 오인입될 수 있는 위험이 방치됨.
  - **해결책**: 최종 엑셀 적재 직전에 `validate_chemical_balances`를 2중으로 돌리는 포스트 밸리데이션(Post-Validation) 예외 처리 검문소를 수술적으로 매립하였으며, 검증 실패 시 하드 예외(RuntimeError 등) 대신 그레이스풀 격리 수거 회로(`_get_graceful_error_dict`)를 기동하여 🔴(빨강불) 딕셔너리를 그레이스풀하게 반환하게 해 무정차 전진을 사수함.
- **오류 명칭**: 실전 레이스 가동 완료 시 기계실과 GUI/Excel 데이터 간의 가독성 있는 대조 검증 표 누락 에러 (V24.6.0.5)
  - **증상**: 10개 전수 자재의 레이스 가동 후, 기계실(Engine)의 신호등/엔진종류 등 내부 정보와 GUI/Excel의 최종 입고 명칭/CAS/매칭정합성이 올바르게 일치하는지 한눈에 대조 검증할 수 있는 상세 보고 양식이 부재하여 검수 효율성이 떨어짐.
  - **해결책**: `run_production_race.py`의 종료 요약 수집 시점에 East Asian Width 기반 정렬 패딩 헬퍼 함수를 장착하고, [표 1] 기계실 기본 정보 요약 표와 [표 2] CAS/측정대상 상세 대조 표의 2단계 2중 대조 상세 표를 마크다운 호환 양식으로 콘솔에 출력하도록 보강함.
- **오류 명칭**: ODL-v3.0_Priority 행 파서의 조기 루프 중단 및 CAS 무차별 감지 에러 (V24.6.3.0)
  - **증상**: ODL 행 분석기(`parse_row_robust_v2`) 내에서 함량 수치를 발견했을 때 조기 루프를 탈출(`break`)하는 오배선으로 인해 행 내 다중 CAS 번호가 존재할 때 일부 수치가 누락되거나, 오염된 CAS 번호 파편 및 찌꺼기 데이터가 검증을 거치지 않고 무차별적으로 수집되어 최종 데이터 정합성이 깨지는 현상 발생.
  - **해결책**: 조기 루프 탈출(`break`) 로직을 완전히 제거하고 행 전체를 전수조사하도록 패치하였으며, `verify_cas_number` 검증 인터락을 `parse_row_robust_v2` 내에 주입하여 사전에 규격 외 CAS 데이터를 완전히 여과 및 정화 완료함.
- **오류 명칭**: 비전 정찰병 Recon 기동 시 미존재 함수 call_gemini_with_retry 호출 에러 (V24.6.3.1)
  - **증상**: 텍스트 탐지가 불가능한 스캔본 PDF의 3섹션(구성성분 표) 위치 판별을 위해 비전 정찰병(`extract_section3_images`)을 가동할 때, 코드 내에 존재하지 않는 `call_gemini_with_retry` 메서드를 호출하여 속수무책으로 `AttributeError` 런타임 크래시가 발생하고 정찰이 무력화되는 현상 발생. 또한 AI 응답이 markdown 형식(```json ...)으로 유입되는 경우 json.loads 파싱 실패 가능성 상존.
  - **해결책**: 마스터 열쇠를 사용하는 유효한 `call_vertex_gemini_with_retry` API 메서드로 교체 수선하고 모델을 `gemini-2.5-flash`로 정상 결착함. AI 응답을 안전하게 파싱할 수 있도록 마크다운 기호를 세척하는 정규식을 주입하였으며, 통신 에러 발생 시 프로세스가 블로킹되지 않도록 `except Exception as recon_err: pass`로 감싸 다음 페이지 조사를 순차 수행하도록 정위치 배선을 마감함.



## SUCCESS DNA (성공 패턴 기록 목록)
- **패턴 1**: 한글 범위어 조건 평탄화 세척 기전
  - **설명**: `clean_content_text` 함수를 통하여 `(\d+)\s*이상\s*~\s*(\d+)\s*%?\s*미만` 패턴을 `\1~\2%` 물결 기호 형태로 정형화하여 구버전 수신단 및 정규식 파서가 안정적으로 소화할 수 있도록 함.
- **패턴 2**: 밀도 클러스터링(Density Clustering) 수평/수직 분할 기전
  - **설명**: 세로 오차범위 3.0포인트 이내의 텍스트 상자들을 동일한 수평 행(Row)으로 묶고, 가로 방향으로는 글자 높이의 60%(`char_height * 0.6`)를 초과하는 gap을 칸막이 경계선으로 식별하여 동적으로 셀을 격리 추출함.
  - **설명**: 100을 초과하는 순수 숫자(예: Monograph ID `17036` 등)가 함유량 기둥으로 오입력되는 문제를 차단하기 위해, 함량 후보 필터링 단계에서 100을 초과하는 숫자를 즉시 차단 기각하는 오염 방지 가드레일을 구축함.
- **패턴 3**: 통계적 동적 임계값 및 시각적 정렬 기전 (V24.5.0.0)
  - **설명**: 페이지 내 모든 활자 크기와 가로 격차(gap)의 중간값(Median)을 대표 스케일로 채택하여 고정 상수를 전멸시켰으며, `min_height * 0.25` 비율의 세로 임계값 및 `median_gap / base_scale` 비율의 가로 여백 비율을 동적으로 산출해 경계를 제어함. 또한, 분류된 행들을 중간 y0 좌표 기준으로 오름차순 시각적 정렬하여 위에서 아래로 읽는 완벽한 순서 무결성을 확보함.
- **패턴 4**: 실전 텍스트 추출 엔진의 동적 수직 격리 및 시각적 상하 정렬 기전 (extract_from_text_regex)
  - **설명**: 텍스트 블랙홀 결함을 파쇄하기 위해 고정비 겹침 판단 공식을 배제하고 `min_height * 0.25` 비율의 동적 세로 오차 임계값을 이식하여 성분 분리 무결성을 대폭 개선함. 더불어, physical_lines 리스트를 반환하기 직전에 대표 세로 좌표(y) 기준으로 완벽한 오름차순 정렬을 집행하여 PDF 내부 장부 순서 노이즈를 근원적으로 제거함.
- **패턴 5**: 1% 미만 범위형 상한선 보존 기전 및 하네스 안전 잠금 구축 (V24.5.1.0)
  - **설명**: 범위형 함량 데이터 정제 시, 상한선 수치가 정확히 1 또는 1.0인 경우(예: `0.1~1% 미만`)에만 미만 기호(`<`)를 보존하고(`0.1~<1%`), 상한선이 그 외의 수치(예: `15~20% 미만`)인 경우에는 미만 기호를 완전히 제거하고 순수 수평 범위로 평탄화하는 규칙을 이식함. 또한, 검증 흐름에서 미만 기호 누출을 방지하기 위해 `15~<20%` ➔ `15~20%` 및 `1~<5` ➔ `1~5%`로 자가 치환을 검증하고, 기호가 유출될 시 `RuntimeError`를 발생시키는 하네스 안전 잠금(HARNESS_SAFETY_LOCK)을 연동함.
- **패턴 6**: 텍스트/이미지 계단식 라우팅 및 1선 완착 바이패스(Bypass) 아키텍처
  - **설명**: 문서 투입 시 디지털 글자 수를 기준으로 텍스트 문서와 이미지 문서를 엄격히 분리하고, 텍스트 문서인 경우 1선 격자/정규식 규칙 엔진만으로 유효 성분을 무결하게 확보했다면 AI 호출 단계를 바이패스하여 속도를 향상시킴. 야간 배치 모드에 대해선 극단적 1.5초 타임아웃을 연동해 실패 즉시 3선 백업 선로로 보냈으며, 이미지 문서는 노비타를 차단하고 Gemini 2.5 Flash Lite 및 GPT-4o-mini의 비전 이중 연쇄 백업으로 제어함.
- **패턴 7**: 임포트 시동 자가 품질 검증(Quality Check) 강제 배선
  - **설명**: 모듈 시동 시 자동으로 가동되는 `run_v5_automated_quality_check()` 함수를 신설하여 범위형 함량 규격 자가 테스트를 수행하고, 정제 실패 시 즉시 `RuntimeError`를 던져 안전하게 작동을 차단하도록 강제 배선함.
- **패턴 8**: 부등호 상호 교차 검증 연동 기전
  - **설명**: 이미지 비전 품질 게이트에서 단순 결과물 검사가 아닌, 원본 PDF 텍스트(`local_grounding_text`)에 미만/이상/꺾쇠 기호가 실제로 존재하는 경우에만 결과물의 부등호 존재 여부를 비교 검증하는 상호 교차 검증 방식을 도입하여, 기호가 없는 청정 자재 문서의 불필요한 AI 상위 단계 승격 및 비용 누출을 원천 방어함.
- **패턴 9**: 완전 스캔본 전용 다이렉트 패스(Direct Pass) 아키텍처 (V24.5.4.0)
  - **설명**: 디지털 텍스트 레이어가 완전히 상실된 이미지 PDF에 한하여 경량화 비전 모델(Lite)의 구동`을 원천 금지하고, 시각 판독 인지력이 월등한 정식 Gemini 2.5 Flash를 1선으로 다이렉트 매핑함. 이를 통해 기호 누출을 방지하고 품질 게이트를 `check_omission` 단일로 정밀 재편하여 예외적인 탈루가 감지될 때만 2선 보루인 GPT-4o-mini로 승격되도록 정돈함.
- **오류 명칭**: 외부 비전 API의 429 Rate Limit 및 503 타임아웃 오류
  - **증상**: 스캔본 문서 투입 시 외부 클라우드 비전에 거대한 이미지 데이터를 반복적으로 업로드하면서 API 호출 제한(429) 및 503 오류가 발생하고 공장 가동이 멈추거나 비용이 누출됨.
  - **해결책**: 스캔본 지형에서 클라우드 비전 이미지 업로드를 원천 우회(Bypass)하고, 로컬 PaddleOCR + PP-Structure 엔진을 가동하여 표를 HTML 텍스트로 복구한 뒤 텍스트 단독 규격으로 LLM API에 전달하여 비용과 안정성을 확보함.
- **패턴 10**: 로컬 PaddleOCR + PP-Structure 하이브리드 비전 라우팅 인프라 (V24.5.5.0)
  - **설명**: 완전 스캔본 지형 진입 시 외부 비전 API를 완전히 차단하고, 로컬에서 `PPStructure`를 가동하여 표 데이터를 HTML 형식의 깨끗한 텍스트로 복구함. 복구된 HTML 표 데이터를 LLM 입력용 프롬프트에 동적으로 바인딩하여 텍스트 전용 API(Gemini/GPT)를 통해 초저가 단가 사양(Text Token)으로 유해 물질 장부를 완벽하게 정제함. 로컬 추론 시 메모리 누수를 원천 차단하기 위해 세션 완료 시점에 가비지 컬렉터(`gc.collect()`)를 강제 호출하고, 로컬 하드웨어 장애 시에는 안전하게 비상 예비 클라우드 멀티모달 차선으로 즉시 우회(Fallback)하는 2중 안전 밸브를 구축함.

- **오류 명칭**: 가상환경 격리 해제로 인한 CLI 도구(ODL) 로딩 모듈 누락 에러
  - **증상**: 가상환경 venv_312에서 GUI를 실행했으나, 내부 PDF 파서 CLI(OpenDataLoader) 호출 시 단순 문자열 `"py"`가 명시되어 있어 시스템 기본 파이썬이 구동됨. 이로 인해 가상환경 내에 설치된 `opendataloader_pdf` 라이브러리를 찾지 못해 PDF 분석이 차단되는 문제 발생.
  - **해결책**: `opendataloader/pdf/__init__.py` 파일에 `sys` 모듈을 임포트하고, CLI 호출 명령어의 첫 인자를 `sys.executable`로 대체하여 현재 가상환경의 파이썬 바이너리를 완벽하게 다이렉트 매핑함.
- **오류 명칭**: GUI 필수 엑셀 통신 라이브러리(pywin32) 입고 누락 에러
  - **증상**: 가상환경 venv_312 내부에 PyQt5 등은 완착되어 있었으나 `win32com`, `pythoncom` 라이브러리가 존재하지 않아 엑셀 정화 기능 및 GUI 연동 시 작동 불능 및 크래시 위험이 상존함.
  - **해결책**: 가상환경 `pip` 도구를 사용하여 `pywin32`를 정식 설치하여 의존성을 완전히 종결함.

- **오류 명칭**: 3섹션 머크사 특유의 복합 부등호 함량 오독 및 문맥 수집 범위 협소 에러 (V24.4.3.22)
  - **증상**: 머크(Merck)사 특유의 복합 부등호 함량 서식('>= 45 - < 50 %')이 1선 정규식/밀도 엔진에서 조각나거나 '50%'로 오독되고, CAS 번호와 실제 함량 텍스트의 거리가 멀어 문맥 탐색 창(기존 3줄)에서 누락되어 Bypass를 통과해 버리는 오독 현상 발생.
  - **해결책**: `cont_pattern_single` / `local_cont_pattern_single` 정규식의 좌측 레이더 범위를 확장하여 부등호 패키지를 통째로 수집하도록 개조하고, CAS 번호 기준 문맥 수집 범위를 아래로 13줄(`idx + 13`)까지 확장하여 복합 부등호를 정밀하게 감지하는 `check_complex_bounds_format` 센서를 이식하였으며, 감지 시 Bypass를 취소하고 2선 청소부(`Gemini-2.5-Flash-Lite`)가 실무 표준 규격(`45~50%`, `<1%`)으로 정제해 최종 components에 입고되도록 조치함.
- **오류 명칭**: 3선 비상대기조(GPT-4o-mini) 제품명 오독 및 파일명 불일치 에러 (V24.4.3.22)
  - **증상**: 구글 쿼터 초과 시 투입되는 GPT-4o-mini 비상대기조가 제품명을 '하이드로스페셜' 등으로 생뚱맞게 오독하여 잘못된 유령 답변이 최종 장부에 그대로 완착되는 결함 발생.
  - **해결책**: 제품명 최종 출력 직전에 [최종 출구 파일명 검문소]를 매립하여, 파일명에서 한글/영문 키워드를 분리해 낸 뒤 AI의 추출물과 교차 대조하여 일치하는 키워드가 단 하나도 없을 경우 오독으로 간주, AI의 유령 답변을 지우고 파일명 기반의 깨끗한 제품명(예: `페놀(PHENOL)`)으로 강제 대치 수정하도록 가드를 구축함.

## SUCCESS DNA (성공 패턴 기록 목록)
...
- **패턴 23**: 최종 출구 파일명 교차 검문 및 제품명 비상 방어 기전 (V24.4.3.22)
  - **설명**: AI가 가져온 제품명 결과물과 원본 PDF 파일명을 상호 교차 대조하여, 파일명 기반 유효 키워드들이 추출 결과물에 단 하나도 매칭되지 않는 명백한 오독 발생 시 AI 유령 답변을 즉시 지워버리고 파일명 기반의 청정 물질명/상표명(예: `페놀(PHENOL)`)을 최종 제품명 칸에 강제 대치 입고시키는 2중 안전 검문소를 매립함.
- **패턴 24**: 복합 부등호 2선 청소부 격발 및 실무 표준 포맷 정제 기전 (V24.4.3.22)
  - **설명**: 텍스트 내에 부등호가 2개 이상 교차 분산되었거나 하이픈으로 연결된 복합 서식이 포착될 경우(check_complex_bounds_format 센서 감지), 1선 Bypass 자동문을 전면 기각하고 2선 청소부(`Gemini-2.5-Flash-Lite`)를 기동하여 텍스트 조각을 실무 표준 규격인 `45~50%` 또는 `<1%` 형태로 깔끔하게 정제 복원하여 components에 완착 입고되도록 연동함.

- **패턴 11**: sys.executable 기반 가상환경 서브 프로세스 상속 기전
  - **설명**: 외부 CLI 및 모듈 실행을 위해 `subprocess`를 기동할 때, 하드코딩된 `"py"` 나 `"python"` 문자열 대신 `sys.executable`을 동적 매핑함으로써 실행 세션의 가상환경(venv_312) 종속성 컨텍스트를 하류 프로세스로 안전하게 상속하여 작동 무결성을 사수함.

- **오류 명칭**: 수선 도면 1 최상단 가드레일 내 리스트 컴프리헨션 변수명 오타 에러
  - **증상**: `cleaned_path = [path for p in sys.path ...]`와 같이 리스트 컴프리헨션 내부 루프 인덱스 `p`를 정의해두고 반환값으로 정의되지 않은 `path` 변수명을 명시하여 GUI 구동 시 즉시 `NameError` 크래시가 유발됨.
  - **해결책**: 리스트 컴프리헨션 변수를 `[p for p in sys.path ...]`로 일치시켜 변수 선언 정합성을 확보하여 구동 무결성을 회복함.

- **패턴 12**: 비전 정찰병 좌표 연동 선택적 크롭 가속 및 무결성 롤백 기전 (V24.5.5.2)
  - **설명**: 무거운 고해상도 전체 이미지를 판독하던 비전 파이프라인의 속도 개선을 위해, fitz(PyMuPDF) 정찰병 루프로 "구성성분" 검출 페이지 및 table_quadrant 범위를 선제 확보하고 해당 구역만 크롭 피딩하여 CPU 가동 부하를 획득함.
  - **설명**: 크롭 가속 분석 도중 수거된 성분 개수가 0건이거나 유실 조짐이 보이면 즉시 안전 롤백 게이트를 격발하여 원본 전체 PDF 스캔 방식으로 회군함으로써 최종 결과 정합성을 철저히 지킴.
  - **설명**: 런타임 환경 변수(`PYTHONPATH`) 강제 세척 장치 및 `sys.path` 내 3.13 경로 역주행 방지 필터를 `smu_gui.py` 최상단에 장착하여, 가상환경 `venv_312` 가동 정체성을 최우선으로 사수하는 원천 세척 메커니즘을 이식함.

- **오류 명칭**: sys.path 과잉 가위질로 인한 표준 라이브러리(Lib) 누락 에러
  - **증상**: sys.path에서 "Python313" 문자열을 무조건 검문하여 삭제하다 보니, 가상환경 외부의 표준 라이브러리 경로(Lib 폴더)까지 삭제되어 `import json` 시 `ModuleNotFoundError` 크래시가 유발됨.
  - **해결책**: 필터링 대상 조건을 `site-packages` 디렉토리가 포함되어 있으면서 3.13 경로인 주소로만 한정하여 표준 라이브러리는 남겨두고 외부 패키지 오염만 정교하게 차단함.

- **오류 명칭**: 가상환경 외부 구동 시 PyQt5 모듈 누락 및 cp949 이모지 인코딩 에러
  - **증상**: 가상환경 외부(시스템 3.13)에서 `py smu_gui.py` 실행 시 가상환경 패키지인 PyQt5를 찾지 못해 `ModuleNotFoundError`가 발생했으며, 이를 감지하고 가상환경 경로로 리다이렉트 메시지를 print할 때 유니코드 이모지(🚀 등)가 한글 윈도우 cp949 콘솔과 호환되지 않아 `UnicodeEncodeError`로 크래시되는 2중 문제 발생.
  - **해결책**: 현재 `sys.executable`을 비교하여 가상환경 경로가 아닐 경우 자동으로 `venv_312` 파이썬으로 자기 자신을 복제 재구동(`subprocess.Popen`)한 후 자폭(`sys.exit(0)`)하는 자가 이주 관문을 최상단에 구축하였고, 출력 구역 내 모든 유니코드 이모지 기호를 안전한 cp949 표준 기호(`[*]`, `[ERROR]` 등)로 전면 세척하여 해결함.

- **패턴 13**: 스캔본 1단계 로컬 자가 검문소(Self-Diagnosis) 및 3단계 공정 라우팅 기전 (V24.5.6.0)
  - **설명**: 로컬 PaddleOCR가 복구한 HTML 표 데이터를 기반으로 외부 API 호출 전, 숫자 자리에 알파벳이 유입된 오식 CAS(`[0-9a-zA-Z]{2,7}-[0-9a-zA-Z]{2}-[0-9a-zA-Z]{1}`) 감지 및 물질명-함량 간 1:1 매치 정합성을 로컬에서 선제 검문함. 모든 성분의 무결성이 확인되면 🟢 초록불(used_engine: local_bypass)을 켜고 외부 AI 호출을 생략(Bypass)하여 0초 직행 처리하며, 오식/누락 포착 시 🔴 빨간불과 함께 Gemini(`gemini_cleaner`) 세척을 거쳐 최종 OpenAI(`openai_cleaner`) 연쇄 백업으로 오타를 복원함.

- **패턴 14**: GUI 환경설정 연동 무결성 엑셀 입고 기전
  - **설명**: 사용자가 엑셀로 내보내기 시 신호등, 매칭 엔진, 무결성 점수를 원하는 열에 유연하게 저장할 수 있도록 `ModernMappingPanel`에 세 필드를 추가하고, `perform_standard_save` 에서 파일별 추출 데이터(`self.results`)를 동적 연동하여 엑셀 셀에 `🟢 초록`/`🟡 노랑`/`🔴 빨강`, `local_bypass`/`gemini_cleaner`, `100점 (청정)` 등의 완성도 규격을 안전하게 적재함.

- **오류 명칭**: 로컬 OCR 100% 초과 오독 및 누적 함량 비대화 에러
  - **증상**: 로컬 OCR이 성분표 이미지를 분석할 때 소수점 누락 등으로 함량이 100%를 초과(예: 1.5%가 150%로 오독)하거나 성분들의 최소 함량 평균이 물리적 한계를 초과함에도 불구하고 1선 자가 검문소를 통과하여 비정상 데이터가 Bypass되는 현상 발생.
  - **해결책**: 1선 자가 검문소 리턴 직전에 `verify_integrity_of_local_data` 저울 센서를 결착하여 100% 초과 수치 및 비대화 함량 감지 시 즉시 `is_perfect = False` 처리하고 외부 AI 정제 차선으로 강제 회군시키는 안전 가드레일을 장착함.

- **패턴 15**: 수학적 상한선 저울 센서 기반 무결성 검증 기전 (V24.5.6.2)
  - **설명**: 수거된 성분들의 함량 텍스트 내 모든 숫자들을 추출하여 100% 초과 여부를 계측하고, 3개 이상 성분의 평균 함량이 90%를 초과하는지 여부를 실시간 산출하여 물리적 모순이 있는 문서는 즉시 1선 Bypass 자동문을 차단하고 외부 AI 정제 차선으로 강제 회군시켜 데이터 오염을 예방함.

- **오류 명칭**: 가속 크롭 내 CAS 번호의 함량 수치 오탐 에러
  - **증상**: 선택적 크롭 후 수집한 텍스트에 대해 100% 초과 저울 센서 검문을 돌릴 때, CAS 번호(예: 64742)의 거대한 앞자리 숫자가 함량으로 잘못 오인되어 정상 자재가 비정상(Bypass 차단 및 Fallback)으로 계속 기각되는 오탐 오류 발생.
  - **해결책**: `validate_composition_limits` 함수 내부에서 CAS 번호 양식 정규식(`[0-9a-zA-Z]{2,7}-[0-9a-zA-Z]{2}-[0-9a-zA-Z]{1}`)에 부합하는 패턴들을 사전에 완전히 지우고 남은 문자열에 대해서만 수치 검증을 하도록 전처리 가드레일을 장착함.

- **패턴 16**: 좌측 1/3 레이저 조준 수평 재단 및 CAS 제거 수치 검증 기전 (V24.5.6.3)
  - **설명**: 문서 내 3번 및 4번 간판의 위치를 종이 좌측 1/3 세로 띠지 공간 안에서만 레이저 추적하여 start_y 및 end_y를 정밀 산출하고, 해당 영역 높이로 수평 재단(Crop)하여 OCR 연산 부하를 10배 이상 감소시킴. 이와 동시에 크롭된 결과물의 CAS 번호 숫자를 소거한 뒤 100% 초과 오독을 교차 검증하여 물리적 모순이 발견되면 즉시 3선 AI 정제 차선으로 롤백 복귀시킴.

- **오류 명칭**: 가속 크롭 내 CAS 번호의 함량 수치 오탐 및 PPStructureV3 API not callable 크래시
  - **증상**: PPStructureV3를 인스턴스 직접 호출 `paddle_layout_engine(img_bytes)` 시 not callable 에러 발생. 또한 CAS 번호의 숫자가 100% 초과 저울 센서 검문 시 함량 수치로 오인되어 오탐 및 바이패스 오작동 발생.
  - **해결책**: `paddle_layout_engine.predict(img_np)` 형태로 numpy array 변환을 통과시킨 API 명시 호출 구조로 변경하고, `verify_mathematical_천칭_filter` 내부에서 CAS 번호 정규식 패턴을 사전에 소거한 뒤 감시하도록 전처리 가드레일을 구축함.

## SUCCESS DNA (성공 패턴 기록 목록)
- **패턴 17**: 비정형 간판 대응형 샌드위치 크롭 및 천칭 필터 연쇄 통제 기전 (V24.5.6.5)
  - **설명**: 완전 스캔본 지형 진입 시 `PPStructureV3`를 통해 좌측 1/3 띠지 영역을 선제 판독하고, 유연 정규식(Flexible Regex)으로 3번 간판(시작선) 및 4~6번 간판(차선 차단선)의 Y축 높이를 추적하여 3-4번방 사이 공간을 샌드위치 가속 크롭(Crop)함. 크롭 영역을 로컬 OCR로 판독한 후 100% 초과 여부를 저울 검문(천칭 필터)하여, 무결하면 `local_bypass`로 0초 직행 처리하고, 오염 포착 시 잘라낸 깨끗한 조각 이미지(`cropped_bytes`)만 외부 AI 세척선(`FALLBACK` 트랙)으로 송출하여 API 연산 비용과 안정성을 사수해야 합니다.
- **패턴 18**: 1섹션 울타리 격리 및 정규식 철수 기반 제품명 추론 기전 (V24.5.6.6)
  - **설명**: 정문 제품명 탐색 공정에서 경직된 정규식 로직을 전면 철수시키고, 1섹션과 2섹션의 유연 앵커 매칭을 통해 1섹션 공급 구역 내부의 텍스트 전체만을 대형 AI에 전달하여 주변 문맥을 바탕으로 진짜 상업용 제품명을 유연하게 종합 추론 및 확정할 수 있도록 조율함.
- **패턴 19**: 2단계 분리 계량 회로 수학적 천칭 저울 기전 (V24.5.6.7)
  - **설명**: 1선 자가 검문소 및 크롭 파이프라인 출구에 ① 개별 성분 저울(모든 숫자 중 100.0% 초과 모순 즉시 차단)과 ② 범위 최대치 합계 저울(진짜 함량 매칭들의 최대값 합산이 110.0% 초과 시 차단)로 구성된 2단계 분리 계량 회로를 적용하여, 크롭 영역 오조준 노이즈 차단와 실무 기재 오차 허용 상한선을 완벽하게 조율함.
- **패턴 20**: 제품명 표준 Flash 모델 스위칭 및 1섹션 핀포인트 샌드위치 격리 기전 (V24.5.6.8)
  - **설명**: 1섹션 제품명 AI 판단 시 모델을 `Gemini-2.5-Flash`로 정밀 스위칭하여 판단 지능을 높였으며, '1. 화학제품과 회사에 관한 정보' 시작부터 '2. 유해성·위험성' 간판 직전까지의 [1섹션 울타리 내부 텍스트]만 칼같이 샌드위치로 오려내 공급해 울타리 외부 텍스트 혼입에 따른 오독을 원천 방어함.
- **패턴 21**: 3섹션 물질명 추출 회로 전면 폐기 및 콤팩트 규격 입고 기전 (V24.5.6.8)
  - **설명**: KOSHA API 매핑의 이점을 활용하여 3섹션 추출 흐름에서 물질명(Chemical Name) 수집 및 복구 연산을 전면 폐기(자원 다이어트)하고 오직 CAS 번호와 함유량만을 수집하여 `CAS(함유량)` 형태로 입고를 완착함.
- **패턴 22**: 수학적 천칭 검문소 CAS 공백 제거 및 판정 일원화 기전 (V24.5.6.8)
  - **설명**: `verify_mathematical_천칭_filter`와 `verify_integrity_of_local_data`를 일원화하여 중복 로직의 오류 발생 가능성을 제거하고, 공백이 포함된 CAS 번호까지 정합적으로 선제 소거한 후 1단계 개별 수치 100% 초과 차단 및 2단계 범위 최대치 합계 110% 초과 시에만 차단막이 안정적으로 작동되도록 밸브를 고정함.
  - **해결책**: `verify_mathematical_천칭_filter`의 수치 검증 로직을 ① 개별 성분 저울(모든 숫자 중 100.0% 초과 즉시 Fallback)과 ② 범위 최대치 합계 저울(진짜 함량 범위 수치 최대값 누적 합계가 110.0% 초과 시에만 오염 판정)의 2단계 분리 구조로 깔끔하게 통합 결착하여 해결함.

- **패턴 25**: 자재 형태별 1섹션 분리 격리 수거 및 텍스트 온리 추론 기전 (V24.5.6.9)
  - **설명**: 디지털 텍스트와 스캔본 각각의 1섹션 추출 과정을 완전히 격리 조율하고 비전 호출 엔진을 배제함으로써, API 비용 절감과 노이즈 차단을 동시에 달성하고 체급이 높은 Gemini 2.5 Flash가 주변 문맥을 유연하게 추론해 제품명을 독점 판독하도록 가이드라인을 마감함.
- **패턴 26**: 후방 수학적 천칭 검문소 2단계 분리 계량 고도화 기전 (V24.5.6.9)
  - **설명**: 개별 성분 저울과 범위 최대치 합계 저울의 판정 공식을 정교하게 이원화하여, 공백이나 알파벳 오타가 섞인 CAS 및 날짜 오탐 노이즈는 완벽히 전처리로 빻아 지우고 개별 100% 초과 차단 및 범위 합계 110% 초과 차단을 일관성 있게 집행하도록 밸브를 고정함.
- **패턴 27**: PaddleOCR 2.7+ API 스펙 최적화 및 크래시 방지 기전 (V24.6.0.1)
  - **설명**: PaddleOCR 최신 스펙 버전의 `ocr` 메서드 내부 인자 불일치 예외를 방어하기 위해, `cls=False` 인자 제공을 전면 생략하고 순수 이미지 넘피 배열 정보만 피딩하여 CLI 및 라이브러리 가동 무결성을 영구 보장함.
- **패턴 28**: check_omission용 3섹션 성분 표 구획 격리 및 스나이퍼 누락 정화 기전 (V24.6.0.1)
  - **설명**: 무결성 누락 검사 시 문서 전체 텍스트를 대조군으로 삼아 독성 정보, 참고 문헌 등에서 유출된 CAS 번호가 검출되어 발생하는 감점 및 🟡 경고등 오탐을 완전 종식하기 위해, 3섹션 간판과 4섹션 간판 사이 영역을 정밀하게 가위질(Sandwich Cut)하여 성분표에 실존하는 진짜 CAS 번호 목록으로만 대조하도록 정비함.
- **패턴 29**: 완전 스캔본 전용 OCR 복구 기반 grounding_pool 2중 안전 장치 (V24.6.0.2)
  - **설명**: 글자 정보가 없는 완전 스캔본 처리 시 무결성 검증을 위해 `grounding_pool`을 로컬 OCR 복구 데이터(`local_ocr_html` 또는 `res_acc['raw_data']`의 태그 제거 텍스트) 혹은 `section3_text`로 안전하게 공급하여 CAS 번호 오기락 기각을 영구 방지함.
- **패턴 30**: 팁시크 V4 플래시(DeepSeek-V4-Flash) 단독 호출 및 API 규격 완벽 모방 (V24.6.0.2)
  - **설명**: 기존 무료/Gemini 대장 키 호출부를 Novita AI OpenAI 호환 규격의 `deepseek/deepseek-v4-flash` 로 전격 대체하되, 하류의 모든 JSON 파싱 로직과의 100% 호환을 보장하도록 Gemini JSON 규격(`candidates`)의 모방 딕셔너리로 래핑하여 리턴하는 구조를 완착함.
- **패턴 31**: 무정차 런타임 예외 쉴드 및 그레이스풀 격리 수거 리턴 (V24.6.0.2)
  - **설명**: 자재 파싱 도중 성분이 검출되지 않거나 예외 크래시 발생 시 배치 공정을 멈추는 `raise` 명령을 전면 폐기하고, 최외곽 `try-except` 관문에서 `🔴` 격리수거 딕셔너리를 그레이스풀하게 리턴함으로써 전체 자재의 무정차 전진(Bypass Jump) 루프 생존권을 완벽 사수함.



- **오류 명칭**: 1선 완착 바이패스 블록 내 프롬프트 문자열 유입 및 구문 에러 (SyntaxError) (V24.6.0.3)
  - **증상**: `replace_file_content`로 코드 수정 시, 치환 범위 오조준으로 인해 아랫줄 정제 프롬프트 내용이 문자열 기호 밖으로 새어 나가 붙으면서 SyntaxError 및 IndentationError가 발생해 공장이 락 상태로 전면 기동 불능이 됨.
  - **해결책**: 깨진 꼬인 블록(lines 661~688)을 통째로 걷어내고 원래의 온전한 1선 완착 바이패스 로깅 코드로 원복 수선함.
- **오류 명칭**: GCP Vertex AI REST API 호출 시 role: user 누락에 의한 HTTP 400 에러 (V24.6.0.3)
  - **증상**: 구글 AI 스튜디오 규격에서는 role 생략을 허용하나, GCP Vertex AI REST API 호출 시 role 필드 누락으로 인해 `INVALID_ARGUMENT (Please use a valid role: user, model.)` 400 오류가 발생하여 격발 실패함.
- **패턴 32**: REST AccessToken 동적 갱신 기반 버텍스 AI 마스터 열쇠 직결 기전 (V24.6.0.3)
  - **설명**: Vertex AI SDK 대신 `google.oauth2.service_account`로 발급받은 AccessToken을 Authorization 헤더에 실어 REST API로 직접 호출함으로써, 기존 AI 스튜디오 규격의 복잡한 페이로드 데이터 변환을 배제하고 100% 호환되도록 경량 결착함.
- **패턴 33**: 1선 DeepSeek 30초 가드 및 2선 Vertex Gemini 비전 무정차 Failover 기전 (V24.6.0.3)
  - **설명**: 1선 DeepSeek 채널에 30초 타임아웃을 강제하여 무한 프리징을 방어하고, 에러 감지 즉시 `try-except`로 포획하여 2선 버텍스 제미나이 채널로 무정차 롤백 Failover 하도록 배선하여 견고성을 극대화함.
- **패턴 34**: 1선 완착 성분-제품명 연쇄 바이패스(Full Bypass) 기전 (V24.6.0.4)
  - **설명**: 텍스트 분석 시 1선 ODL/밀도 클러스터링으로 성분을 우선적으로 긁어모아 1선 완착 무결성을 선제 판정하고, 완착 성공 시 제품명 LLM API와 성분 LLM API 호출을 모두 0회로 일괄 생략(Bypass)하며 파일명 기반 제품명으로 즉시 적재하여 연산 비용을 0원으로 절감하는 Full Bypass 라우팅을 구축함.
- **패턴 35**: 2중 예외 처리 검문소(Post-Validation) 및 무정차 그레이스풀 격리 수거 기전 (V24.6.0.4)
  - **설명**: 비전 구원 투수나 AI가 최종 정제하여 보고해 온 수치 데이터에 대해서도 장부 입고 직전 천칭 검증(`validate_chemical_balances`)을 2중으로 적용하여 물리적 무결성을 최종 사수하고, 만약 검문 실패 시 크래시(raise)를 배제하고 그레이스풀 격리 수거 회로를 통해 빨간불(`🔴`) 에러 딕셔너리로 우회 반환해 전체 프로세스의 영구 무정차 전진을 보증함.
- **패턴 36**: 기계실 및 GUI 2중 대조 상세 표 출력 및 이중 검수 연동 기전 (V24.6.0.5)
  - **설명**: 오프스크린 실전 레이스 구동 완료 시점에 엔진 내부 메모리 데이터(used_engine, 신호등, 정합성 점수, 천칭 필터)와 최종 GUI/Excel 기입 데이터(제품명, CAS, 측정대상 물질명)를 1:1로 매핑하여 두 개의 상세 대조 표(기본 정보 표 및 CAS/측정물질 상세 표)를 구성하여 콘솔 창에 마크다운 표 양식으로 줄 깨짐 없이 출력되도록 함으로써 최종 공정 무결성을 2중 검증할 수 있는 통로를 구축함.

- **오류 명칭**: 1선 완착 실패 후 ODL/밀도 오독 찌꺼기가 AI 정제 함량을 덮어쓰는 병합 유실 에러 (V24.6.0.6)
  - **증상**: 036번 자재에서 AI가 본문을 정독하여 정확하게 40~<50%로 추출했음에도, ODL 1선 오독인 ≥40%가 병합 단계에서 존재한다는 이유만으로 AI 정밀 데이터를 덮어써서 물결 평탄화 검증이 최종 실패하는 현상 발생.
  - **해결책**: ODL과 AI 성분의 병합 순서를 전격 뒤집어 AI의 정제 함량을 최우선 1순위로 채택하고, ODL/밀도 클러스터링 데이터는 AI가 놓친 미기재 CAS 번호에 대한 보충 백업 용도로만 안전하게 병합되도록 개정함.

- **패턴 37**: 1섹션 본문 콜론(:) 계층 추적을 통한 제품명 본문 정독 기전 (V24.6.0.6)
  - **설명**: 파일명에서 제품명을 무단 덮어쓰던 is_misread 오버라이트 로직을 완전히 폐기하고, 1섹션 본문 내부의 가. 제품명 :, 1.1 제품명 : 과 같은 정량화된 콜론 계층을 1순위로 추적하여 파싱해오는 extract_product_name_from_section_1 본문 정독 선로를 제1원칙으로 고정함. 이를 통해 상업용 명칭 힌트가 훼손되는 일을 완전히 방지함.

- **패턴 38**: 원본 데이터 스냅샷 1:1 수치 검문선 (raw_snapshot_cross_check) 기전 (V24.6.0.6)
  - **설명**: AI가 정제해 온 함량 수치가 실제 원본 텍스트의 CAS 번호 출현 좌우 150자 문맥 영역 내에 존재하는지 1:1로 매칭하는 크로스 체크 알고리즘을 이식함. 이 수치 검문선을 최종 천칭 검증 및 품질 검문에 연동하여 수치 환각(예: 003번 97% -> 4%) 발생 시 즉시 감지하여 2선 비전 소방차 또는 격리 격실로 강제 토스하게 배선함.

- **오류 명칭**: 제품명 콜론 추적 시 줄바꿈 공백 매칭에 의한 날짜 오독 에러 (V24.6.0.7)
  - **증상**: 008번 자재처럼 본문의 `Product name :` 뒤에 제품명이 비어 있고 그 아래에 `Date prepared : Nov. 29, 2018`과 같이 날짜 텍스트가 있을 때, 제품명 매칭 정규식의 공백 패턴(`\s+`)이 개행(`\n`)을 포함하여 다음 줄까지 삼켜버림으로써 `. 29, 2018`과 같은 날짜 정보가 제품명으로 오독 기입되는 현상 발생.
  - **해결책**: `extract_product_name_from_section_1` 내의 공백 매칭 패턴을 줄바꿈을 포함하지 않는 `[ \t]+`와 `[ \t]*`로 제한하여 한 줄 내에서만 매칭이 작동하도록 격벽을 수립하여 해결함.
- **오류 명칭**: 본문 부재 및 AI 제품명 미추출 오류 메시지의 최종 장부 입고 에러 (V24.6.0.7)
  - **증상**: 005번 이미지 스캔본 자재처럼 본문 텍스트가 없어 AI가 제품명을 추론하지 못할 때, `입력된 텍스트가 없으므로 제품명을 추출할 수 없습니다.`와 같은 AI의 오류 답변 문자열이 블랙리스트에 걸러지지 않고 최종 제품명 칸에 그대로 기입되어 완착되는 결함 발생.
  - **해결책**: `is_blacklisted_pn` 함수 내부에 날짜 형식 정규식(`\d{4}[.\-/]\d{1,2}`, `Nov. 29, 2018`), 오류성 메시지 키워드(`텍스트가없으므로`, `추출할수없습니다`), 한글/영문 알파벳 누락 판정 등을 보강하여 부적합 제품명을 즉시 차단하고, 안전하게 파일명 기반의 청정 단어로 우회(Fallback)하도록 제품명 확정 검문소를 전면 고도화함.

- **패턴 39**: 제품명 공백 제한 매칭 및 날짜/에러 메시지 정밀 기각 블랙리스트 기전 (V24.6.0.7)
  - **설명**: 본문 제품명 추출 시 줄바꿈 오독을 원천 방어하기 위해 공백 매칭을 한 줄 내의 탭/스페이스(`[ \t]+`)로 한정하였으며, AI의 오류 안내 문구 및 날짜 포맷이 최종 제품명 장부에 스며드는 것을 방지하기 위해 정교화된 제품명 블랙리스트 가드레일(`is_blacklisted_pn`)을 이식하여 파일명 기반의 청정 명칭으로 안전하게 Fallback되도록 설계함.

- **오류 명칭**: sys.path 과잉 세척으로 인한 Python 내장 라이브러리(base64 등) Import Error (V24.6.0.8)
  - **증상**: `sanitize_paths`가 DLL 충돌 방지를 위해 `python313` 경로를 무조건 `sys.path`에서 삭제하면서, 내장 라이브러리 `Lib` 폴더까지 누락시켜 `ModuleNotFoundError: No module named 'base64'`가 유발되어 가동 정지됨.
  - **해결책**: 필터링 대상 조건을 `site-packages` 디렉토리가 포함되어 있으면서 3.13 경로인 곳으로만 정밀하게 제한하여 표준 라이브러리는 보존하고 외부 오염 패키지만 격리함.
- **오류 명칭**: ODL 및 밀도 오독 데이터의 정밀 정규식(Regex-Recovery) 기호 Overwrite 누출 에러 (V24.6.0.8)
  - **증상**: ODL 파서가 부등호를 유실한 채 `86%`를 먼저 점유하면, 뒤이은 `Regex-Recovery`가 `<86%`를 가져와도 `final_quality_control` 내의 선점 조건(`old_cont == "미기재%"`)에 가로막혀 기호가 버려지거나, 밀도 클러스터링 병합 시 정규식 수선본이 덮어씌워져 기호가 유실됨.
  - **해결책**: `final_quality_control`에서 `Regex-Recovery` 데이터 유입 시 기존 ODL 데이터를 강제 덮어쓰기(Overwrite) 하도록 서열을 재조정하고, 병합부(`merged_map_1st`)에서도 정밀 정규식 데이터가 이미 존재할 경우 덮어쓰지 않도록 안전 가드를 이식함.

- **패턴 40**: 제품명 딥시크 프롬프트 수치 귀속 인터락 기전 (V24.6.0.8)
  - **설명**: 딥시크 정제 프롬프트에서 규격 제외 문구를 지우고, 제품명 우측에 결착된 농도 범위 수치(%, ppm)나 고유 식별 넘버는 제품 간 혼선을 막는 핵심 명칭이므로 임의 누락 없이 박제하도록 강제 조치하여 제품명 유실을 차단함.
- **패턴 41**: 3섹션 조기 종료 텍스트 파쇄 차단막 고도화 기전 (V24.6.0.8)
  - **설명**: 전체 페이지 순회 루프와 `find_section3_pages` 정찰 루프 초입에 `SECTION 4`, `4. 응급조치 요령` 대간판 문구를 포착할 경우 즉시 루프를 탈출(`break`)하도록 센서 키워드를 보강하여 뒷페이지의 불필요한 하이픈 노이즈 유입을 물리적으로 차단함.
- **패턴 42**: 개별/합량 저울 130% 실무 상한선 상향 조정 기전 (V24.6.0.8)
  - **설명**: 수학적 천칭 저울 검문 및 함량 검문소 경보 마지노선 수치를 기존 120.0%에서 130.0%로 상향하여, 다성분 범위형 정상 자재가 억울하게 차단되는 오탐을 방어함.

- **오류 명칭**: find_section3_pages 함수 내 2페이지 초과 시 조기 강제 리턴 버그 (V24.6.0.8)
  - **증상**: 3섹션 성분 표가 3페이지 이상으로 이어져 있거나 4항 대간판(응급조치 요령)이 나타나기 전에 추가 성분이 있더라도, 3섹션 탐색 페이지가 2권 축적되는 시점에 강제 조기 종료(`if len(pages) >= 2: return sorted(list(set(pages)))`)되어 성분 누락을 발생시키는 결함 상존.
  - **해결책**: 해당 조기 리턴 분기 조건문 2줄을 완전히 철거하여, SECTION 4 대간판 문구가 포착될 때까지 3섹션 성분 페이지 수집 관로를 끝까지 열어두도록 개선함.
- **패턴 43**: 딥시크 정제 프롬프트 사명 단독 추출 방지 지침 보강 기전 (V24.6.0.8)
  - **설명**: 딥시크 정제 프롬프트 내에 'Sigma'나 'Sigma-Aldrich' 와 같은 특정 사명 단독으로 제품명이 추출되는 오독 현상을 원천 방어하기 위해, 농도 수치(%, ppm)와 실제 고유 화학물질명이 포함된 전체 상표명을 반드시 보존해 박제하도록 금지 및 지시 앵커를 추가 적용함.

- **오류 명칭**: 다중 페이지 3섹션 성분 표의 페이지 경계부 데이터 누락 및 조기 종료 결함 (V24.6.0.8)
  - **증상**: 3섹션 성분 표가 여러 페이지에 걸쳐 존재할 때, (1) 수직 상속(병합추정) 변수가 페이지 단위로 초기화되면서 페이지 경계선 바로 다음 성분들이 함량을 상속받지 못해 `미기재%`로 짤려 나가는 현상, (2) 1페이지 하단이나 표 내부 항목 번호(예: `4. 산화방지제`, `8. 노출기준`)가 4항 대간판(exit_pattern)으로 오인 매칭되어 2페이지를 수집하기도 전에 조기 종료되어 성분이 대거 짤려 나가는 현상이 발생함.
  - **해결책**:
    1. `find_section3_pages`에서 `exit_pattern` 매칭 시, 해당 매칭 라인 아래로 해당 페이지 내에 CAS 번호 패턴이 1개라도 관측된다면 성분 표가 여전히 지속되고 있는 것으로 간주하여 조기 리턴을 보류하는 '오탐 방지 인터락' 추가.
    2. `extract_components_odl_robust` 및 `extract_from_text_regex` 함수 시그니처에 `inherited_last_valid`를 추가하여 페이지 간 `last_valid_info` 연속성을 확보하고, 페이지 경계 진입 시 Y축 오차 검사를 스킵하고 X축 정렬 오프셋만으로 수직 상속을 집행할 수 있는 `is_first_in_page` 안전 밸브 장착.

- **패턴 44**: 4항 조기 종료 오탐 방지 인터락 기전 (V24.6.0.8)
  - **설명**: 3섹션 성분 표 페이지 수집 중 4항 대간판(exit_pattern) 오인 매칭이 발생할 때, 해당 매칭 지점 하단에 CAS 번호 패턴이 여전히 1개 이상 실존하는 경우 3섹션 표가 해당 페이지 하단까지 계속되는 상태로 판단하여 조기 종료를 유예하고 루프를 지속하도록 함으로써 다중 페이지 자재의 데이터 짤림을 원천 방어함.
- **패턴 45**: 페이지 경계 수직 상속(병합추정) 연속성 인계 및 X축 전용 경계 매칭 기전 (V24.6.0.8)
  - **설명**: `extract_from_text_regex` 함수 시그니처에 `inherited_last_valid` 파라미터를 추가하여 페이지 순회 간 수직 상속 함량 기준값(`last_valid_info`)을 인계받으며, 새로운 페이지의 최초 성분(`is_first_in_page == True`)에 대해서는 Y축 오차범위 검증을 안전하게 우회하고 X축 정렬 오프셋만으로 이전 페이지 함량을 상속할 수 있도록 설계하여 다중 페이지 병합 셀 구조에서의 데이터 유실을 완벽히 해결함.
- **오류 명칭**: 3섹션 내 4항 혼입 시 맹목적 텍스트 파쇄 차단막에 의한 페이지 수집 및 대조군 누락 버그 (V24.6.0.8)
  - **증상**: 002번 Cd 자재처럼 3섹션 성분 표 하단에 4항 대간판(응급조치요령)이 공존하거나 연이어 등장할 때, `find_section3_pages` 루프 초입에 위치한 '텍스트 파쇄 차단막'(i > 0 이고 4항 키워드 매칭 시 break)이 페이지를 수집하기도 전에 작동하여 해당 페이지 전체가 3섹션 대상에서 배제됨. 또한, `full_text_for_grounding` 빌드 단계에서도 동일 차단막이 텍스트 누적 전에 작동하여 3섹션 데이터가 대조군(`grounding_pool`)에서 누락되어 성분이 통째로 짤려 나감.
  - **해결책**:
    1. `find_section3_pages` 함수 내의 맹목적 `break` 차단막 로직을 전면 제거하여, 수집된 `pages` 하단부에 위치한 정교한 `exit_pattern` 및 오탐 방지 인터락에 차단 통제권을 단독 이임함.
    2. `_process_msds_pipeline_impl` 내 `full_text_for_grounding` 누적 시, 페이지 텍스트를 먼저 누적하고 난 뒤 차단막 조건을 검사하고 탈출하도록 순서를 보정함.

- **패턴 46**: 3섹션 개방형 페이지 수집 및 단독 exit_pattern 조기 제어 기전 (V24.6.0.8)
  - **설명**: 3섹션 페이지 수집 시 맹목적인 `break` 조건을 완전히 제거하고, 아래에 배치된 오탐 방지 인터락이 연계된 `exit_pattern` 만으로 조기 종료 제어권을 완전히 행사하도록 함으로써, 3섹션과 4항이 물리적으로 공존하는 모든 다중 페이지 문서를 안전하게 100% 수집함.
- **패턴 47**: grounding 대조군 선 수집 후 조기 종료 순서 보정 기전 (V24.6.0.8)
  - **설명**: `full_text_for_grounding` 텍스트 누적부의 실행 순서를 보정하여, 4항 키워드가 등장하는 페이지일지라도 텍스트를 `grounding_pool`에 먼저 누적한 후에 비로소 break 루프 탈출을 실행함으로써, 마지막 3섹션 본문 데이터가 통계적 품질 대조군에서 기각되는 오류를 원천 차단함.

- **오류 명칭**: 정규식 추출기 내부의 2중 차단막(break) 오작동 및 ODL/밀도 병합 시 정답 덮어쓰기 유실 결함 (V24.6.0.8)
  - **증상**: (1) 10번 포름알데하이드 자재처럼 3섹션과 4항이 같은 페이지 내에 밀접 공존할 때, `extract_from_text_regex` 내부 Y축 스캔 루프에서 '4항' 키워드를 포착하는 순간 `break`를 쳐서 그 아래에 배치된 진짜 성분(포름알데하이드 등)을 읽지 못하고 누락시키는 결함 발생. (2) 4번 자재 등에서 이전 페이지로부터 수직 상속 연계된 유효한 수치 함량(95~100%)이 ODL/밀도 후순위 여백 찌꺼기(미기재%) 데이터에 의해 덮어씌워져 유실되는 현상 발생.
  - **해결책**:
    1. `extract_from_text_regex` 내부의 L1793~1796 구간 4항 조기 탈출 break 로직을 전면 삭제하여, 3섹션 내에 4항이 있더라도 성분표 전체를 끝까지 온전하게 정독하도록 개선함.
    2. `final_quality_control` 함수 내부 병합 루프에 `old_cont != "미기재%" and cv == "미기재%"` 조건일 때 덮어쓰기를 무조건 패스(우회)하는 '정답 선점 잠금장치(Gate-Lock)' 회로를 이식하여 기존 수치 정답을 사수함.

- **패턴 48**: 정규식 추출 엔진 내 4항 조기 브레이크 전면 철거 기전 (V24.6.0.8)
  - **설명**: 3섹션 성분 추출 시 Y축 좌표 스캔 도중 4항 키워드 발견으로 루프를 중간 탈출하는 독소 조건문 블록을 완전히 삭제함으로써, 3섹션 테이블 내에 4항이 혼입/공존하더라도 성분 번호와 함량 리스트 전체를 무손실로 끝까지 수집 정독함.
- **패턴 49**: 정답 선점 잠금장치(Gate-Lock) 함량 병합 수호 기전 (V24.6.0.8)
  - **설명**: 최종 품질 제어 및 성분 병합 시, 기존 refined_dict 에 이미 완착된 함량 수치가 유효 수치(`!= "미기재%"`)이고 새로 병합할 성분 함량이 무의미한 찌꺼기(`== "미기재%"`)라면 기존 데이터를 강제 보호하도록 pass 처리하는 인터락을 결착하여 정답 유실을 방어함.

- **오류 명칭**: 10번 파일 다중 페이지 성분 짤림 및 find_section3_pages 내 조기 리턴 오류 (V24.6.0.9)
  - **증상**: 3섹션 성분 표 탐색 도중 4항 대간판(exit_pattern) 매칭 시, 함수 전체를 조기에 return 종료함으로써 다음 페이지에 지속되는 성분 정보들이 수집 대상에서 누락되는 현상 발생.
  - **해결책**: `find_section3_pages` 함수에서 exit_pattern 매칭 후 조기 return 하던 구문을 `break`로 교격하여 이 페이지의 검사는 중단하되 다음 페이지로 루프를 지속 순회하여 누락 없이 다중 페이지 성분표를 수집하도록 개선함.

- **오류 명칭**: 수집 마샬링 관로 내 기존 수치 정답 덮어쓰기 유실 오류 (V24.6.0.9)
  - **증상**: 4번 자재 등에서 ODL/정규식 병합 시, 이미 수확된 수치 정답이 존재하더라도 후속 노이즈 데이터('미기재%')가 유입되면 맹목적으로 기존 값을 덮어써서 정답이 유실되는 결함 발생.
  - **해결책**: 수집 마샬링 관로(`existing_item` 처리부)에 이미 수확된 정답이 진짜 수치(미기재%가 아님)이고 신규 진입자가 '미기재%' 노이즈라면 덮어쓰기를 원천 차단하고 통과시키는 '정답 방어 인터락'을 이식하여 기존 수치를 보호함.

- **오류 명칭**: 핵심 파싱 엔진 변경에 따른 골든 마스터 지문 불일치 경고 오류 (V24.6.0.9)
  - **증상**: 핵심 파싱 엔진 코드를 수정한 후 `GOLDEN_HASH` 값을 동기화하지 않아 회귀 테스트 실행 시 형상 변조 경고 로그(`🚨 [형상 변조 경고]`)가 출력됨.
  - **해결책**: 새로 계산된 16자리 sha256 해시 지문(`68f4476370e09cd8`)을 `msds_engine_v6.py` 내 `GOLDEN_HASH`에 수술적으로 이식하여 변조 경고등을 해제함.
- **오류 명칭**: 010번 자재 계단식 이름 미상속 및 독성 분류 노이즈 셀 오독 에러 (V24.6.1.0)
  - **증상**: 010번 자재처럼 첫 행에는 물질명만 있고 두 번째 행에 CAS 번호와 수치 기호(H-코드 등)가 혼재하는 비정형 계단식 분할 서식에서, 독성 분류 노이즈가 수치 기호로 오독되어 함량이 오염되고, 물질명 상속 버퍼가 경계를 넘어 오버플로우되거나 오작동하여 이름 상속이 실패하는 현상 발생.
  - **해결책**: 독성 분류 전용 차단 마커(`hazard_rx`) 필터망을 구축해 노이즈 셀 진입을 물리적으로 차단하고, CAS 번호가 없는 단독 물질명 행을 감지하여 수직 상속 버퍼(`last_row_single_name`)에 격리 파킹한 뒤, 루프 종료 시점마다 버퍼를 강제 초기화하여 타 자재 경계 침범 회귀를 영구 방어함.
- **오류 명칭**: 모의 테스트 mock 바인딩 클래스 메서드 NameError 및 수식 기호 오입력 에러 (V24.6.1.0)
  - **증상**: 모의 테스트 `test_html_table_parsing_regression` 실행 시 클래스 메서드로 정의된 `parse_html_table_to_components`를 글로벌 스코프에서 직접 참조하여 `NameError`가 발생했으며, 함량 셀 내의 꺾쇠(>, <) 및 등호(=) 기호가 기존 이름 필터 정규식에 걸리지 않아 함량 수치 셀이 이름 후보로 잘못 채택되는 문제 발생.
  - **해결책**: `MockEngine`에 바인딩할 함수를 `MSDSEngineV6.parse_html_table_to_components` 클래스 메서드로 명시적 지정하여 해결하고, 정규식 패턴을 `^[\d\s.,\-~%<>≤≥=∼～–—\+]+$` 으로 확장해 수치 기호가 포함된 셀이 이름으로 오인입되는 관로 누수를 차단함.
- **오류 명칭**: 대용량 범위 자재 및 007번 디지털 자재의 110% 초과 기각 에러 (V24.6.2.0)
  - **증상**: 정상적인 대용량 중첩 자재의 범위 상한치 합산이 110.0%를 초과하는 경우, 수학적 천칭 검증 기준(110.0% 이하)에 걸려 오염 자재로 오판당하고 Bypass가 차단되거나 불필요한 AI 호출/기각으로 🟡/🔴 등이 켜지는 현상 발생.
  - **해결책**: `validate_chemical_balances`와 `verify_mathematical_천칭_filter` 양쪽 검문소 내 범위 최대치 총합 검증의 임시 한계 상한선을 110.0%에서 130.0%로 전격 조정하여 유연한 통과 실무 기준을 확립함.
- **오류 명칭**: AI 응답 구성성분 기호 미정제 및 엑셀 적재 규격 불일치 에러 (V24.6.2.0)
  - **증상**: AI 모델이 함량 기호나 범위를 분석하여 리턴할 때, 하류 정류 레이어인 `clean_percentage` 필터를 거치지 않고 원형 그대로 엑셀 저장소로 넘어가게 되어 규격화되지 않은 문자열 찌꺼기가 적재되거나 표기 일관성이 깨지는 문제 발생.
  - **해결책**: `_process_msds_pipeline_impl` 함수 내부 AI 응답 수거부의 구성성분 루프 직후 엑셀 출력 직전 구간에 `msds_utils_v3.clean_percentage`를 강제 관통하도록 1선 직결 납땜 배선을 반영함.
- **오류 명칭**: OCR 스캔본 내 함량 부등호 오타("미맊", "미밖", "밎안", "이핚")에 의한 천칭 검문소 기각 에러 (V24.6.2.0)
  - **증상**: OCR 스캔 시 한글 범위어 및 부등호 결착 조건어 부분이 잉크 가루나 오타("미맊", "미밖", "밎안", "이핚" 등)로 오독되어 정형화되지 못해, 함량 천칭 검증 및 범위 정제 알고리즘에서 수치 기호 오인으로 인해 기각당하는 에러가 상존함.
  - **해결책**: `msds_utils_v3.py` 내 `clean_content_text` 함수 초입에 정형화 오타 Fuzzy 정류 정규식 세트(`re.sub(r'미\s*[만맊밖먄내발방안]|밎안|미밖', '미만', ...)` 등)를 격발 주입하여 사전에 표준 기호로 세척하도록 보강함.

- **패턴 50**: exit_pattern break 교격 및 마샬링 정답 방어 인터락 기전 (V24.6.0.9)
  - **설명**: 3섹션 페이지 순회 중 exit_pattern을 만나면 return 대신 `break`로 교격하여 다음 페이지를 끝까지 탐색하게 배선하고, 마샬링 관로에서 기존 수치 데이터가 후속 '미기재%' 노이즈에 의해 덮어씌워지지 않도록 2중 안전 장치(pass 차단막)를 결착함.
- **패턴 51**: 계단식 상표명 수직 상속 및 독성학 분류 격실 정밀 차단 기전 (V24.6.1.0)
  - **설명**: CAS 번호 없이 물질명만 단독으로 선점한 첫 행을 감지하여 `last_row_single_name` 버퍼에 임시 파킹하고, 다음 행의 CAS 기둥 매핑 시 해당 상표명을 완착 수직 상속하는 전용 레일을 구축함. 아울러 H-코드 등 법적 규제 한계치 노이즈를 솎아내는 `hazard_rx` 정밀 필터를 결착하고 루프 매 회차 종료마다 상속 버퍼를 강제 비움으로써 타 자재로의 이름 오염 전이를 완벽 차단함.

- **오류 명칭**: 1선 DeepSeek Novita API 호출 타임아웃 15초에 의한 Read timed out 오류 (V24.6.1.1)
  - **증상**: 해외망 통신 상태 지연이나 1선 Novita DeepSeek API 응답 지연 시, 기본 15초의 짧은 타임아웃 한도 탓에 `Read timed out` 에러가 유발되어 강제로 2선 Vertex Gemini로 Failover 롤백이 격발되고 비용 낭비 및 지연이 발생하는 문제 발생.
  - **해결책**: 타임아웃 값을 기존 15초에서 30초로 상향 조정하고 주석 및 로그를 갱신하여 넉넉한 네트워크 가드레일을 확보함.

- **패턴 52**: 1선 팁시크 30초 가드레일 대기 및 Failover 회선 안정성 확보 기전 (V24.6.1.1)
  - **설명**: 1선 DeepSeek REST 호출의 기본 대기 타임아웃 임계값을 30초로 넉넉하게 스케일업함으로써 일시적인 네트워크 지연 시 불필요하게 2선 우회 관로로 롤백되는 비용 낭비 현상을 원천 방단하고 통신 복원 안정성을 보장함.
- **오류 명칭**: 구형 파일명 검문소 및 2선 청소부 기동 모듈 간섭에 의한 제품명/함량 왜곡 오류 (V24.6.1.2)
  - **증상**: AI가 추출한 순수 상표명이 파일명 기반 필터(`[최종 출구 파일명 검문소]`)에 의해 파일명으로 오버라이트되어 상표명이 왜곡되고, 2선 청소부(Gemini-2.5-Flash-Lite)가 불필요하게 가동되어 복합 부등호 수선 등을 수행하면서 지연 및 자원 낭비가 발생하는 현상.
  - **해결책**: 파일명 검문소 코드 블록과 2선 청소부 기동 모듈을 완전히 삭제하여 AI 추출 순수 상표명이 하단 연산 벨트로 곧바로 흐르도록 1선 직결 관로를 완성함.

- **패턴 53**: 순수 상표명 보존 및 1선 직결 파이프라인 단일화 기전 (V24.6.1.2)
  - **설명**: 파일 이름 교차 검증 검문소를 영구 철거하여 AI가 판독한 제품명의 훼손을 차단하고, 2선 청소부 API 호출 모듈을 완전히 제거함으로써 1선 정규식/격자 엔진 통과 데이터를 중간 간섭 없이 하단 무결성 판별부로 직결 이송하는 단일 파이프라인 구조를 구축함.

- **오류 명칭**: 외부 AI 응답 거부 또는 키 오독(한글/영문 변조)에 의한 파싱 마비 오류 (V24.6.1.3)
  - **증상**: 대형 AI 모델이 응답을 반환할 때 `"구성성분"` 키 대신 `"성분"`, `"components"` 등의 영한 혼용 변칭을 사용하거나 함량/CAS 컬럼명을 오독해오면 파싱 루프 진입이 실패하여 `외부 AI 추출 결과가 존재하지 않음` 에러로 강제 격리 수거 처리되어 자재 무결성이 손상됨.
  - **해결책**: AI 응답 처리부 초입에 유연한 키 정규화 노멀라이저(Alternate Key Normalizer)를 이식하여 alternate_key들을 `"구성성분"`, `"content"`, `"cas"` 표준으로 보정하고, 통신 단절이나 포맷 파괴 등 극단적 장애 시에는 상류 1선 정규식 엔진이 적재해 둔 `checked_1st` 백업 자산을 장부에 강제 완착시키는 2중 안전 가드레일(최후방 회생 가드레일)을 결착함.

- **패턴 54**: AI 오독 방지 키 노멀라이저 및 1선 백업 자산 최후방 회생 기전 (V24.6.1.3)
  - **설명**: 외부 AI의 키명 오독 및 변조 현상을 방어하기 위해 JSON 응답을 수령한 즉시 구성성분/함량/CAS 번호의 변칭(alt_keys)들을 표준 포맷으로 강제 수선 매핑함. 아울러 AI 호출 실패나 파싱 완전 붕괴 등 최악의 국면이 도래하더라도 1선 엔진의 수거 데이터인 `checked_1st`가 생존해 있다면 이를 강제 복구해 최종 components에 최종 안착시키는 2중 잠금식 안전망을 구축함.

- **오류 명칭**: GUI 초기화 및 엔진 새로고침 시 캐시 레지스트리 동결 재사용 에러 (V24.6.1.4)
  - **증상**: GUI에서 전체 초기화 또는 엔진 새로고침을 실행해도 메모리 캐시 및 `smu_cache.json`만 지워질 뿐, 엔진 레벨의 캐시 레지스트리인 `msds_cache_registry.json`은 그대로 상주하여 틀린 정답셋 캐시가 계속 불러와지는 오류 발생.
  - **해결책**: `smu_gui.py` 내 `reset_all` 및 `reload_engine` 함수에 `msds_cache_registry.json` 파일을 찾아 물리적으로 삭제하는 안전 코드를 수술적으로 이식하여 해결함.

- **패턴 55**: GUI 캐시 레지스트리 영구 초기화 및 정화 연동 기전 (V24.6.1.4)
  - **설명**: GUI 전체 초기화 및 엔진 새로고침 시 기존 메모리 캐시 및 `smu_cache.json` 세척 외에, 엔진 레벨의 캐시 장부 파일(`msds_cache_registry.json`)까지 동시 탐색하여 물리적으로 삭제함으로써, 틀린 캐시 값이 복구되어 재사용되는 관로 오염 현상을 원천 방어함.

- **오류 명칭**: MSDS 3섹션 천칭 검문소의 단순 함량 수치 무차별 합산 오탐 에러 (V24.6.1.5)
  - **증상**: `verify_mathematical_천칭_filter`에서 문맥 정보 없이 전체 텍스트에서 임의의 숫자들을 긁어모아 누적 합산하여 110%를 비교하는 바람에, H-코드(H314)나 TWA 노이즈가 함량으로 오인되어 멀쩡한 1선 엔진이 차단(Bypass 기각 및 Fallback 유도)되는 오탐 문제 발생.
  - **해결책**: 본문 내 유효한 CAS 번호 패턴의 위치(Anchor)를 먼저 파악한 뒤, 각 CAS 번호가 위치한 물리적 행(Line) 및 앞뒤 50자 반경 내에서만 함량 수치를 한정 파싱하고, H코드/날짜/CAS 자체를 소거한 후 검증하는 형태로 천칭 필터를 고도로 고도화함.

- **오류 명칭**: 1선 DeepSeek 호출 시 네트워크 장애 및 타임아웃에 의한 무한 블로킹 에러 (V24.6.1.5)
  - **증상**: 1선 DeepSeek(novita.ai) API 호출 시 타임아웃 30초에 걸렸음에도, 네트워크/프록시 및 라이브러리 소켓 유지 특성에 의해 소켓이 차단되지 않고 무한 대기 상태에 빠져 전체 프로세스가 행(hang)이 걸리는 마비 현상 발생.
  - **해결책**: `call_deepseek_with_retry` 내부의 API 타임아웃을 12초(커넥션 5초, 읽기 12초)로 대폭 단축 및 세분화하고, `msds_engine_v6.py` 최상단에 `socket.setdefaulttimeout(20.0)`을 글로벌하게 매립하여 모든 API 및 네트워크 호출이 20초 이내에 강제 종료 및 Failover 회군을 보장하도록 가드레일을 구축함.

- **오류 명칭**: 디지털 문서 2선 텍스트 AI 호출 시 과도한 이미지 전송으로 인한 업로드 병목 에러 (V24.6.1.5)
  - **증상**: 디지털 자재 정제 목적으로 2선 텍스트 AI 호출 시, 불필요하게 전체 페이지 이미지 데이터(`image_list`)를 인라인으로 수송하는 바람에 페이로드 크기가 수십 MB로 폭증하여 네트워크 업로드 지연 및 API 전송 마비 현상 발생.
  - **해결책**: 텍스트 2선 정제 경로에서는 OCR 비전이 불필요하므로 `image_list` 루프를 과감히 제거하고 오직 `raw_prompt` 텍스트만 전송하도록 페이로드를 초경량 최적화함.

- **패턴 56**: CAS 번호 Anchor 기반의 국소 근접 천칭 검문 기전 (V24.6.1.5)
  - **설명**: 텍스트 무차별 수치 긁어모으기 방식을 전면 폐기하고, 검출된 CAS 번호의 인접 물리 행 및 앞뒤 50자 이내의 근접 컨텍스트 바운더리 안에서만 함량 수치를 제한적으로 파싱하여, H-코드/날짜 등 외부 노이즈의 함량 둔갑 오탐을 완벽 차단하고 무결성을 100% 방어함.

- **패턴 57**: 글로벌 소켓 기본 타임아웃 강제 및 초경량 2선 페이로드 최적화 기전 (V24.6.1.5)
  - **설명**: 외부 API(Novita, Vertex 등)의 비정형 지연이나 Keep-Alive 행(hang) 현상을 방어하기 위해 `socket.setdefaulttimeout(20.0)`을 이식하고, 1선 타임아웃을 12초로 단축하여 예기치 않은 블로킹을 사전 격파함. 동시에 디지털 2선 복구 호출 시 불필요한 이미지 데이터 수송 루프를 완전히 걷어내어 네트워크 오버헤드를 99.9% 절감하고 속도 무결성을 종결함.

- **오류 명칭**: 표 외곽 섹션의 CAS 번호 오유입 및 수평선 이탈 함량 오독 에러 (V24.6.1.6)
  - **증상**: `page.get_text("words")` 전체 스캔으로 인해 11항(독성), 15항(규제) 등 3섹션 외부 영역의 CAS 번호가 유입되고, `verify_mathematical_천칭_filter`에서 앞뒤 50자 근접 컨텍스트 탐색 시 H코드/타 행 함량 수치가 혼입되어 정상 자재가 탈선하는 에러 발생.
  - **해결책**: `extract_from_text_regex`에 상류 ODL 및 밀도 클러스터링이 식별해 낸 tr, td 격실 바운딩 박스(`table_bboxes`)를 전달하여 이 박스 내부에 속하는 단어만 긁어오도록 좌표계 락다운(Boundary Lockdown)을 결착함. 동시에 `verify_mathematical_천칭_filter` 내 수집 대상을 오직 동일 수평선상(HTML `<tr>` 혹은 일반 텍스트 동일 line) 내부의 수치로만 한정하여 락다운시킴.
- **오류 명칭**: 비정형 표 외곽 제품명 유실 및 002번 등 황색등(🟡) 오류 (V24.6.1.6)
  - **증상**: 제품명이 정형 표 서식 밖에 파묻혀 있거나 LLM이 제품명을 비어있거나 불량한 상태로 리턴하면 제품명이 유실되어 002번 등에서 신호등이 🟡 노란불로 내려앉는 정합성 미달 결함 발생.
  - **해결책**: `extract_section_1`에 'Description', 'Identification' 의미론적 추적 패턴을 확장 보완하고, 제품명이 미출시되거나 2자 미만 혹은 오류 찌꺼기가 포함될 시 상류 파일명 힌트(`file_pn_hint`)를 강제 복사 입고하는 마스킹 쉴드를 보강하여 황색등을 영구 박멸함.
- **오류 명칭**: 가상 세로 관로 노이즈 및 구분(종) 기둥 순번 오독에 의한 정합성 미달 에러 (V24.6.3.2)
  - **증상**: 8번 사라퐁 자재 등에서 성분 함량이 아닌 구분(종) 기둥의 순번 숫자(1, 2, 3 등)나 무관한 수치 정보가 함량 수치로 오독 매핑되는 노이즈가 발생해 최종 정합성 점수가 하락함.
  - **해결책**: 가상 세로 관로(Column Corridor) 레이다 및 중심점 매핑 인터락을 통해, 함량 대간판 아래가 아닌 구분(종) 기둥의 중심점과 매핑되는 수치를 영하 5만점 처리로 강력하게 기각(소각)하여 해결함.
- **오류 명칭**: 범위 기호 없는 단일 부등호 복합 파편 오독에 의한 데이터 오염 에러 (V24.6.3.2)
  - **증상**: 범위 연결 기호(`~`)가 없으면서 단일 부등호만 단 1개 포착된 복합 파편(예: 3 < 1)이 오독 유입되어 잘못된 함량이 검출되고 장부가 오염되는 현상 발생.
  - **해결책**: 범위 기호가 없고 부등호가 1개인 경우 전방의 순번 노이즈 숫자를 전면 거세하고 후방의 진짜 함량 수치(예: `<1%`)만 단독 추출하는 인터락을 이식하여 데이터 무결성을 사수함.
- **오류 명칭**: 1선 추출 성공 시 즉시 튜플 반환으로 인한 후속 파이프라인 연동 마찰 및 AttributeError 에러 (V24.6.0.0)
  - **증상**: 1선 정규식/격자 엔진에서 자산 확보 성공 시 `return components, "Text-Analytic"` 튜플을 즉시 리턴하게 되어, 후속 파이프라인(`msds_core.py`) 및 회귀 테스트 함수에서 딕셔너리(`dict`) 반환을 기대하고 `res.get()` 등을 호출할 때 `AttributeError: 'tuple' object has no attribute 'get'` 예외가 발생하여 프로세스가 다운됨.
  - **해결책**: 1선 추출 성공 시 즉시 튜플을 리턴하지 않고 기존 파이프라인의 흐름(최종 엑셀 포장 및 `res_obj` 딕셔너리 빌드)을 타도록 조율하되, 완료 시점에 출력되는 완료 로그와 최종 자산 출력 포맷만 요청된 직관적 요약 형식(다이어트 로그)으로 출력되도록 연동 수선함.
- **오류 명칭**: 로그 다이어트 패치의 1선 실패 시 조기 리턴으로 인한 스캔본 AI 구출 기회 차단 에러 (V24.6.0.0)
  - **증상**: 로그 다이어트 패치 적용 시 1선 추출 실패(`else:` 분기) 시 `return [], "Error"` 튜플을 즉시 리턴하게 됨으로써, 뒤에 배치된 이미지 기반 스캔본 AI 구출(Chunk 12) 기회를 잃고 무조건 실패를 반환하게 되는 논리 흐름 오류 발생.
  - **해결책**: 1선 추출 실패 시 조기 리턴을 생략하고 즉시 스캔본 구출 AI 로직(Chunk 12)으로 흘러갈 수 있도록 예외 흐름을 조율하여 해결함.



- **패턴 58**: ODL/밀도 격실 좌표계 락다운(Boundary Lockdown) 및 수평선상 천칭 계량 기전 (V24.6.1.6)
  - **설명**: `extract_from_text_regex` 구형 전체 스캔을 중단하고 상류에서 찾은 td/tr 격실 바운더리 박스 마진 3포인트 이내의 단어만 정규식 수집 범위로 차단하여 외곽 섹션 CAS 오유입을 완전 분리함. 또한, `verify_mathematical_천칭_filter`에서 근접 50자 컨텍스트를 배제하고 HTML `<tr>`~`</tr>` 혹은 단일 line 수평선상 격실 내부에서만 수치를 계량하도록 동결하여 0.1~<1% 등의 정밀한 1선 자산이 H코드 등 노이즈에 억울하게 기각당하지 않도록 차단 보호함.
- **패턴 59**: 1섹션 다중 레이어 의미 스캔 및 제품명 마스킹 쉴드 인터락 기전 (V24.6.1.6)
  - **설명**: `extract_section_1` 내에 Description, Identification 등 다중 단락 키워드 정규식을 매립하여 제품명이 비정형 위치에 있더라도 정확히 추적하고, AI 제품명이 불량/공란인 조건(2자 미만 또는 '미추출', '오류', 'none' 등 포함 시)에 맞춰 파일명 기반 청정 힌트(`file_pn_hint`)로 강제 복사 오버라이팅하는 마스킹 쉴드를 결착하여 제품명 유실을 원천 격파함.
- **패턴 60**: 수학적 천칭 저울 상한 130% 임시 조정 및 OCR Fuzzy 오타 정류 기전 (V24.6.2.0)
  - **설명**: 007번 디지털 자재 등 범위 상한 중합이 110%를 초과하는 정상 자재의 억울한 기각을 막고자 저울 상한선을 130.0%로 전격 패치하였으며, 스캔 깨짐 단어("미맊", "미밖", "밎안", "이핚")의 자가 표준화 전처리 규칙을 완착하여 오탐률을 혁신적으로 제거하고 엑셀 기입 직전 퍼지 기호 세척망 관통 배선으로 규격 무결성을 최종 확보함.

- **패턴 61**: ODL 행 전수조사 및 CAS 체크디지트 검증 인터락 기전 (V24.6.3.0)
  - **설명**: `parse_row_robust_v2` 내에서 함량 탐색 조기 탈출 로직을 제거하여 다중 컬럼에 걸쳐 있는 수치들을 전수 수거하며, CAS 번호 추출 즉시 `verify_cas_number` 검문소(체크디지트 수학적 검산)를 관통시켜 노이즈 유입을 원천 차단함.
- **패턴 62**: 비전 정찰병 Vertex 직결 및 Fail-Safe 정류 기전 (V24.6.3.1)
  - **설명**: 텍스트 탐지 실패 자재용 비전 정찰병 기동 시 존재하지 않는 함수 오타 결함을 해결하기 위해 `call_vertex_gemini_with_retry`로 단독 직결하였으며, 마크다운 기호(` ```json ` 등)의 동적 평탄화 전처리 및 정찰 내부 오류 발생 시 `pass` 가드레일을 완착하여 연속 조사를 보장하고 영구 행(hang) 현상을 원천 격파함.
- **패턴 63**: 가상 세로 관로 중심점 매핑 및 구분(종) 순번 기각 기전 (V24.6.3.2)
  - **설명**: 단어의 수평/수직 정렬을 추적하는 가상 세로 관로(Column Corridor) 레이다를 활성화하고, 중심점 매핑을 통해 진짜 함량 컬럼 구역이 아닌 구분(종) 기둥 등의 순번 숫자에 대해 점수를 대폭 감점(-50,000점)하여 완벽하게 소각(기각)함.
- **패턴 64**: 범위 기호 없는 단일 부등호 순번 노이즈 거세 및 함량 단독 강착 기전 (V24.6.3.2)
  - **설명**: `_normalize_single_content` 함수 내에 범위 연결 기호가 없으면서 단일 부등호만 단 1개 포착된 경우(예: 3 < 1), 전방의 불량 순번 숫자를 지워버리고 후방의 진짜 수치(`<1%`)만을 수거해 함량으로 단독 추출 및 강착시킴.
- **패턴 66**: 1선 완착 파이프라인 호환성 보존 및 콤팩트 로그 다이어트 연동 기전 (V24.6.0.0)
  - **설명**: 1선 추출 성공 및 실패 분기 시 즉시 튜플을 리턴하지 않고, 기존 엑셀 입고용 콤팩트 포장(딕셔너리 리턴) 구조를 100% 보존하여 후속 모듈과의 호환성을 사수함. 이와 동시에 최종 완료/실패 시점의 인쇄 로그는 `✅ [파일명] 완료 (성분: N건) /  └─ 최종 자산: CAS(함량), ...` 양식의 직관적인 다이어트 요약본으로 출력되도록 정밀 조율함.

