<USER_REQUEST>
🛠️ 업그레이드 및 버그 수정 반영 핵심 컴포넌트 소스코드 명세
안티그래비티가 smu_gui.py 내부 파일 수정 시 오차 없이 단 한 번에 이식을 마칠 수 있도록 수술 부위별 완성형 소스코드를 배출합니다.

📍 1. 4번 열 더블클릭 이벤트 Hijacking 버그 수정 (HTMLDelegate)
수술 지점: HTMLDelegate 클래스의 createEditor 내부 (기존 파일 410번 라인 부근)

변경 코드:

Python
class HTMLDelegate(QStyledItemDelegate):
    """[Part 15-B] 고도화된 델리게이트. 배경과 텍스트 드로잉 분리 및 다중행 편집 지원"""
    
    # --- [V11.0] 다중 행 편집기(QTextEdit) 지원 로직 이식 ---
    def createEditor(self, parent, option, index):
        # 🚨 [주님 버그 핀셋 교정] 4번 열(측정대상)은 QTextEdit가 이벤트를 가로채지 못하도록 무조건 None 반환!
        # 이 조치로 인해 더블클릭 시 수동 성상 선택 팝업창(SubstanceSelectDialog)이 즉시 기동합니다.
        if index.column() == 4:
            return None
            
        editor = QTextEdit(parent)
        editor.setAcceptRichText(False)
        editor.setStyleSheet("QTextEdit { padding: 3px; font-family: 'Malgun Gothic'; font-size: 9pt; }")
        return editor
📍 2. 상단 액션바 원터치 토글 단추 이식 (_create_extraction_page)
수술 지점: _create_extraction_page 함수 내 '실행 중지' 버튼 바로 하단부 (기존 파일 1200번 라인 부근)

변경 코드:

Python
        self.btn_stop = QPushButton("실행 중지")
        self.btn_stop.setFixedHeight(35); self.btn_stop.setStyleSheet("QPushButton { background-color: #d93025; color: white; font-weight: bold; } QPushButton:disabled { background-color: #eaa9a9; color: #ffffff; }")
        self.btn_stop.setEnabled(False)
        self.btn_stop.clicked.connect(self.stop_process)
        header.addWidget(self.btn_stop)

        # 🚨 [주님 요청 업그레이드] 하단 우측 교정창 원터치 접
<truncated 11594 bytes>
           self.correction_panel.hide()
            QMessageBox.information(self, "검증 완료", "2단계 API 검증이 완료되었습니다. (특이 명칭 교정 내역 없음)")
📍 6. 영구 보존용 HTML 보고서 빌더 연동 동기화 (export_correction_report)
수술 지점: export_correction_report 핵심 루프 구간 (기존 파일 1420번 라인 부근)

변경 코드:

Python
        for log in self.correction_logs:
            # 🚨 [주님 요청 사양] 메모리 버퍼 내 \n 글머리기호 구조를 HTML 표준 개행 태그(<br>)선으로 완벽 역치환
            html_reason = escape(log['reason']).replace('\n', '<br>')
            html_content += f"""
                    <tr>
                        <td style="text-align: center;"><b>{log['row']}</b></td>
                        <td>{escape(log['prod'])}</td>
                        <td class="highlight">{escape(log['corrected'])}</td>
                        <td>{html_reason}</td>
                    </tr>
            """
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-05-30T12:16:25+09:00.

The user's current state is as follows:
Other open documents:
- c:\Users\USER\Desktop\안티그래티비\MSDS_EXtaction_V3(v24+GUI통합)\scratch\test_regex_extraction.py (LANGUAGE_PYTHON)
- c:\Users\USER\Desktop\안티그래티비\MSDS_EXtaction_V3(v24+GUI통합)\msds_core.py (LANGUAGE_PYTHON)
- c:\Users\USER\Desktop\안티그래티비\MSDS_EXtaction_V3(v24+GUI통합)\msds_utils_v3.py (LANGUAGE_PYTHON)
- c:\Users\USER\Desktop\안티그래티비\MSDS_EXtaction_V3(v24+GUI통합)\smu_gui.py (LANGUAGE_PYTHON)
- c:\Users\USER\Desktop\안티그래티비\MSDS_EXtaction_V3(v24+GUI통합)\msds_index.json (LANGUAGE_JSON)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.5 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>