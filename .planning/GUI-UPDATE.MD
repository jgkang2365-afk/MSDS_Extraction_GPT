# 📋 안티(AI 에이전트) 주입용 최종 완전무결 로직 구현 명세서 (`로직 구현.MD`)

[CRITICAL CONTROL PROTOCOL]
1. 첨부된 '로직 구현.MD'는 주님과 조력자가 무결점 티키타카를 통해 최종 확정한 시스템 최상위 법전입니다.
2. 명세서에 기재된 Task 1부터 Task 5까지의 물리적 타겟 위치와 알고리즘 가이드라인을 100% 완벽히 이행하십시오.
3. 명세서에 언급되지 않은 기존의 안정적인 소스코드 인프라(파일 해시, 데이터 구조, 캐시 저장부 등)는 단 1바이트도 무단 변경하거나 삭제(하이재킹)하지 마십시오.
4. 자율적인 유령 코드를 임의로 추가하지 말고, 오직 지정된 데이터 정류 및 UI 렌더링 수술만 핀셋 형태로 안전하게 집행하십시오.

# 📋 안티(AI 에이전트) 주입용 최종 완전무결 로직 구현 명세서 (`로직 구현.MD`)

## 📌 [Version 2.2 - Complete Patch]

본 문서는 산업위생 작업환경측정 계획서 수립 자동화 시스템의 데이터 오염, 프로그램 마비, 시인성 붕괴, 모델명 코드 훼손 및 API 미조회 시의 데이터 누수를 원천 차단하기 위해 AI 에이전트(Anti, Cursor)에게 주입하는 최상위 물리적 로직 구현 지침서입니다.

---

## 1. 테이블 위젯 마스터 헤드명 정의 및 좌표계 고착

안티 에이전트는 화면단 UI 구현 시, 반드시 아래의 7대 고정 헤드명 레이아웃 순서 격자를 100% 절대 기준으로 삼아 셀 매핑 타격을 진행해야 합니다.
* **0번 열:** `순번` (물리 행 세로 병합 구역 - `setSpan`)
* **1번 열:** `No` (물리 행 세로 병합 구역 - `setSpan`)
* **2번 열:** `원본 제품명` (PDF 원석 100% 원형 보존 및 세로 병합 구역 - `setSpan`)
* **3번 열:** `CAS 원본(수정)` (성분 단위별 물리 행 분리 입력 구역)
* **4번 열:** `측정대상` (최종 성상 이름 및 규제 접두사 한정 마킹 구역)
* **5번 열:** `2차 결과(규제)` (순수 고시 표준 명칭 마킹 및 API 미조회 시 빈값 구역)
* **6번 열:** `1차 결과(전체)` (전체 매칭 내역 출력 구역)

---

## 2. 단계별 에이전트 코딩 집행 가이드라인

### 🛠️ [Task 1] 입구 관문 CAS 체인 정류 및 `원본 제품명` '절대 원형 보존' 공정
* **물리 타겟 위치:** `msds_core.py` ➔ `MSDSCore.extract_from_pdf(self, file_path)` 함수의 리턴 직전 구역
* **세부 집행 로직:**
  * **원본 제품명 100% 원형 보존:**
    * 제1항 구역에서 파싱 및 비전 스나이핑 엔진이 포착한 제품명 문자열에 대해서는 그 어떠한 영문 삭제, 정규식 가공 연산도 집행하는 것을 전면 절대 금지합니다.
    * `K-308`, `CR-13` 등 고유 모델 코드를 단 1바이트의 유실도 없이 100% 원형 그대로 수령하여 테이블의 2번 열 `원본 제품명` 셀 및 마스터 캐시에 적재합니다.
  * **성분 수치 가드레일:**
    * 구성성분 리스트들의 양끝 유령 공백을 `strip()`으로 제거하고 영문은 소문자로 강제 통일합니다.
    * 함유량 칸의 `balance`, `remainder`, `잔량`, `나머지` 등의 표현은 마스터 규격 약어인 `Rem.` 문자열로 일괄 강제 치환 매핑합니다.
    * 개별 성분들을 `CAS번호(함유량%)` 형태로 규격화하여 오직 `"; "` 구조로 결합한 뒤 `v2.0_Clean` 스냅샷 캐시 레이어 영역에 분리 보관합니다.

### 🛠️ [Task 2] 단일 마스터 통합 구성 위젯 창(Wizard) 기동 및 성상 낙찰
* **물리 타겟 위치:** `smu_gui.py` ➔ `ExtractionWorker` 완료 슬롯 내부 하단부
* **세부 집행 로직:**
  * 1단계 PDF 추출이 완료되는 즉시 백그라운드 분석을 통해 다중 성상 물질들이 포착되면, 대상 파일 내의 모든 선택 필요 성분들을 단일 표 구조로 시각화한 '단일 마스터 통합 구성 위젯 창'을 화면에 단 한 번만 트리거하여 표출합니다.
  * **UI 편의성 및 예외 조치:**
    * 위저드 창이 열릴 때 후보 성상 라디오 버튼들 중 첫 번째 항목에 디폴트 체크를 활성화해 둡니다.
    * 사용자가 창을 강제로 닫거나 취소를 누르는 경계 상황 발생 시, 가장 글자 수가 짧은 첫 번째 자식 성상 명칭을 시스템이 강제로 자동 임시 낙찰하여 `is_manual = True` 플래그와 함께 캐시에 강제 적재하여 데이터 공백을 원천 방어합니다.

### 🛠️ [Task 3] 물리 행 '가로 인덱스 동기화 락' 및 'API 미조회 물질 빈값 처리' 집행
* **물리 타겟 위치:** `smu_gui.py` ➔ `on_validation_finished` 및 `regenerate_validation_results` 연산 렌더링 구역
* **세부 집행 로직:**
  * **물리 행 가로 인덱스 동기화 락:**
    * 테이블 위젯 행 데이터 기입 시 타겟 행 좌표를 무지성 고정시키지 말고 반드시 `current_target_row = start_row + i` (여기서 `i`는 해당 제품 내 구성성분의 고유 index 루프 카운터) 공식으로 완벽하게 물리적 동기화 락을 채워 데이터 증발 현상을 100% 원천 봉쇄합니다.
  * **코샤 반환 물질명 내 영문/괄호 전량 삭제:**
    * 코샤 API 및 마스터 DB 매칭으로 반환된 문자열에서 뒤편에 지저분하게 묻어나오는 `(29~39%)` 등 모든 형태의 숫자/영문 괄호 덩어리는 정밀 정류 정규식(`re.sub(r'\s*\([^)]*\)', '', text)`)을 통해 연산단에서 완벽히 파쇄(삭제)합니다.
  * **API 미조회 물질 청정 빈값 처리:**
    * 코샤 API 호출 결과 객체가 `None`을 리턴하거나 규제 여부 플래그가 전부 `False`로 떨어져 기계 장부 조회가 불가능한 CAS 번호인 경우, 5번 열 `2차 결과(규제)` 셀 공간에는 `[미조회]` 등의 임시 토큰 문자열이나 주황색 배경색 하이라이트를 주입하는 행위를 절대 전면 엄금하며, 철저하게 완벽한 빈값(빈 문자열 `""`) 구조로 청정 출력해야 합니다.
    * 다만, 성분의 존재 식별을 위해 4번 열 `측정대상` 칸에는 [Task 1]의 상류 PDF 엔진이 최초 파싱해 온 원석 한글 물질명(예: `산화지르코늄`)을 백업 자산으로 정상 주입 표출합니다.
  * **규제 접두사 4번 열 한정 마킹:**
    * 코샤 API 15번 항목에서 탐지된 등급 접두사(`[특별]`, `[특검]`, `[허가]`) 문자는 오직 4번 열(`측정대상`) 셀의 국문 이름 전면에만 결합하여 노출하며, 5번 열 `2차 결과(규제)` 및 타 데이터 셀에는 접두사 기호를 붙이는 행위를 절대 전면 금지합니다.
  * **용접봉 GUI 화면 선제 주입 가드레일:**
    * 파일 제품명에 'CR-13' 또는 일반 '용접' 공정 키워드가 감지되는 행 구조인 경우, 최종 엑셀 저장 단계까지 기다리지 말고 현재 GUI 테이블 렌더링 루프 내부에서 즉각 발동시킵니다.
    * 기존 광물성 분진류 성분 칸을 화면에서부터 깨끗하게 지워버리고, 특수 지침에 의거한 `용접흄`과 `산화철(분진, 흄)` (CR-13의 경우 철, 망간, 이산화티타늄) 규격 자산을 4번 열 `측정대상`과 5번 열 `2차 결과(규제)` 화면상에 실시간으로 강제 주입 표출합니다.
  * **정렬 붕괴 원천 방어 및 setSpan 병합:**
    * 0번 열 `순번`, 1번 열 `No`, 2번 열 `원본 제품명` 셀은 `setSpan` 함수를 실행하여 수직 병합 처리하고, 화면 수동 정렬 기능(`setSortingEnabled`)은 완전히 비활성화 잠금 처리합니다.
    * 대신 로딩 시점에 내부 메모리 레벨에서 미리 완벽히 선정렬을 완료하여 화면에 청정 정착시킵니다.

### 🛠️ [Task 4] 직접 타이핑 수정 신호 차단 무한 루프 차단 및 우클릭 원복
* **물리 타겟 위치:** `smu_gui.py` ➔ `on_table_item_changed` 슬롯 및 테이블 위젯 우클릭 컨텍스트 메뉴 구현부
* **세부 집행 로직:**
  * 실무자가 4번 열 `측정대상` 셀을 직접 키보드로 수정하는 순간, 반드시 `self.tableWidget.blockSignals(True)` 명령을 선제 집행하여 변경 알림 신호를 물리적으로 끊어 무한 루프 화면 멈춤 버그를 차단합니다. 신호가 차단된 안전 스코프 내에서 수정한 텍스트 전체를 캐시 금고에 적재하고 `is_manual = True` 물리 락을 채웁니다.
  * 4번 열을 직접 타이핑 수정 완료했을 때, 인접한 5번 열 `2차 결과(규제)` 및 6번 열 `1차 결과(전체)` 등 타 데이터 셀은 기계가 최초 API로 검증했던 깨끗한 표준 상태를 묵묵히 그대로 유지하게 통제하며 동적 난도질을 방어합니다. 공정이 완료되면 즉시 신호 체인을 복구(`blockSignals(False)`)시킵니다.

### 🛠️ [Task 5] 후단 엑셀