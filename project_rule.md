# Project Rules

## 작업 완료 및 검수 인계 규칙

작업자가 `완료`라고 보고하는 시점은 로컬 구현이 끝난 시점이 아니라, **검수자가 원격 저장소에서 동일한 최종 결과를 확인할 수 있는 상태가 된 시점**이다.

### 완료 전 필수 조건

- 최종 변경사항이 모두 commit 되어 있어야 한다.
- 해당 작업의 원격 head branch에 push 되어 있어야 한다.
- Draft/Open PR이 작업 산출물인 경우 PR이 최신 head를 가리켜야 한다.
- 마지막 코드 변경 이후 최종 HEAD에서 필수 테스트를 다시 실행해야 한다.
- 작업지시서의 수정 금지 범위에 대한 diff 확인을 완료해야 한다.
- worktree의 `git status`가 의도한 상태인지 확인해야 한다. 완료 보고 시에는 원칙적으로 clean이어야 한다.

로컬에서만 수정이 끝났거나 commit/push/PR 갱신이 남아 있으면 `작업 완료`라고 보고하지 않는다. 이 경우 `로컬 구현 완료, 검수 준비 미완료`처럼 명시한다.

### 검수 인계 시 필수 보고 항목

완료 보고에는 검수자가 추가 질문 없이 대상을 식별할 수 있도록 다음을 반드시 포함한다.

- repository
- base branch 및 base SHA
- head branch 및 **원격 head SHA**
- PR 번호와 URL(해당하는 경우)
- 최종 commit SHA
- 변경 파일 목록 또는 범위 요약
- 실행한 검증 명령과 각 결과
- 변경 금지 범위 diff 확인 결과
- `git status` clean 여부
- CI/status check 유무와 결과. CI가 없으면 `CI 없음`이라고 명시
- 미검증 사항 또는 환경 제약
- 실제 사용 모델 및 오케스트레이션 확인 결과

### 원격 상태 확인

완료 보고 직전에 반드시 fetch/remote 조회 또는 동등한 방법으로 다음을 확인한다.

- 로컬 HEAD와 원격 head branch가 동일한가
- PR head SHA가 방금 보고할 SHA와 동일한가
- PR base가 작업지시서에서 지정한 branch인가
- push 이후 추가 로컬 commit이나 미커밋 변경이 없는가

이 중 하나라도 불일치하면 먼저 동기화하거나 불일치 사실을 보고하고, 검수 준비 완료로 표시하지 않는다.

## 오케스트레이션 및 사용 모델 관측 규칙

작업지시서는 사용자가 Orca/Codex의 실제 라우팅과 오케스트레이션이 정상 동작하는지 검수할 수 있도록 **실제로 사용된 모델을 확인 가능한 형태로 남기도록 요구해야 한다.**

### 모델 선택 원칙

- 작업지시서가 모델명을 임의로 고정하지 않는다. 최신 전역/프로젝트 `AGENTS.md`의 모델 라우팅 규칙을 따른다.
- 사용자가 특정 모델 또는 추론 수준을 명시적으로 승인한 작업만 예외로 한다.
- 보고하는 모델명·추론 수준·역할은 실제 세션/작업 메타데이터, Orca/Codex 로그, UI 또는 동등한 관측 근거에서 확인된 값이어야 한다.
- 확인할 수 없는 모델 정보를 추측하지 않는다. 관측 수단이 없으면 `확인 불가`와 그 이유를 명시한다.

### 작업 착수 시 확인

가능한 경우 착수 보고 또는 작업 로그에 다음을 남긴다.

- coordinator/orchestrator 사용 여부
- worker/sub-agent 사용 여부
- reviewer/검수 agent 사용 여부
- 각 역할에 실제 배정된 모델명
- 각 역할의 reasoning/effort 수준(관측 가능한 경우)
- 병렬/직렬 실행 여부

직접 단일 세션 작업이라 하위 오케스트레이션을 사용하지 않은 경우에는 `오케스트레이션 미사용`이라고 명시하고, 이를 모델 라우팅 실패로 오인하지 않도록 한다.

### 완료 보고 시 필수 모델/오케스트레이션 표

완료 보고에는 최소 다음 형식의 정보를 포함한다.

| Role | Model | Effort | Execution | Evidence |
| --- | --- | --- | --- | --- |
| Coordinator/Orchestrator | 실제값 또는 미사용 | 실제값/확인 불가 | direct/serial/parallel | session/log/UI 등 |
| Worker/Sub-agent | 실제값 또는 미사용 | 실제값/확인 불가 | direct/serial/parallel | session/log/UI 등 |
| Reviewer/Validation agent | 실제값 또는 미사용 | 실제값/확인 불가 | direct/serial/parallel | session/log/UI 등 |

여러 worker가 서로 다른 모델을 사용했으면 worker별로 행을 분리한다.

### 오케스트레이션 정상 여부 보고

오케스트레이션을 사용한 작업은 완료 보고에 다음을 추가한다.

- coordinator가 worker를 실제 dispatch했는지
- worker 결과가 coordinator에 회수되었는지
- 병렬 작업이 요청된 경우 실제로 병렬 실행되었는지
- 중단/유실/미회수 sub-agent가 있는지
- reviewer가 별도 역할로 실행되었는지
- 기대한 모델 라우팅과 실제 모델 라우팅이 일치했는지

불일치가 있으면 코드 결과가 정상이어도 숨기지 말고 `오케스트레이션/라우팅 불일치`로 별도 보고한다.

## 작업지시서 작성 규칙

모든 구현 작업지시서의 `완료 조건 / 검증 / PR 정책`에는 최소한 다음을 명시한다.

1. 최종 HEAD에서 필수 테스트 실행
2. 최종 변경 commit 및 원격 branch push
3. PR head SHA 최신화 확인
4. base/head branch와 SHA를 완료 보고에 명시
5. 변경 금지 범위 diff 확인
6. `git status` 확인
7. 검수자가 GitHub/원격 저장소에서 즉시 검수 가능한 상태인지 확인
8. 실제 사용 모델과 coordinator/worker/reviewer 역할별 모델 배정 결과 보고
9. 오케스트레이션 사용 시 dispatch/회수/병렬성/라우팅 일치 여부 보고

작업지시서 상단 시작 문구에도 `AGENTS.md`와 `project_rule.md`를 읽고 최신 모델 라우팅 규칙을 따르며, 완료 시 실제 사용 모델을 보고하라는 요구를 포함한다.

사용자에게 수동 Git 명령 실행이나 검수 대상 찾기를 떠넘기지 않는다.

### 금지

- push하지 않은 로컬 결과를 `완료`라고 보고하지 않는다.
- PR 본문에 오래된 테스트 수치나 오래된 head SHA를 남긴 채 완료 보고하지 않는다.
- 사용자 승인 없이 force push, amend, rebase로 검수 기준 SHA를 바꾸지 않는다.
- CI가 없는데 CI PASS로 표현하지 않는다.
- 실제 관측 근거 없이 사용 모델이나 추론 수준을 추정하여 보고하지 않는다.
- 최신 `AGENTS.md` 라우팅을 무시하고 작업지시서에서 임의 모델을 고정하지 않는다.
